# API Standards — Mobile (Backend NestJS Terpisah, Kontrak Dibagi dengan Web)

Semua aturan pemakaian API dan session dari **sisi mobile app**. Backend adalah NestJS + Sequelize/TypeORM campuran + MySQL, source ada di `/Users/asdarsaid/JSI/api` (branch aktif `persiapan-2029`, dikonfirmasi branch terbaru 2026-08-23 — lihat § Hasil Rekap untuk histori verifikasi).

**⚠️ Repo backend ini BERUBAH dari waktu ke waktu** (module `timses` yang dulu dibaca Feature 02 sudah tidak ada lagi per 2026-08-23, diganti modul `user`; base URL yang dulu butuh prefix `/api` ternyata sudah tidak berlaku) — **jangan percaya begitu saja dokumentasi lama di file ini untuk modul manapun tanpa re-verifikasi cepat kalau ada tanda 404/error yang tidak masuk akal.** Ikuti § Alur Verifikasi Endpoint di bawah SETIAP kali mau implementasi/wiring endpoint baru atau curiga kontrak lama sudah basi — ini aturan tetap (permintaan eksplisit user 2026-08-23), bukan cuma untuk sesi itu.

---

## Alur Verifikasi Endpoint (WAJIB sebelum wiring apapun ke backend real)

Urutan ini **wajib** dijalankan berurutan setiap kali sebuah fitur mobile mau di-real-kan (ganti dari mock ke API sungguhan) atau setiap kali ada kecurigaan kontrak yang didokumentasikan di file ini sudah tidak akurat lagi (mis. error 404 yang tidak masuk akal). Jangan langsung menebak/mengarang endpoint, dan jangan langsung percaya dokumentasi lama tanpa cek ulang.

1. **Cek dulu ke frontend web** (`/Users/asdarsaid/JSI/client`) — cari halaman/komponen yang memakai fitur setara, baca endpoint apa yang dipanggil (`axios.get/post/...`), body/params-nya, dan base URL yang dipakai (cek apakah proyek itu pakai host/proxy khusus seperti `baseUrl2024` — jangan asumsikan semua fitur web pakai host yang sama, lihat § Hasil Rekap untuk contoh nyata 2 host berbeda).
2. **Cocokkan ke repo API** (`/Users/asdarsaid/JSI/api`) — cari controller yang menangani path itu (`grep -rn "@Controller" src/`), baca route (`@Get`/`@Post`/dst.), guard (`@UseGuards`), dan DTO/model yang direspons. Konfirmasi apakah endpoint itu **masih ada, sudah pindah modul, atau sudah dihapus** — jangan asumsikan dokumentasi lama di file ini masih akurat, backend ini terbukti berubah.
3. **Kalau ragu (endpoint tidak ketemu, atau bentuk response tidak jelas dari baca kode saja) — tes langsung** via `curl` read-only ke `https://api.demokrasikreatif.org/{endpoint}` (GET aman langsung; untuk route POST-only, tes dengan body kosong/`{}` untuk lihat apakah balas 422/400 validasi — **route ADA** — vs 404 "Cannot POST/GET" — **route TIDAK ADA**; JANGAN kirim kredensial asli lewat curl). Ini bukti paling kuat, lebih diutamakan daripada baca kode kalau ada konflik.
4. **Laporkan ke user SEBELUM implementasi** — khusus bagian yang: (a) tidak cocok antara observasi web vs repo API vs hasil tes live, atau (b) endpoint/kapabilitasnya sama sekali tidak ditemukan di manapun. Jangan diam-diam mengarang jalan keluar (Aturan #6 CLAUDE.md) — tanya dulu, baru lanjut kalau sudah dikonfirmasi.
5. **Update dokumentasi ini** (`api-standards.md`) begitu ketemu kontrak yang benar — supaya sesi berikutnya tidak mengulang riset yang sama, dan supaya bagian yang ternyata basi jelas ditandai (strikethrough + catatan koreksi, bukan dihapus diam-diam — lihat pola koreksi Auth Flow/Timses di bawah sebagai contoh).

---

## Environment Variables

```bash
# .env — JANGAN pernah di-commit
EXPO_PUBLIC_API_URL=...        # analog VITE_API_URL di client/.env — sudah termasuk suffix /api
EXPO_PUBLIC_USE_MOCK_API=...   # "true" = semua service pakai mock, lihat § Mock Mode
```

Aturan:

- Expo hanya mengekspos env var ke client kalau diberi prefix `EXPO_PUBLIC_` — jangan pakai prefix ini untuk apapun yang sensitif
- `.env.example` selalu berisi nama variabel tanpa value — update saat ada env baru

---

## Mock Mode (aktif sejak 2026-08-21 — lihat progress-tracker.md Decisions)

**Kenapa:** `https://api.demokrasikreatif.org` (base URL production, dikonfirmasi user) diproteksi **Cloudflare Managed Challenge** di seluruh domain (`cf-mitigated: challenge` di response header, HTTP 403 dengan body HTML "Just a moment..." untuk SEMUA request — bukan cuma `/auth/login`, dicoba juga `GET /api`). Ini memblokir client non-browser (axios dari mobile app, `curl`, dst.) di lapisan Cloudflare **sebelum** request sampai ke NestJS. Ini masalah infrastruktur di luar scope repo `jsi-mobile` maupun `api` — butuh perubahan konfigurasi zone Cloudflare (whitelist/bypass Managed Challenge untuk path API) oleh siapa pun yang pegang akses dashboard-nya.

**Cara kerja toggle:**
- `.env`: `EXPO_PUBLIC_USE_MOCK_API=true` → semua service function cek `isMockApiEnabled()` (`src/lib/api/mock.ts`) di awal, dan short-circuit ke data hardcode/canned kalau `true`, skip `apiClient` sama sekali.
- Set `"false"` (atau hapus) begitu Cloudflare sudah dibuka aksesnya — service otomatis balik manggil endpoint asli tanpa perlu diedit lagi (mock ada di cabang terpisah dalam fungsi service yang sama, bukan ganti implementasi).

**Pola yang WAJIB diikuti tiap kali service baru dibuat (dtdoor, gotv, timses, dst.) selama flag ini `true`:**
1. Definisikan data mock secukupnya untuk memverifikasi UI (list kosong/isi, minimal 1 contoh per state yang relevan)
2. Tambahkan cabang `if (isMockApiEnabled()) return mock...(...)` di awal fungsi service, SEBELUM blok `try` yang manggil `apiClient`
3. Simulasikan delay (`mockDelay()`) supaya loading state tetap bisa diverifikasi visual
4. Tiru behavior/pesan error backend asli semirip mungkin (lihat contoh `loginRequestMock` di `src/services/auth.ts`) — supaya tidak ada kejutan pas mock dimatikan
5. Kalau screen butuh visibilitas kredensial/data demo untuk user testing (seperti Login), tampilkan hint singkat yang **hanya render saat `isMockApiEnabled()` true** — jangan biarkan nempel di production build

**Status saat ini:** hanya `src/services/auth.ts` (`loginRequest`) yang sudah punya cabang mock (2 akun demo: `admin.jsi`/`admin123` role admin, `yayat.hidayat`/`timses123` role timses). Modul lain belum ada service-nya sama sekali (Phase 2/3 belum dikerjakan) — begitu dibangun, ikuti pola di atas selama flag masih `true`.

---

## Struktur Client

```typescript
// src/lib/api/client.ts
import axios from "axios";
import { getToken } from "@/lib/api/token";

export const apiClient = axios.create({
  baseURL: process.env.EXPO_PUBLIC_API_URL,
});

apiClient.interceptors.request.use(async (config) => {
  const token = await getToken();
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});
```

```typescript
// src/lib/api/token.ts — implementasi asli sekarang simpan token + user (lihat Auth Flow poin 6)
import * as SecureStore from "expo-secure-store";

export async function getToken(): Promise<string | null> { ... }
export async function setSession(token: string, user: AuthUser): Promise<void> { ... }
export async function getSession(): Promise<Session | null> { ... }
export async function clearSession(): Promise<void> { ... }
```

Catatan: JWT dari NestJS jauh di bawah batas ukuran `expo-secure-store` (~2048 byte) — diverifikasi saat implementasi Feature 02 (payload login berisi `id`/`nik`/`nama_lengkap`/`roles`, jauh lebih kecil dari batas), tidak perlu pola penyimpanan alternatif.

---

## Auth Flow

**Terverifikasi langsung ke `/Users/asdarsaid/JSI/api/src/auth` saat implementasi Feature 02 (2026-08-21) — kontrak ini BEDA dari yang diamati di `client/src/pages/Login.jsx`.** Web ternyata memanggil endpoint dengan field/shape yang tidak dibaca backend NestJS ini (lihat Decisions di `progress-tracker.md` untuk detail penuh). Ikuti kontrak backend di bawah, bukan pola web.

**⚠️ 2026-08-23 — KOREKSI poin 1 & 6 di bawah, backend sudah berubah sejak dokumentasi ini ditulis.** Checkout lokal `/Users/asdarsaid/JSI/api` sekarang di branch `persiapan-2029` (lihat § Hasil Rekap untuk histori verifikasi branch) — `main.ts` di branch ini **TIDAK** `setGlobalPrefix('api')` (dibaca langsung, dan dikonfirmasi live: `POST https://api.demokrasikreatif.org/auth/login` balas 422 validation — artinya route ADA tanpa `/api`; `POST .../api/auth/login` balas 404 "Cannot POST"). Modul `timses` (dipakai entity `Timse` TypeORM, poin 6) juga **sudah tidak ada** di branch ini — diganti modul `user` (Sequelize `TimsesModel`, `@Controller('user')`, self-service via JWT `GET/POST /user/profile`, bukan lagi `GET/PATCH /timses/:id`). Kemungkinan besar dokumentasi Feature 02 ditulis waktu checkout lokal masih di branch lama (`master` atau lainnya) yang beda dari yang sekarang aktif — **jangan percaya "sudah terkonfirmasi" begitu saja untuk modul manapun di sini tanpa re-verifikasi cepat (curl/baca controller) kalau ada tanda-tanda 404, karena branch backend bisa saja sudah berubah lagi.** `src/services/profile.ts` & `src/services/timses.ts` (mobile) **BELUM diperbaiki** untuk kontrak baru ini — akan 404 kalau `EXPO_PUBLIC_USE_MOCK_API=false` (lihat progress-tracker.md Decisions 2026-08-23 untuk detail & rencana).

**⚠️ 2026-08-23 (lanjutan) — KOREKSI TOTAL poin 3, 4, 5 di bawah, dijalankan via § Alur Verifikasi Endpoint (cek web → cocokkan repo API → tes live curl, ketiganya cocok satu sama lain).** `main.ts` ternyata pasang `app.useGlobalPipes(new CustomValidationPipe())` DAN `app.useGlobalInterceptors(new TransformInterceptor())` global — dokumentasi lama ("tidak ada ValidationPipe/DTO binding", "flat bukan `{data:{data}}`") **salah dari awal** (bukan drift baru, DTO `SignInDto` sudah begitu bentuknya). `src/services/auth.ts` (mobile) sudah diperbaiki mengikuti kontrak di bawah.

1. ~~Base URL wajib menyertakan prefix `/api`~~ **KOREKSI: TIDAK.** `.env` sekarang `EXPO_PUBLIC_API_URL=https://api.demokrasikreatif.org` (TANPA `/api`) — lihat catatan koreksi di atas. `/backend-api` yang dipakai web **hanya path proxy dev Vite** (`vite.config.js` rewrite ke root) — tidak ada di backend asli.
2. Ambil lokasi GPS (`expo-location`) — **wajib secara UX mobile** (blok tombol submit sampai granted), TAPI backend **tidak membaca/memvalidasi field `location` sama sekali** (`SignInDto.location` tidak punya decorator validasi) — ini murni gate client-side. Tetap kirim `location: { lat, long }` di body untuk forward-compat (harmless, backend abaikan field ekstra).
3. ~~`POST .../auth/login` dengan body `{ nik, password, location }` — field-nya `nik`, BUKAN `username`~~ **KOREKSI: field-nya `username`, BUKAN `nik`.** `SignInDto` (`src/auth/dto/sign.dto.ts`) declare `username: string` (divalidasi `@UsernameOrEmail` — regex alfanumerik/titik/underscore/dash ATAU format email) + `password: string` (`@IsString()`) + `location?: {lat,long}` (tanpa decorator). Body yang benar: `{ username, password, location: { lat, long } }`. Nilai `username` yang dikirim tetap dicocokkan ke **kolom `nik`** di `AuthService.loging` (`findOne({ nik: username })`) — jadi secara konsep tetap NIK, cuma key JSON-nya `username`. Dikonfirmasi identik dengan yang dikirim `client/src/pages/Login.jsx`.
4. ~~Response sukses: `{ payload: {...}, acces_token }` flat~~ **KOREKSI: dibungkus `{ message, data }` oleh `TransformInterceptor` global**, dan `acces_token` ada DI DALAM `data`, sejajar field user (bukan sibling `payload`). Bentuk asli: `{ message: "Login Success", data: { id, nik, nama_lengkap, namaLengkap, roles, acces_token } }` (HTTP 200). Catatan: `nama_lengkap` (snake_case) DAN `namaLengkap` (camelCase) SAMA-SAMA ada di response (duplikat sengaja di `AuthService`), mobile pakai `namaLengkap` (cocok dengan yang dibaca `client/src/pages/Login.jsx` via `data.data.namaLengkap`).
5. ~~Response gagal: selalu HTTP 500 generik~~ **KOREKSI: HTTP 422 terstruktur, pesan beda per jenis kegagalan** — `ValidationException` (`src/commons/exceptions/validation.exception.ts`) + `ExceptionBaseFilter` (`src/commons/filters/fastify-exception.filter.ts`) balas `{ message: "<pesan>", errors: { "<field>": "<pesan>" } }`, semuanya HTTP 422 (`UNPROCESSABLE_ENTITY`):
   - Body tidak valid (field kosong/tipe salah): `message: "Validation failed"`, `errors: { <field>: "<pesan class-validator>" }` — **diverifikasi live** (`POST` body `{}` → `422 {"message":"Validation failed","errors":{"password":"password must be a string"}}`).
   - Username/NIK tidak ditemukan: `message: "NIK/EMAIL/USERNAME SALAH"`, `errors: { username: "NIK/EMAIL/USERNAME SALAH" }` — **diverifikasi live** (username acak → 422 persis ini).
   - Password salah: `message: "Password SALAH"`, `errors: { password: "PASSWORD SALAH" }` — dari baca `AuthService.loging` langsung (tidak dites live karena butuh username asli yang valid).
   - `error.response.data.message` tetap bisa dipakai apa adanya untuk pesan generik (field ada di kedua kasus lama & baru) — mobile TIDAK perlu decode per-status-code. Field `errors` tersedia kalau nanti mau tampilkan error per-input (belum dipakai, `LoginScreen` masih 1 baris error generik — di luar scope perbaikan kontrak ini).
6. ~~Tidak ada endpoint `GET /user/profile` di repo ini~~ **KOREKSI 2026-08-23: SEKARANG ADA** — `@Controller('user')` (`src/user/user.controller.ts`), `@UseGuards(AuthGuard)`, route `GET /user/profile` (baca `@User()` dari JWT, tidak butuh `:id`) & `POST /user/profile` (update, body `UserDTO`, juga dari JWT bukan param). Response `apiResponse('User Profile', profile)` — `profile` = full row `TimsesModel` (Sequelize, kolom sama dengan `Timse` TypeORM lama secara konsep tapi BELUM di-diff field-by-field). Session login (`payload` dari `POST /auth/login`) tetap dipakai untuk info awal (`id`/`nik`/`nama_lengkap`/`roles`), tapi field tambahan (No. HP, kecamatan/desa/dusun) yang sebelumnya di-fetch via `GET /timses/:id` sekarang **harus** lewat `GET /user/profile` — `src/services/profile.ts` BELUM diupdate ke kontrak baru ini (masih panggil `/timses/:id`, akan 404).
7. Simpan token + user via `src/lib/api/token.ts` (`setSession`/`getSession`/`clearSession`)
8. Role (`admin` / `adminsekret` / lainnya = timses & varian relawan*) menentukan tab navigator mana yang dirender — lihat `RootNavigator.tsx`. Nilai `roles` adalah **satu string**, bukan array, meski tipe TS di backend (`Timse.roles: Role[]`) menyiratkan array — kolom TypeORM-nya `type: 'enum'` tanpa `array: true`, jadi runtime-nya single value (konsisten dgn `default: Role.RELAWAN` dan cara web membandingkannya `roles === "admin"`).
9. 401 di response manapun (endpoint terproteksi lain, bukan `/auth/login` yang selalu 500) → hapus token, kembali ke `AuthStack` — diimplementasikan via `registerUnauthorizedHandler` di `src/lib/api/client.ts` + `AuthProvider` di `src/hooks/useAuth.tsx`.

**Catatan:** `JwtStrategy.validate` (`/Users/asdarsaid/JSI/api/src/auth/jwt.strategy.ts`) baca `payload.sub` untuk `id`, padahal `auth.service.ts` sign payload dengan key `id` (bukan `sub`) — kemungkinan bug backend (di luar scope repo ini untuk diperbaiki), efeknya `req.user.id` di endpoint terproteksi lain kemungkinan `undefined`. Belum relevan untuk mobile karena belum ada endpoint terproteksi yang dipakai.

---

## Realtime (Socket.io)

Web (`client/src/pages/dpt/layout-hook.js`) connect ke `io(proxyTarget, { query: { token } })` untuk event `onlineUsers`. Mobile mengikuti pola yang sama: connect setelah login pakai `EXPO_PUBLIC_API_URL` + token, disconnect saat logout/unmount. Verifikasi shape event yang sebenarnya ke `/Users/asdarsaid/JSI/api` (cek gateway socket.io di sana) sebelum implementasi — jangan asumsi identik dengan yang diamati di web kalau backend NestJS ini ternyata sudah beda kontrak dari backend lama yang dipakai web.

---

## Known Endpoints (per modul)

### Auth — repo ada (`/Users/asdarsaid/JSI/api/src/auth`)
- `POST {EXPO_PUBLIC_API_URL}/auth/login` (base URL sudah termasuk `/api`) — lihat Auth Flow di atas untuk kontrak lengkap yang sudah diverifikasi
- Tidak ada endpoint profile terpisah — lihat Auth Flow poin 6

### Door To Door / GOTV / Ruang Publik / Timses — repo ada
- Baca `/Users/asdarsaid/JSI/api/src/dtdoor`, `src/gotv`, `src/ruangpublik` langsung untuk daftar endpoint & DTO. Jangan salin daftar dari `client` tanpa cross-check — web bisa saja memanggil endpoint versi lama yang beda dari NestJS API ini.
- **⚠️ Modul `timses` (TypeORM, dibaca Feature 03) SUDAH TIDAK ADA di branch `persiapan-2029` (dikonfirmasi 2026-08-23, `find`/`grep` ke seluruh `src/` nihil) — koreksi total terhadap 4 poin di bawah, disimpan dengan strikethrough untuk jejak sejarah.** Diganti modul `user` (`src/user/`, Sequelize `TimsesModel`) — **self-service SAJA**, lihat § Hasil Rekap area koreksi Auth Flow poin 6 untuk kontrak barunya (`GET/POST /user/profile`). Perbedaan besar: `TimsesModel` cuma punya kolom `id/nik/namaLengkap/jenisKelamin/roles/statusOnline` — **kolom `dusun`/`desa`/`kecamatan`/`no_telpon`/`lat`/`long` SUDAH TIDAK ADA SAMA SEKALI** (bukan cuma pindah endpoint). Dampak:
  - `src/types/profile.ts` (`TimsesProfile`) — field-field itu **dihapus** dari type (bukan dibuat opsional), supaya tiap pemakaian lama ketahuan via error compile.
  - **Tidak ada endpoint list-semua-anggota-timses lagi** — dicari ke seluruh backend, nihil. `TimsesScreen`/`LacakRelawanScreen` (`src/services/timses.ts` § `fetchTimsesList`) **sengaja `throw` pesan jelas** (Aturan #6, bukan menebak endpoint) — akan tampil "gagal memuat" sampai ada sumber data pengganti.
  - **Scoping wilayah (role non-admin dibatasi ke kecamatan sendiri) SEMENTARA dimatikan di 9+ screen** (`TimsesScreen`, `KekuatanWilayahScreen`, `KekuatanPemilihScreen`, `SwingVoterFollowUpScreen`, `QuickCountScreen`, `DtdoorAnalyticsScreen`, `TokohMasyarakatScreen`, `HomeScreen`, `ProfileScreen`/`AccessScopeNotice`) — permintaan eksplisit user, lihat progress-tracker.md Decisions 2026-08-23. Semua role sekarang lihat semua wilayah.
  - `useLocationBeacon`/`updateOwnLocation` (`src/services/timses.ts`) — **no-op langsung** (bukan coba PATCH lalu gagal) karena tidak ada kolom lat/long lagi untuk disimpan.
  - ~~`GET /timses/:id` untuk baca profil, `PATCH /timses/:id` untuk update~~ **KOREKSI: `GET /user/profile` & `POST /user/profile`** (self-service dari JWT, tidak ada `:id`). `POST /user/profile` cuma terima `{ namaLengkap, jenisKelamin }` (DTO `UserDTO`) — `no_telpon` **tidak bisa lagi dikirim** (kolomnya sudah tidak ada), `services/profile.ts` mengirim `jenisKelamin` = nilai profil yang sedang aktif (backend `@IsEnum` mewajibkan field ini, bukan optional).
  - **⚠️ Bug ditemukan & diperbaiki 2026-08-23 (sesi lanjutan setelah fix Auth Flow di atas):** `GET /user/profile` JUGA dibungkus `{ message, data }` oleh `TransformInterceptor` global yang sama (`main.ts`, berlaku app-wide — bukan cuma `/auth/login`), tapi `services/profile.ts::fetchProfile()` sempat membaca body response langsung sebagai record profil tanpa unwrap sekali lagi (`data.data`), sama persis kelas bug yang baru diperbaiki di Auth Flow. Dikonfirmasi via baca `user.controller.ts` (`apiResponse('User Profile', profile)`) + cross-check `client/src/pages/dpt/layout-hook.jsx` yang eksplisit baca `resp.data["data"]`. Sudah diperbaiki — lihat progress-tracker.md Decisions.
  - ~~`GET /timses/:id` balas entity utuh termasuk hash `password`~~ tetap berlaku secara prinsip (defaultScope `TimsesModel` exclude `password` dari query, jadi sebenarnya sudah aman di level model — tidak perlu mapping manual lagi untuk menghindari field ini).
  - ~~`PATCH /timses/:id` balas TypeORM UpdateResult, bukan record ter-update~~ **KOREKSI: `POST /user/profile` juga tidak balas record ter-update** (`UserService.profileUpdate` balas hasil `TimsesModel.update()`, Sequelize) — `services/profile.ts` tetap fetch ulang via `GET /user/profile` setelah sukses, pola yang sama masih berlaku.
  - Tidak ada kolom `email` di `TimsesModel` (tetap sama seperti sebelumnya) — field ini tetap `editable={false}` di UI.
  - `UserController` di-guard `AuthGuard` (beda nama class dari `JwtAuthGuard` lama, fungsinya sama — proteksi JWT). Tidak ada role/ownership guard tambahan, tapi karena endpoint sekarang murni self-service (ambil `id` dari token, bukan dari path), isu "bisa panggil untuk id siapa saja" yang dulu ada di `PATCH /timses/:id` **otomatis tidak relevan lagi**.
- **Door To Door (Feature 04, dipakai `src/services/dtdoor.ts`):** entity `Dtdoor` (`/Users/asdarsaid/JSI/api/src/dtdoor/entities/dtdoor.entity.ts`) — hanya `nama_lengkap` & `jumlah_wajib_pilih` yang `NOT NULL`, sisanya nullable. Endpoint dipakai: `GET /dtdoor/data` (list, params `page`/`limit` saja — filter/search lain tersedia tapi tidak dipakai, di luar scope Feature 04), `GET /dtdoor/count` (angka mentah), `POST /dtdoor` (create). Catatan kontrak penting:
  - **Tidak ada auth guard sama sekali** di seluruh `DtdoorController` — guard global (`RolesGuard`, `main.ts`) ternyata no-op (`canActivate()` selalu `true`). Semua endpoint dtdoor bisa diakses tanpa login. Backend gap, di luar scope repo ini.
  - **`POST /dtdoor` upsert-by-`idDpt` sebelum insert** (lihat `dtdoor.controller.ts`): kalau ada row existing dengan `idDpt` (+`kabId` kalau ada) yang sama, controller malah `update()` row itu (balas `UpdateResult` mentah, bukan entity) alih-alih insert baru. Karena mobile belum ada linkage DPT asli (lihat Decisions `progress-tracker.md`), `services/dtdoor.ts` **generate `idDpt` sintetis unik tiap submit** (`generateSyntheticIdDpt()`) supaya cabang `create()` (balas entity fresh) yang selalu dieksekusi — JANGAN kirim `idDpt` kosong/undefined ke endpoint ini.
  - Relasi `kategori` (FK ke `KategoriDtDoor`) **tidak punya endpoint lookup** untuk baca daftar id/label valid. Mobile pakai daftar hardcode di `src/types/dtdoor.ts` (`KATEGORI_DTDOOR_OPTIONS`, dari screenshot user) dengan id diasumsikan berurutan 1–7 — **belum diverifikasi ke tabel `kategori_dt_door` asli**, wajib dicek sebelum production.
  - Relasi `status` (FK ke `StatusDtDoor`) **sengaja tidak dipakai** di Feature 04 — dikonfirmasi user berasal dari relasi DPT↔dtdoor (jadi "Sudah Dikunjungi" begitu DPT ter-link), bukan sesuatu yang di-set dari form kunjungan standalone.
  - `program_bantuan1/2/3` adalah string bebas di kolom `Dtdoor`, **bukan** relasi FK — entity `BantuanDtDoor` di folder yang sama ternyata tidak pernah direferensikan dari `Dtdoor`, jangan dikira relasi.
  - `idDpt` (opaque string) dan `kabId` (number) **tidak berelasi ke tabel DPT manapun** — tidak ada entity DPT di repo backend ini sama sekali (dikonfirmasi `find -iname "*dpt*"` nihil). Perlakukan sebagai field string/angka lepas, bukan foreign key.
  - **⚠️⚠️ 2026-08-23 — KOREKSI TOTAL, seluruh bullet di atas basi.** Modul `dtdoor` ternyata sudah di-desain ulang total (Sequelize `src/database/models/timses/Dtdoor.ts`, BUKAN lagi TypeORM `entities/dtdoor.entity.ts` yang sudah tidak dipakai controller manapun) — dikonfirmasi baca `dtdoor.controller.ts`/`dtdoor.service.ts`/model langsung + **live curl `GET /dtdoor?page=1&limit=2`** (200, data produksi asli, 1 record real "MUHAMMAD SYAH" Aceh Selatan) + **live curl `POST /dtdoor` body kosong & body sebagian** (422, daftar field wajib lengkap). Kontrak baru:
    - **Path list `GET /dtdoor` (root, BUKAN `/dtdoor/data`)** — path lama sekarang match ke `@Get(':idDpt')` (dianggap `idDpt="data"`, balas 200 `{data:{}}` bukan 404 — silent-wrong, bukan error yang jelas, jadi gejalanya bukan "gagal memuat" tapi list kosong-aneh). `GET /dtdoor/count` **SUDAH TIDAK ADA SAMA SEKALI** di controller — diganti derive dari `meta.total` hasil `GET /dtdoor?limit=1`.
    - Response list dibungkus `TransformInterceptor` (app-wide, sama seperti Auth/Profile) jadi `{message, data: DtdoorRecord[], meta: {page,limit,offset,totalPages,total}}` — field CAMELCASE (`namaLengkap`, `jenisKelamin`, `noTelpon`, `jumlahWajibPilih`, dst.), BUKAN snake_case seperti diasumsikan sebelumnya. `findAll()` (backend) **SELALU `include` relasi `kunjungans` dengan `required:true`** (INNER JOIN) — record TANPA kunjungan tidak akan pernah muncul di list.
    - **Field kategori/program-bantuan pindah dari top-level record ke array nested `kunjungans[]`** — tiap kunjungan: `{id, tipePemilihId, pilihanPilegId, programBantuanId, merchendise, namaRelawan, kontakRelawan, tipePemilih:{id,nama}, pilihanPileg:{id,nameKategori}, programBantuan:{id,nama}}`. Kolom lama (`kategoriId`, `programBantuan1/2/3`, `statusId`) MASIH ADA di model tapi TIDAK diisi lagi oleh create baru (`null` untuk record baru) — cuma tersisa di data lama.
    - **3 endpoint lookup BARU ditemukan** (dulu dikira tidak ada endpoint lookup kategori sama sekali) — **dikonfirmasi live**: `GET /dtdoor/tipe-pemilih` → `{id,nama}[]`, isinya **PERSIS SAMA** (id 1-7, label sama) dengan `KATEGORI_DTDOOR_OPTIONS` hardcode lama (jadi asumsi id 1-7 lama TERNYATA BENAR, meski sumbernya ternyata endpoint ini bukan tebakan screenshot). `GET /dtdoor/pilihan-pileg` → `{id,nameKategori}[]` (7 nilai "A"-"G", konsep BARU tidak pernah ada di UI mobile sebelumnya — representasi pilihan partai/caleg generik). `GET /dtdoor/program-bantuans` → `{id,nama}[]` (8 nilai, sama persis `PROGRAM_BANTUAN_OPTIONS` lama).
    - **`POST /dtdoor` body baru (dikonfirmasi live via curl validasi)** — WAJIB: `nik, namaLengkap, desa, kecamatan, kabupaten, tps, rt, rw, jenisKelamin, jumlahWajibPilih (number), idDpt (number, BUKAN string!), kabId (number), kelId (number), kunjungans (array, min 1 — tiap elemen wajib tipePemilihId/pilihanPilegId/programBantuanId/merchendise/namaRelawan, kontakRelawan opsional)`. `noTelpon` opsional.
    - **Bug backend baru ditemukan (beda dari upsert-by-idDpt lama, TETAP RELEVAN):** `DtdoorService.create()` sesudah insert melakukan `WilKab2024.findOne({where:{wilId: created.kabId}})` lalu langsung akses `.proKode` **TANPA null-check** — kalau `kabId` bukan wilId kabupaten yang benar-benar ada di tabel `WilKab2024`, request **crash 500** (bukan validasi 422 yang rapi). **`kabId` WAJIB berupa kabupaten nyata** — mobile pakai `STANDALONE_KAB_ID=7303` (Bantaeng, satu-satunya kabupaten dengan data DPT lengkap yang dikonfirmasi user) sebagai default untuk entri standalone tanpa linkage DPT (lihat `src/services/dtdoor.ts`). Upsert-by-`idDpt` lama (silent update kalau `{idDpt,kabId}` match row lain) **masih berlaku juga** — tetap wajib generate `idDpt` unik tiap submit, sekarang via `generateSyntheticIdDptNumber()` (angka, bukan string — lihat `src/lib/api/syntheticId.ts`).
    - **⚠️ Bug backend KEDUA ditemukan 2026-08-23 (laporan user setelah tes device pertama):** `DtdoorService.create()` juga jalankan `Dtdoor.findOne({where:{rt,rw,tps,kepalaKeluargaId: createDtdoorDto.kepalaKeluargaId}})` (cek "apakah kepala keluarga ini sudah ada") — Sequelize **throw error kalau `kepalaKeluargaId` absen dari body** (`WHERE parameter "kepalaKeluargaId" has invalid "undefined" value", bukan 422 rapi, tidak bisa dibedakan dari error lain via status code). `kepalaKeluargaId` OPSIONAL di DTO (`@IsOptional()`), jadi wajar kalau mobile tidak mengirimnya — tapi service tidak guard `undefined` sebelum masuk `where`. **Fix (mobile-side, backend tidak bisa diubah dari repo ini):** body create SELALU sertakan `kepalaKeluarga: null, kepalaKeluargaId: null` eksplisit (bukan diomit) — `@IsOptional()` di class-validator meloloskan `null` (beda dari `undefined`), dan Sequelize `where: {kepalaKeluargaId: null}` valid (`IS NULL`), tidak crash. **Pola ini WAJIB diikuti tiap kali ada field opsional lain di endpoint manapun yang backend-nya pakai Sequelize `where` langsung dari DTO tanpa guard** — kirim `null` eksplisit, jangan pernah omit key kalau ragu.
    - `mapDtdoor()` (`src/services/dtdoor.ts`) sekarang fallback `kategoriId`/`kategoriLabel`/`programBantuan1-3`/`namaRelawan`/`kontakRelawan` dari `kunjungans[0]` kalau kolom top-level lama kosong — supaya Kekuatan Wilayah/Kekuatan Pemilih/Ringkasan Data (fitur yang baca `Dtdoor.kategoriId` dkk., lihat `build-plan.md` § Fase Berikutnya) tetap dapat kategori dari data baru tanpa perlu diubah sendiri. **Belum full-migrasi ke `kunjungans[]` sebagai sumber utama** (sengaja, di luar scope perbaikan create-form sesi ini — lihat progress-tracker.md Decisions 2026-08-23) — kalau 1 record punya BEBERAPA kunjungan dengan tipePemilih berbeda-beda, cuma kunjungan pertama yang kepakai untuk kategori/scoring.
    - Auth guard: ~~masih tidak ada sama sekali~~ **KOREKSI 2026-08-25 (RBAC): `GET /dtdoor` SEKARANG WAJIB Bearer token** (`AuthGuard`, ditambah untuk scoping wilayah — lihat § RBAC di atas). Endpoint dtdoor lain (`POST /dtdoor`, `rekap-group`, dst.) TIDAK ikut berubah, masih tanpa guard.
- **Social Event / GOTV (Feature 05, dipakai `src/services/gotv.ts`):** entity `Gotv` (`/Users/asdarsaid/JSI/api/src/gotv/entities/gotv.entity.ts`) jauh lebih sederhana dari `Dtdoor` — kolom real cuma `id, nik (nullable, default '-'), nama_lengkap, nama_kegiatan, tps, desa, kecamatan, kabupaten, no_telpon (nullable), jumlah_wajib_pilih, idDpt (nullable), created_at, updated_at`. **`nama_lengkap`, `nama_kegiatan`, `tps`, `desa`, `kecamatan`, `kabupaten`, `jumlah_wajib_pilih` semuanya `NOT NULL`** (beda dari dtdoor yang cuma 2 kolom wajib). Endpoint dipakai: `GET /gotv/data` (list, params `page`/`limit`), `GET /gotv/count` (angka mentah), `POST /gotv` (create). Catatan kontrak:
  - **Relasi `status` (ke `StatusGoTv`) ada di file entity tapi di-comment-out** (`// @ManyToOne(() => StatusGoTv...) status: StatusGoTv;`) — bukan kolom aktif, cuma dead code/WIP yang belum jadi. Field `pilihan_pileg`, `status_dpt`, `program_bantuan1/2/3` juga di-comment-out semua. Badge status "Selesai"/"Terjadwal" di `program-pemenangan.png` (untuk tab Social Event) **tidak punya data backing sama sekali** — sengaja tidak ditampilkan di `GotvCard` (lihat `ui-registry.md`).
  - `CreateGotvDto` (TS type) mendeklarasikan field yang **tidak match** kolom entity asli (`dusun`, `status_dpt`, `jenis_kelamin` ada di DTO tapi bukan kolom Gotv; sebaliknya `nama_kegiatan` yang justru kolom NOT NULL **tidak ada** di DTO type). Tidak masalah secara runtime (tidak ada `ValidationPipe`, field ekstra di body JSON tetap sampai ke `repository.save()` dan match kolom entity berdasar shape objek, bukan tipe DTO) — tapi jangan percaya daftar field DTO sebagai daftar kolom asli, selalu cross-check ke entity.
  - **`POST /gotv` beda perilaku dari `POST /dtdoor` saat ada koleksi `idDpt`:** dtdoor diam-diam upsert (update record lain), gotv langsung `throw HttpException('DPT telah di input sebelunya', 404)` kalau `getOne({ idDpt })` menemukan match. Karena `idDpt` kosong/undefined bikin lookup itu match row APAPUN yang sudah ada di tabel (where kosong → ambil row pertama), **create kedua dan seterusnya akan SELALU gagal dengan error palsu ini** kalau tidak dikirim `idDpt` unik. Diakali dengan `generateSyntheticIdDpt()` bersama (`src/lib/api/syntheticId.ts`, dipakai juga oleh `dtdoor.ts`) — WAJIB dipakai tiap create standalone.
  - `GotvService.create()` balas entity hasil `repository.save()` **langsung** (bukan `UpdateResult` seperti `update()`/`PATCH`) — beda dari pola `timses`/`dtdoor` PATCH, tidak perlu fetch ulang setelah create sukses.
  - ~~Tidak ada auth guard di `GotvController` sama sekali~~ **KOREKSI 2026-08-25 (RBAC): `GET /gotv` SEKARANG WAJIB Bearer token** (`AuthGuard`, ditambah untuk scoping wilayah — lihat § RBAC). `POST /gotv`/`GET /gotv/count` TIDAK ikut berubah, masih tanpa guard.
  - **⚠️ 2026-08-23 — KOREKSI path & envelope (field/DTO di atas TETAP akurat, tidak berubah).** Modul `gotv` JUGA sudah pindah ke model Sequelize baru (`src/database/models/timses/Gotv.ts`, `GoTvService`/`GoTvController` baru) — TAPI beda dari `dtdoor`, nama field-nya **tetap snake_case** (`nama_lengkap`, `nama_kegiatan`, `no_telpon`, `jumlah_wajib_pilih` dst., dikonfirmasi live curl cocok 1:1 dengan `GotvApiRecord` lama, TIDAK perlu diubah). Yang berubah:
    - **Path list `GET /gotv` (root, BUKAN `/gotv/data`)** — dikonfirmasi live: `/gotv/data` → 404 "Cannot GET", `/gotv` → 200. `GET /gotv/count` **masih ada** (tidak hilang seperti dtdoor).
    - Semua response (list/count/create) dibungkus `TransformInterceptor` app-wide → `{message, data}` — list: `{message, data: GotvRecord[]}` **TANPA field pagination sama sekali** (`GoTvService.get()` pakai `findAll()` biasa, bukan `findAndCountAll()` — tidak ada `total`/`totalPages`). Mobile infer `totalPage` dari jumlah baris balik (`rows.length < limit` → halaman terakhir). Count: `{message, data: <number>}` (bukan angka mentah). Create: `{message, data: <record>}`.
    - Auth guard, bug upsert-by-idDpt (`generateSyntheticIdDpt()` versi STRING, bukan number — beda dari dtdoor), dan seluruh field/DTO note di atas **tidak berubah**, tetap berlaku.
  - **⚠️ 3 bug tambahan ditemukan 2026-08-23 (laporan user "validation failed" saat submit form GOTV), semua diverifikasi via curl POST validation-only (body sengaja dibuat gagal validasi, TIDAK ada insert sungguhan ke tabel produksi):**
    1. **`tps` dengan angka nol di depan ("01"-"09") ditolak validasi.** `CustomValidationPipe` global (`src/commons/pipes/validation.pipe.ts`) auto-convert string numerik ke number SEBELUM validasi, KECUALI string itu match regex `/^0\d+$/` (`checkIfZeroPaddedString`) — kasus itu dipertahankan sebagai string. DTO `tps` didekorasi `@IsNumber()` (meski TS type-nya `string`, sama pola inkonsisten dengan `kelId`/`idDpt` di dtdoor) — string "01" yang dipertahankan itu lolos ke validator dan GAGAL `@IsNumber()`. **Fix:** `services/gotv.ts` strip leading zero (`String(Number(input.tps))`) sebelum kirim.
    2. **`kabId` — bug SAMA PERSIS dengan `kepalaKeluargaId` di dtdoor.** `GoTvController.insert()` jalankan `goTvService.findOne({idDpt, kabId: goTvInsertDTo.kabId})` (cek duplikat) SEBELUM create — Sequelize throw `WHERE parameter "kab_id" has invalid "undefined" value` kalau `kabId` absen dari body (dikonfirmasi live). **Fix:** body SELALU sertakan `kabId: null` eksplisit (bukan diomit) — sama pola dengan fix `kepalaKeluargaId`.
    3. **`nik` WAJIB terisi di level DATABASE** (kolom `NOT NULL` tanpa default) — **beda dari DTO yang menandainya `@IsOptional()`**, jadi tidak ke-tangkap sebagai validasi 422 yang rapi, malah lolos ke `GoTV.create()` dan gagal di level SQL mentah (dikonfirmasi live: `"Field 'nik' doesn't have a default value"`, error MySQL langsung, bukan pesan NestJS). **Fix:** `CreateGotvInput.nik` diubah dari opsional jadi wajib (`types/gotv.ts`), `GotvFormScreen.tsx` validasi NIK wajib diisi. **⚠️ Risiko presisi belum terselesaikan:** DTO `nik` didekorasi `@IsNumber()` — pipe global convert NIK 16-digit (misal `"3204010101800001"`) via `parseInt`/`Number()`, yang **kehilangan presisi untuk integer >15-16 digit** (melebihi `Number.MAX_SAFE_INTEGER`). NIK asli berpotensi tersimpan salah/terpotong di backend — bug backend (kolom seharusnya string), di luar kemampuan mobile untuk diperbaiki, cuma bisa didokumentasikan sebagai risiko dikenal.
  - **⚠️ 2026-08-24 — bug ke-4 & ke-5 ditemukan (laporan user "validation failed" saat submit form Social Event), diverifikasi via curl POST validation-only (body sengaja dibuat gagal validasi di 1 field lain sekaligus, TIDAK ada insert sungguhan ke tabel produksi):**
    4. **`no_telpon` TERNYATA WAJIB** (`@IsNumberString()` di DTO **TANPA** `@IsOptional()`, dikonfirmasi live: body tanpa `no_telpon` balas `"no_telpon must be a number string"`) — beda dari dokumentasi lama (poin 3 di atas cuma soal `nik`) yang tidak menyorot field ini. `GotvFormScreen.tsx` sebelumnya menganggap field ini opsional (kirim `undefined` kalau kosong) — inilah penyebab utama error "Validation failed" yang dilaporkan user. **Fix:** `CreateGotvInput.noTelpon` (`types/gotv.ts`) diubah dari opsional jadi wajib, `GotvFormScreen.tsx` validasi No. Telpon wajib diisi (pola sama `nik`).
    5. **`no_telpon` juga TIDAK menerima strip `-`/spasi** (`@IsNumberString()` murni digit, dikonfirmasi live: `"0812-5555-6666"` gagal, `"081255556666"` lolos) — placeholder umum nomor telpon Indonesia pakai dash, jadi field ini gampang gagal kalau user copy-paste format lazim. **Fix:** `services/gotv.ts` strip semua karakter non-digit (`input.noTelpon.replace(/\D/g, "")`) sebelum kirim ke backend, mirip pola strip leading-zero `tps` (poin 1 di atas).
    6. **⚠️ BLOCKER — `nik` TIDAK PERNAH benar-benar tersimpan, `POST /gotv` SELALU crash SQL** (dikonfirmasi 2026-08-24, TIDAK BISA diperbaiki dari repo mobile): model Sequelize `GoTV` (`/Users/asdarsaid/JSI/api/src/database/models/timses/Gotv.ts`) tidak punya `@Column nik` sama sekali (cuma `id,idDpt,kabId,nama_lengkap,nama_kegiatan,kabupaten,kecamatan,desa,tps,no_telpon,jumlah_wajib_pilih`) — apapun yang mobile kirim untuk `nik` dibuang diam-diam oleh ORM sebelum `INSERT` (dibuktikan via curl: body TANPA `nik` sama sekali pun crash identik). Tapi kolom `nik` di tabel `gotv` `NOT NULL` tanpa default → `GoTV.create()` selalu gagal `(SQLState HY000) Field 'nik' doesn't have a default value`, **untuk SEMUA create GOTV, standalone maupun linkage DPT**. **User pilih PARKIR fitur Social Event dulu 2026-08-24** (lihat progress-tracker.md Decisions) — 3 opsi fix backend (tambah `@Column nik` / default hardcode `nik:'-'` di service / migration nullable) sudah didokumentasikan di situ untuk dilanjut nanti. Field `nik` di poin 3 di atas (`CreateGotvInput.nik` wajib) **masih benar secara validasi DTO**, tapi percuma sampai blocker ini diperbaiki di backend.
  - **2026-08-24 — "Tandai ikut Social Event" (linkage DPT→Gotv, dikerjakan di sesi ini, pola identik "Tandai ikut Door To Door"):** dikonfirmasi baca `/Users/asdarsaid/JSI/api/src/dpt/dpt.service.ts` (`GoTV.findAll({where:{kabId: wilKab.wilId, idDpt:{[Op.in]: dptsIds}}})`, match balik `gtv.idDpt == dpt.idDpt`) — join DPT→Gotv **sama persis** dengan DPT→Dtdoor (`kabId` = `wilId` kabupaten, `idDpt` match apa adanya). `GotvFormScreen` (dibuka dari `DptVoterActionSheet`) sekarang kirim `idDpt: String(dptRecord.idDpt)` + `kabId: kabWilId` asli (bukan `generateSyntheticIdDpt()`+`kabId: null`) kalau `dptRecord.idDpt` non-null — badge "Social Event" (`sudahGotv`) di `DptCard` akan ikut field `gotv` response asli begitu list di-refetch, **cuma berlaku untuk record dengan `idDpt` non-null** (sama batasan dengan D2D, lihat catatan CRUD dasar DPT di bawah).

### Tracking / Lacak Relawan (dipakai `src/services/timses.ts` § `fetchTimsesList`/`updateOwnLocation`) — modul `user` diperluas, WIRED PENUH 2026-08-25
Modul `user` (self-service saja sebelumnya, lihat area koreksi Auth Flow poin 6 di atas) dapat 2 kolom + 2 endpoint baru khusus untuk fitur Tracking (peta relawan) — dikonfirmasi baca kode + live curl 401-check (route ada & ter-guard, belum tes dengan token sungguhan):
- **Kolom baru `TimsesModel`:** `lat`/`long` (`DECIMAL(10,7)`, NULLABLE) — ditambahkan ke **kedua file model** (`database/models/user/timses.mode.ts` DAN `database/models/timses/timses.ts`, keduanya baca tabel `timses` yang sama, landmine duplikasi — lihat § RBAC di bawah untuk detail penuh masalah ini). Migration `yarn migrate:lokasi-timses` (idempotent).
- **`POST /user/location`** — body `{ lat: number, long: number }`, self-service dari JWT (bukan `:id`), update baris milik user yang login, balas `apiResponse('Lokasi diperbarui', updated)` (`updated` = full profil terbaru, fetch ulang setelah update). Dipanggil `useLocationBeacon.ts` tiap 30 detik selama app foreground.
- **`GET /user/list`** — balas `apiResponse('Daftar Anggota', list)`, `list: {id,namaLengkap,roles,statusOnline,lat,long,kabId,kecId,kelId}[]` — **field lama (`nik`/`dusun`/`desa`/`kecamatan`/`noTelpon`) TIDAK ADA di response ini** (beda dari `GET /user/profile` yang balas full row). `password` otomatis ke-exclude (`defaultScope`). Sejak RBAC (2026-08-25) response ini **sudah di-scope server-side** sesuai role+wilayah requester — lihat § RBAC.
- `lat`/`long` DECIMAL kadang balik sebagai **string** dari MySQL driver (kuirk node-mysql2 umum untuk kolom DECIMAL) — `services/timses.ts::toNumberOrNull()` parse eksplisit, jangan asumsikan selalu `number` dari response mentah.
- Auth guard: `AuthGuard` standar (JWT), sama seperti `/user/profile`.

### RBAC — Role + Scoping Wilayah (dipakai `src/lib/permissions.ts`, `src/services/timses.ts` § `createTimsesMember`/`updateTimsesRole`) — infrastruktur baru, WIRED SEBAGIAN 2026-08-25
Permintaan baru user (di luar checklist Phase 1-3) — **role berbasis wilayah SEBELUMNYA TIDAK PERNAH sungguhan ditegakkan** di web maupun backend manapun (endpoint filter lama `timseskabupaten`/`timseskecamatan`/`timsesdesa` ternyata filter by ROLE bukan nilai wilayah, temuan Feature 06) — ini fitur baru, bukan revive. Detail keputusan desain lengkap di `progress-tracker.md` Decisions 2026-08-25.

**Role (6, cocok persis `Posisi` di `client/src/data/dataArea.js`):** `admin`, `adminsekret`, `relawankabupaten`, `relawankecamatan`, `relawandesa`, `timses`. **`relawantps` TIDAK ADA** (dihapus dari `types/auth.ts` — sebelumnya salah, tidak pernah jadi posisi assignable di web).

**Kolom baru `TimsesModel` (kedua file model, sama landmine duplikasi dengan `lat`/`long` di atas):** `kabId`/`kecId`/`kelId` (INTEGER NULLABLE, wilId — sama skema modul `dpt_2024`/tabel `wilayah`, BUKAN string bebas seperti skema `timses` TypeORM lama). Admin isi sesuai granularitas role (`relawankabupaten` → cuma `kabId`, `relawankecamatan` → `kabId`+`kecId`, `relawandesa`/`timses` → ketiganya). Migration `yarn migrate:rbac-wilayah` (idempotent). `GoTV` juga dapat kolom baru `kelId` (sebelumnya cuma `kabId`) supaya Social Event bisa di-scope sampai desa seperti `Dtdoor`.

**Endpoint admin-only baru (`@Roles('admin','adminsekret')`, via `RolesGuard`+`@Roles()` — infrastruktur baru `src/auth/roles.decorator.ts`/`roles.guard.ts`, jalan setelah `AuthGuard`):**
- `POST /user` — `CreateUserDto {nik,password,namaLengkap,jenisKelamin,roles,kabId?,kecId?,kelId?}`, balas `201` + akun baru (password di-hash bcrypt). **WIRED** — form "Tambah Anggota" (`TimsesFormScreen`, 2026-08-25, tanpa referensi desain, izin eksplisit user).
- `PATCH /user/:id` — `UpdateUserRoleDto {roles?,kabId?,kecId?,kelId?}`, ubah role/wilayah akun existing — **HANYA field ini**, TIDAK ADA `namaLengkap`/nik/password untuk admin edit akun ORANG LAIN (beda dari `POST /user/profile` yang self-edit). **WIRED** — `TimsesFormScreen` mode edit (2026-08-26, dipicu tap `TimsesMemberCard`).
  - **⚠️ Reverse-lookup wilId→nama masih TIDAK LENGKAP** (dicek ulang 2026-08-26 buat kebutuhan prefill form edit): `GET /kabupaten/details/:kabId` cuma dukung skema 2019 (`ParamModel({model: RdppKab, key:'kabId', as:'idKab'})` — TIDAK ADA param `tahun`, tidak bisa dipakai untuk `kabId` skema 2024 yang dipakai RBAC ini). `GET /kecamatan/details/:kecId?tahun=2024` dan `GET /kelurahan/details/:kelId?tahun=2024` **SUDAH** dukung 2024 (`WilKec2024`/`WilKel2024`), tapi `GET /kabupaten?tahun=2024` (list) juga TIDAK expose kolom `proKode` di attributes query-nya (`kabupaten.service.ts::getAll2024`, cuma `kabNama`/`wilId`/`totalDpt`/`proNama`/`totalKec`/`totalKel`) — jadi dari `kabId` tersimpan TIDAK BISA ditentukan `provinsiWilId` induknya untuk prefill Select "Provinsi". Kalau mau diperbaiki: (a) tambah endpoint kabupaten 2024 versi `details/:kabId` (model `WilKab2024`, bukan `RdppKab`), DAN/ATAU (b) tambah `proKode` ke attributes `getAll2024`. Keputusan sementara (user, 2026-08-26): `TimsesFormScreen` mode edit biarkan Select wilayah kosong, tampilkan wilId lama mentah sebagai caption referensi saja — lihat `ui-registry.md` § TimsesFormScreen & progress-tracker.md Decisions.
- `DELETE /user/:id` — **BARU 2026-08-26**, spesifikasi diajukan mobile (bukan observasi kode existing — endpoint ini sebelumnya tidak ada sama sekali, dicek dulu via grep sebelum ngajuin, sesuai Aturan #6) → user jalankan prompt-nya di repo backend, hasilnya diverifikasi baca kode & **cocok 1:1 dengan spesifikasi**. Hard delete (`TimsesModel.destroy`, tabel `timses` tidak punya kolom soft-delete/`paranoid`, jadi TIDAK ADA cara batalkan). Guard sama (`admin`/`adminsekret`), plus proteksi tambahan: 422 `ValidationException` kalau admin coba hapus akun ID miliknya sendiri (`id === requesterId` dari JWT). Response sukses: `apiResponse('Akun relawan berhasil dihapus', { id })`. **WIRED** — tombol "Hapus Anggota" di `TimsesFormScreen` mode edit (`services/timses.ts::deleteTimsesMember`, `useDeleteTimsesMember`), disembunyikan (bukan cuma disabled) kalau `member.id === session.user.id` sebagai UX preventif duplikat proteksi backend. **Belum dites dengan token JWT sungguhan.**

**Scoping otomatis server-side** (`commons/helpers/scope.helper.ts`, baca `getRequesterScope(userId)` dari `TimsesModel` per request — BUKAN dari payload JWT, yang tidak simpan kabId/kecId/kelId — supaya perubahan wilayah langsung berlaku tanpa perlu re-login):
- `buildKabKelScopeWhere()` — dipakai `GET /dtdoor` & `GET /gotv` (model yang cuma punya kolom `kabId`/`kelId`, tanpa `kecId` langsung). kelId requester paling spesifik menang → kecId diterjemahkan subquery `wilayah` (kec_kode→kel_id) → kabId.
- `buildUserListScopeWhere()` — dipakai `GET /user/list` (`TimsesModel` punya `kecId` sendiri, jadi match `kecId` langsung ATAU turunan `kelId`).
- `assertKabDalamScope()` — dipakai `GET /dpt/2024/:wilId` + `/2024/total/:wilId` + `/2024/details/:kabId/:idDpt` (403 `ForbiddenException` kalau `wilId` yang diminta bukan turunan wilayah requester).
- `admin`/`adminsekret` SELALU bypass (lihat semua, tanpa kecuali). **Requester tanpa wilayah sama sekali (kabId/kecId/kelId semua `null`) SENGAJA TIDAK dibatasi** — akun lama tidak mendadak kehilangan akses; scoping baru berlaku setelah admin aktif assign lewat `PATCH /user/:id`.
- **Modul yang di-scope:** HANYA `GET /user/list`, `GET /dtdoor`, `GET /gotv`, 3 endpoint DPT 2024 di atas (keputusan eksplisit user — modul "lapangan" saja). **BELUM** diterapkan ke endpoint dtdoor sekunder (`rekap-group`/`rekap-kekuatan-wilayah`/`kekuatan-pemilih`/`kepala-keluarga`/`filter`/`kabupatens`/`kecamatans`/`kelurahans`) atau DPT 2019-legacy — masih publik/unscoped. Modul HQ kampanye (Budgeting/Quick Count/Rival Caleg/Tokoh/Isu Aspirasi/Anti-Fraud/Hasil Rekap) **sengaja TIDAK di-scope** — tetap semua role lihat semua.
- **⚠️ Breaking change disengaja:** `GET /dtdoor`, `GET /gotv`, dan 3 endpoint DPT 2024 di atas **SEKARANG WAJIB Bearer token** (sebelumnya publik tanpa guard — gap lama dari Feature 04/05, "Tidak ada auth guard sama sekali"). Scoping butuh tahu SIAPA yang minta, jadi auth jadi prasyarat. **Tidak berdampak ke mobile** — `apiClient` selalu attach token via interceptor kalau ada sesi aktif, dan seluruh app ini memang mewajibkan login (`AuthStack` fallback).
- **Diverifikasi:** baca kode penuh + live curl 401-check ke backend lokal (`GET /dtdoor`/`GET /gotv`/`GET /dpt/2024/7303`/`GET /user/list`/`POST /user`/`PATCH /user/1` semua balas `401` tanpa token, bukan `404` — route ada & ter-guard benar). **Belum dites dengan token JWT sungguhan** (butuh kredensial live) — shape response scoped/403 presisi masih berdasar baca kode.

**Sisi mobile:** `src/lib/permissions.ts` (baru, 1 sumber `ADMIN_ROLES`/`isAdmin()`/`ROLE_LABEL`/`requiredWilayahLevel()`, menggantikan 5+3 copy lokal yang berbeda-beda). `TimsesMember`/`TimsesProfile` dapat `kabId`/`kecId`/`kelId`, kehilangan `nik`/`dusun`/`desa`/`kecamatan`/`noTelpon` (tidak ada lagi di response). `AccessScopeNotice` dihidupkan kembali (prop `scopeLevel`, jujur soal LEVEL tapi TIDAK sebut NAMA wilayah — belum ada endpoint reverse-lookup wilId→nama lengkap, lihat catatan `kabupaten/details` di atas). `TimsesScreen` breadcrumb Kecamatan/Desa DILEPAS (grouping lama berbasis string yang sudah tidak ada), diganti list datar + search nama; **tap card sekarang push ke mode edit** (`TimsesFormScreen`, 2026-08-26). Detail penuh: `progress-tracker.md` Decisions 2026-08-25 & 2026-08-26, `ui-registry.md` § TimsesScreen/TimsesFormScreen/AccessScopeNotice/TrackingMemberRow/TimsesMemberCard.

### DPT — ~~repo belum ada, endpoint request belum terkonfirmasi~~ **KOREKSI TOTAL 2026-08-23: MODUL ADA, endpoint request SUDAH terkonfirmasi (baca kode + live curl), WIRED ke backend real**
**⚠️ Koreksi besar terhadap CLAUDE.md Aturan #6 & seluruh entry di bawah ini (2026-08-21/22)** — modul DPT ternyata **sudah ada** di `/Users/asdarsaid/JSI/api/src/dpt` (`dpt.controller.ts`/`dpt.service.ts`/`dpt/wilayah/kabupaten.dto.ts`) + controller terpisah `/provinsi`, `/kabupaten`, `/kecamatan`, `/kelurahan`, sama pola dengan temuan Hasil Rekap (asumsi "belum ada repo" ternyata basi, bukan cuma "belum dicek"). Semua endpoint GET **cocok byte-for-byte** dengan `contohdpt.ts` (dikonfirmasi live curl, termasuk data produksi asli — mis. id 191483 "HASMAWATI M" Kec. Bissappu Kab. Bantaeng).

**Base URL:** `EXPO_PUBLIC_API_URL` (`https://api.demokrasikreatif.org`, TANPA `/api`) — sama seperti auth/dtdoor/gotv, bukan host terpisah.

**Endpoint GET (query param `tahun=2024` WAJIB di 4 endpoint wilayah, TIDAK ada di endpoint dpt sendiri karena tahun sudah di path):**
| Endpoint | Query | Response `data` |
| --- | --- | --- |
| `GET /provinsi` | `tahun=2024` | `[{wilId,proNama,totalDpt,totalKab}]` |
| `GET /kabupaten` | `tahun=2024,provId` | `[{wilId,kabNama,totalDpt,proNama,totalKec,totalKel}]` |
| `GET /kecamatan` | `tahun=2024,kabId` | `[{wilId,totalDpt,kecNama,kabKode,totalKel}]` |
| `GET /kelurahan` | `tahun=2024,kabId,kecId` (**keduanya wajib bareng**, bukan cuma kecId) | `[{wilId,totalDpt,kecKode,kabKode,kelNama}]` |
| `GET /dpt/2024/:wilId` | `page,limit,nama,kecId,kelId,tps` | `[DptRecord...]` + `kab:{wilId,totalDpt,kabNama,proNama,proKode,totalKec}` + `meta:{page}` **(⚠️ CUMA `page`, TIDAK ADA total/limit/offset/totalPages — beda dari dtdoor, dikonfirmasi live curl + baca `DptService.getDpt()` setelah crash `toLocaleString of undefined` di device. `hasMore` di-infer dari `data.length === limit` yang diminta.)** |
| `GET /dpt/2024/:wilId?type=tps` | + `kecId,kelId` | `[{noTps,namaTps}]` (endpoint lookup TPS BARU ditemukan — dulu dikira tidak ada, opsi TPS di mobile sekarang pakai ini, bukan lagi di-derive dari record yang sudah termuat) |
| `GET /dpt/2024/total/:wilId` | `kecId,kelId,tps,nama` | `<number>` — **WAJIB dipakai untuk total count akurat** (list endpoint sendiri tidak balas total sama sekali, lihat baris di atas), dikonfirmasi live (`kabId=7303` → 151952 total kabupaten, `+kecId=730301` → 26255 cocok persis `totalDpt` kecamatan BISSAPPU). |
| `GET /dpt/2024/details/:kabId/:idDpt` | — | `DptRecord` (match kolom `idDpt` ASLI, bukan `id` — lihat landmine di bawah) |

**`DptRecord` (field asli, CAMELCASE — bukan snake_case):** `{id,idDpt,idKec,idKel,noTps,namaKec,namaKel,namaTps,nama,nik,jenisKelamin,usia,alamat,rt,rw,hidup,isKk,dtdoor,gotv,createdAt,updatedAt,kelId}` — `dtdoor`/`gotv` object kalau ada linkage nyata (match idDpt+kabId ke record Dtdoor/Gotv), `null` kalau belum (dipetakan flag boolean `sudahDtdoor`/`sudahGotv` di mobile, sama pola lama).

**Endpoint WRITE (`POST /dpt/2024-created/:wilId`, `PUT /dpt/details/:kabId/:idDpt`, `DELETE /dpt/details/:kabId/:idDpt`) — DTO `wilayah/kabupaten.dto.ts` (⚠️ ADA DUPLIKAT class sama nama di `dpt/dto/dpt.dto.ts`, TIDAK dipakai controller manapun — dead code, pola sama duplikat DTO gotv sebelumnya):**
- **Create** — field wajib (dikonfirmasi live 422): `idKec,namaKec,idKel,namaKel,nama`. Opsional: `noTps,namaTps,nik(maxlen16),tempatLahir,jenisKelamin,rt,rw`. Response `{message,data:<DptRecord>}`. **`usia` TIDAK ADA di DTO sama sekali** — backend selalu insert `null` untuk field ini (`usia: dptDto['usia'] || null`, dan DTO tidak punya properti itu) — **tidak bisa diisi lewat form, dihapus dari `DptRecordFormScreen`**.
- **⚠️ Field `alamat` mobile dikirim ke backend lewat key `tempatLahir`** — bukan salah ketik, backend literal `alamat: dptDto.tempatLahir` (penamaan asli database, quirk terverifikasi baca `dpt.service.ts`).
- **⚠️⚠️ LANDMINE #1 — kolom `idDpt` TIDAK PERNAH diisi oleh create.** `insertDpt2024()` tidak menyertakan `idDpt` sama sekali di payload `model.create()` — kolom `allowNull:true` tanpa default (dikonfirmasi baca `dpt.model.ts`), jadi **record baru yang dibuat lewat app ini SELALU `idDpt: null`**. Konsekuensi: (a) tidak bisa di-link ke Dtdoor/Gotv asli (fitur "Tandai ikut Door To Door" fallback ke idDpt sintetis standalone, lihat `DtdoorFormScreen.tsx`), (b) **tidak bisa dihapus** lewat `DELETE /dpt/details/:kabId/:idDpt` (match kolom `idDpt`, tidak ada nilai valid untuk record baru — `deleteDptRecord()` di mobile sengaja `throw` pesan jelas untuk kasus ini, Aturan #6), (c) tidak bisa di-detail-fetch lewat `GET /dpt/2024/details/...` (match kolom sama). **Edit TIDAK terpengaruh** (lihat landmine #2, pakai kolom `id` bukan `idDpt`).
- **⚠️⚠️ LANDMINE #2 — PUT edit match kolom `id`, BUKAN kolom `idDpt`, padahal segment URL-nya bernama `:idDpt`.** Dikonfirmasi baca `dpt.service.ts::editDpt2024()`: `model.findOne({where:{id: idDpt}})` — variabel bernama `idDpt` (dari path) tapi dicocokkan ke kolom `id` (primary key besar, 191483 dst.). **BEDA dari `GET details` & `DELETE`** yang sama-sama pakai path `:idDpt` tapi match kolom `idDpt` ASLI (sequence kecil 1,2,3...). Inkonsistensi asli backend (bukan bug mobile) — `updateDptRecord()` WAJIB kirim `record.id` di URL, `deleteDptRecord()`/`fetchDptDetail()` WAJIB kirim `record.idDpt`.
- **⚠️⚠️ LANDMINE #3 — Edit/Delete default ke jalur DATA 2019 kalau tidak diberi tahu eksplisit.** `editDpt()` (dipanggil PUT, satu route untuk semua tahun) destructure `tahun = 2019` dari body — kalau tidak dikirim, salah jalur ke model 2019 (`RdppKab`) walau dipanggil untuk data 2024. **Web sendiri PUNYA bug ini** (`Dpt2024EditModal` tidak pernah kirim `tahun` di body) — TIDAK diikuti di mobile, `updateDptRecord()` SELALU kirim `tahun: 2024` eksplisit di body. `dptDelete()` (controller) juga default `tahun=2019` kalau query param tidak dikirim — `deleteDptRecord()` SELALU kirim `?tahun=2024` eksplisit.
- **PUT balas hasil `model.update()` mentah** (affected-count, bukan entity) — `updateDptRecord()` fetch ulang via `GET /dpt/2024/details/...` (pakai `idDpt` ASLI dari record sebelum diedit) untuk dapat data akurat; kalau `idDpt` null (record baru sesi ini), fallback susun manual dari input yang baru dikirim.
- Auth guard: ~~tidak dicek eksplisit~~ **KOREKSI 2026-08-25 (RBAC): `GET /dpt/2024/:wilId`, `/2024/total/:wilId`, `/2024/details/:kabId/:idDpt` SEKARANG WAJIB Bearer token** (`AuthGuard`, ditambah untuk scoping wilayah — lihat § RBAC di atas). Endpoint DPT lain (CRUD, TPS lookup, 2019-legacy) TIDAK ikut berubah, masih tanpa guard.

**Filtering & pagination DPT list SEKARANG SERVER-SIDE** (2026-08-23, `useDptList` jadi `useInfiniteQuery` dengan filter di query key — ganti dari fetch-penuh-lalu-filter-client-side). Untuk fitur lain yang butuh SELURUH record kabupaten/kelurahan untuk agregasi (Target Suara/Real Count TPS grouping, demo widget HomeScreen) — `fetchDptListAll()`/`useDptListAll()` (baru, `src/services/dpt.ts`) loop semua halaman, dengan **hardcap 5000 record** supaya tidak hang di kabupaten besar tanpa filter kec/kel. Ini BUKAN solusi skalabilitas sesungguhnya (idealnya backend punya endpoint agregat) — cukup untuk sekarang, revisit kalau ada laporan lambat/data terpotong di kabupaten besar.

**CRUD dasar** (List/Filter/Detail/Create/Edit/Delete/TPS lookup) **WIRED penuh ke backend real 2026-08-23** — lihat progress-tracker.md Decisions untuk detail implementasi & 3 landmine di atas. Field `usia` dihapus dari form (tidak bisa diisi). "Tandai partisipasi program" (badge sudahDtdoor/sudahGotv) sekarang derive dari field `dtdoor`/`gotv` response asli (linkage nyata via idDpt+kabId), bukan cuma flip lokal — TAPI cuma berlaku untuk record dengan `idDpt` non-null (data impor asli).

### Hasil Rekap — **DPR RI WIRED ke backend real (2026-08-23)** — `src/services/hasilrekap.ts` sekarang panggil `GET /dpr/hasil-rekap...` sungguhan (bukan mock lagi, `EXPO_PUBLIC_USE_MOCK_API=false`). DPRD Provinsi/DPRD Kabupaten/DPD **masih di luar scope mobile**, kontraknya didokumentasikan di bawah untuk referensi sesi berikutnya saja.

**✅ Wiring DPR RI selesai (2026-08-23):** host dikonfirmasi user = `https://api.demokrasikreatif.org` (bukan `rekap2024.demokrasikreatif.org`), keluarga endpoint yang dipakai = `hasil-rekap` (dapil-first, bukan `hasil-hitung`) — cocok 1:1 dengan hierarki `HasilRekapDapilScreen` dst. yang sudah dibangun. **Seluruh 5 level drill diverifikasi LIVE via curl langsung ke production** (dapil `1101` "ACEH I" → kabupaten `2` "ACEH SELATAN" → kecamatan `3` "BAKONGAN" → kelurahan `4` "KEUDE BAKONGAN") sebelum kode ditulis — bukan cuma baca kode backend. Detail mapping lengkap ada di komentar `src/services/hasilrekap.ts` (helper `buildFromPartaiCalegs`). Client baru `rekapApiClient` (`src/lib/api/client.ts`) dengan `baseURL` dari env var baru `EXPO_PUBLIC_REKAP_API_URL=https://api.demokrasikreatif.org` (TANPA `/api`, beda dari `EXPO_PUBLIC_API_URL` yang dipakai `apiClient`) — lihat penjelasan topologi base URL di bawah untuk alasannya.

**✅ Diverifikasi ulang 2026-08-23 (user sempat khawatir "tadi masih di branch yang lama"):** repo `/Users/asdarsaid/JSI/api` dicek — branch checkout **`persiapan-2029`** (commit 2025-08-19, SAMA dengan `new-update`) selama seluruh sesi riset ini (dikonfirmasi `git reflog`, tidak ada pindah branch). Setelah `git fetch --all --prune`, **`persiapan-2029` terbukti branch TERBARU** yang tersedia (lebih baru dari `master`, 2025-05-30; branch lama seperti `dpr`/`dprd-kab`/`dprd-pro` di remote itu peninggalan Juli 2023, sudah lama di-superscede). Di-diff `master` vs `persiapan-2029` untuk modul `src/dpr*`: **beda signifikan** — seluruh family endpoint `hasil-hitung` untuk DPR RI (`hasilHitung`/`hasilHitungProv`/.../`hasilHitungTps`) **tidak ada** di `master`, begitu juga fallback axios di `hasilRekap().getJsons()` — keduanya SUDAH masuk ke catatan di bawah karena memang dibaca dari `persiapan-2029`. `dpr.controller.ts`/`dpr.service.ts`/`dprd-kab.controller.ts`/`dprd-pro.controller.ts` juga dibaca ulang langsung (bukan cuma percaya laporan agent) dan cocok persis dengan seluruh isi section ini — tidak ada koreksi yang diperlukan.

**⚠️ Topologi base URL beda dari modul lain — WAJIB dicek dulu sebelum isi `.env`:**
- Web frontend flow **2024** (`client/src/pages/hasilrekap2024/`) hardcode base URL TERPISAH: `baseUrl2024 = "https://rekap2024.demokrasikreatif.org"` (`client/src/layouts/dashboard-layout.jsx`) — **BUKAN** `VITE_API_URL` (host utama yang dipakai auth/dtdoor/gotv/timses).
- Web frontend flow **2019** (`client/src/pages/hasilrekap/`) pakai path relatif `/backend-api/dpr/hasil-rekap`, di-proxy Vite ke `VITE_API_URL` (host utama yang SAMA dengan modul lain) — beda host dari flow 2024.
- **Belum dikonfirmasi** apakah `rekap2024.demokrasikreatif.org` adalah deployment LAIN dari repo NestJS yang sama (`/Users/asdarsaid/JSI/api`) atau backend yang sepenuhnya berbeda — kode `src/dpr` yang dibaca cocok secara struktural dengan yang dikonsumsi frontend 2024, tapi ini belum diverifikasi dengan memanggil host itu langsung. **Tanya user host mana yang harus dipakai mobile sebelum wiring API asli.**
- `main.ts` backend ini **tidak** pasang `app.setGlobalPrefix()` — beda dari auth yang butuh suffix `/api` (`EXPO_PUBLIC_API_URL=.../api`, lihat Auth Flow). Endpoint `/dpr/...` dkk. **tanpa** prefix `/api` di depannya, siapa pun host-nya nanti.

**Auth:** tidak ada `@UseGuards` di controller/module manapun (`dpr`/`dprd-kab`/`dprd-pro`/`dpd`), tidak ada `APP_GUARD` global — **sepenuhnya open/unauthenticated**, sama pola dengan dtdoor/gotv.

**Tidak ada DTO sama sekali** (`find`/`grep` `*dto*` di `src/dpr` nihil) — semua response dirakit manual di service layer dari hasil query Sequelize `raw: true`. Nama field response = nama kolom model apa adanya (termasuk beberapa snake_case seperti `nomor_urut` yang lolos karena `raw: true`) — **tidak ada mismatch DTO-vs-entity** seperti kasus gotv (tidak ada DTO untuk di-mismatch-kan), tapi **penamaan key TIDAK konsisten antar level/modul** (lihat di bawah).

**2 keluarga endpoint terpisah per jenis pemilihan** — model beda, makna beda (dikonfirmasi dari kode, bukan tebakan):
- **`hasil-rekap/*`** (model `Hr*` — "Hasil Rekap") — di-drill **berdasar Dapil** (semua model `Hr*` punya kolom `dapilId`, termasuk level nasional). Ini yang dikonsumsi frontend 2024 (`DprRiDapil.jsx`/`DprRiProvinsi.jsx`) dan yang paling dekat dengan hierarki yang SUDAH diimplementasi di mobile (`HasilRekapDapilScreen` dst.).
- **`hasil-hitung/*`** (model `Hs*` — "Hasil Suara") — di-drill **berdasar wilayah administratif murni** (provinsi→kab→kec→kel→tps, TIDAK ada kolom `dapilId` sama sekali di model manapun), ada kolom `progress`, dan di level TPS (`HsDprRiTps`) punya field tally suara sungguhan (`pemilihJ`, `suaraSah`, `penggunaJ`, `suaraTotal`, `suaraTidakSah`, `psu`, `chart`, `images`) — gaya quick-count nasional, BUKAN rekap per-dapil. **Belum ditentukan mana yang dimaksud fitur "Hasil Rekap" mobile ke depannya** — DPR RI yang sudah dibangun sesi ini mengikuti struktur `hasil-rekap` (dapil-first), tapi kalau user maksudnya angka real count per TPS, itu `hasil-hitung`. **Tanya user sebelum wiring.**

**DPR RI — `GET /dpr/hasil-rekap...` (`@Controller('dpr')`):**

| Route | Resolusi `:dapilId` dkk. | Response `data` |
| --- | --- | --- |
| `GET /dpr/hasil-rekap` | — | `{ dapil: [{id,nama,jmlKursi,hargaKursi,partai:[{id,name,suara}]}], partai: [{id,nama,nomor_urut,suaraTotal}] }` |
| `GET /dpr/hasil-rekap/:dapilId` | `HrDprRiDapil.id` (**numeric surrogate PK — BUKAN** `DprRiDapil.id` yang `STRING(35)` kode dapil; controller `Number()`-kan lalu 404 kalau NaN) | `{ partai_calegs: [{id,nama,nomor_urut,calegs:[{id,nama,kabupatens:[{id,nama,suara}]}],suaraCalegs,suaraTotal,suaraPartai}], partai: [{id,nama,nomor_urut,suaraTotal,kabupatens:[{id,nama,suaraCalegs,suaraTotal,suaraPartai}]}] }` |
| `GET /dpr/hasil-rekap/:dapilId/:kabupatenId` | + `HrDprRiKab.id`, cross-check `dapilId` | Shape sama tapi key jadi `partais` (bukan `partai`!) & array anak dinamai **`keluarahans`** (typo asli di kode, bukan `kelurahans`) |
| `GET /dpr/hasil-rekap/:dapilId/:kabupatenId/:kecamatanId` | + `HrDprRiKec.id` | Shape sama 1 level lebih dalam — **balas HTTP 201** (bug di kode asli, `apiResponse(..., 201)` eksplisit, bukan 200 — perlu ditangani di client kalau nanti wiring beneran) |
| `GET /dpr/hasil-rekap/:dapilId/:kabupatenId/:kecamatanId/:kelId` | + `HrDprRiKel.id` | Shape sama, TERMINAL — array anak dinamai `tps` |

**DPRD Kabupaten (`/dprd-kab`) & DPRD Provinsi (`/dprd-pro`)** — pola 2-keluarga sama (`hasil-rekap` via `Hr*`, `hasil-hitung` via `Hs*`), TAPI urutan Dapil beda per modul (mengkonfirmasi observasi awal dari web-only, sekarang tervalidasi ke backend):
- **DPRD Kab:** `hasil-rekap/:kabId` → `.../:dapId` (Dapil SETELAH Kabupaten) → `.../:kecId` → `.../:kelId`. Key response `partaiGroup` (bukan `partai_calegs`).
- **DPRD Pro:** `hasil-rekap/:provId` → `.../:dapilId` (Dapil SETELAH Provinsi, SEBELUM Kabupaten) → `.../:kabId` → `.../:kecId` → `.../:kelId`. Key response juga `partaiGroup`, beberapa level di-sort by `nomor_urut`.
- Keduanya juga punya `hasil-hitung/...provId/kabId/kecId/kelId/tpsId` (chain administratif biasa, sama pola DPR RI), leaf TPS balas `{ results: <full row Hs*Tps minus chart>, partais }`.

**DPD (`/dpd`)** — ada, cuma untuk rekap DPD per provinsi (`hasil-rekap/:proId/:kabId/:kecId/:kelId`, model `HrDpd*` saja, tidak ada `hasil-hitung`) — di luar scope mobile saat ini, tidak didetailkan.

**Envelope response:** global `TransformInterceptor` bungkus semua jadi `{ message, data }` — tidak ada `pagination`/key lain kecuali service secara eksplisit menambah field di level atas `data` (lihat tabel di atas, beberapa balas `{ dapil, partai }`, ada juga `{ partai_calegs, partais }` — **tidak konsisten**, harus di-handle per-endpoint saat mapping, jangan asumsikan 1 shape generik untuk semua level).

**✅ GAP di atas SUDAH diselesaikan untuk DPR RI (2026-08-23):**
- `src/services/hasilrekap.ts` real branch sekarang panggil `rekapApiClient` sungguhan, mock (`DAPIL_TREE` dst.) DIPERTAHANKAN tapi cuma aktif kalau `EXPO_PUBLIC_USE_MOCK_API=true` (sekarang default `false`).
- Reshape caleg (nested-per-partai di API vs flat-top-5 di mobile) diselesaikan lewat 1 helper `buildFromPartaiCalegs()` (generic by region-key `"kabupatens"|"keluarahans"|"tps"`) — akumulasi suara per sub-wilayah lintas semua partai×caleg (untuk list wilayah + `partaiUnggul`), lalu flatten semua caleg lintas partai diurutkan desc diambil top-5 (untuk `calegList`, tiap caleg bawa breakdown penuh dari nested array-nya sendiri).
- HTTP 201 di endpoint kecamatan: **tidak perlu penanganan khusus** — axios resolve semua status 2xx sama, terbukti aman.
- Key response `partai`/`partais` yang tidak konsisten: **disiasati dengan tidak pernah dibaca** — `partaiSuara` mobile di-derive langsung dari `partai_calegs[]` (yang field `suaraPartai`/`suaraCalegs`/`suaraTotal`-nya konsisten di semua level), bukan dari field top-level kedua yang namanya berubah-ubah.
- Field `kursiTerakhir` (cuma ada di mock lama, tidak pernah ada di response asli) **dihapus** dari `HasilRekapDapil`.
- DPRD Provinsi/DPRD Kabupaten/DPD tetap seperti gap sebelumnya — belum dikerjakan, tabel routing di atas tetap jadi referensi kalau mau lanjut ke situ.

### Budgeting Kampanye — **SELURUH 4 endpoint (summary/pos/trend/transaksi) WIRED ke backend real (2026-08-24)**

Modul backend `budgeting` (`/Users/asdarsaid/JSI/api/src/budgeting/`) **baru dibuat user sesi ini** (fitur ini sebelumnya cuma UI+mock, "belum ada endpoint" — lihat riwayat Decisions 2026-08-22). Diverifikasi via Alur Verifikasi penuh: baca `budgeting.controller.ts`/`budgeting.service.ts`/`dto/budgeting.dto.ts`/model Sequelize (`BudgetPlafon`, `BudgetTransaction`) + live curl (GET tanpa token → `401 Unauthorized`, BUKAN `404` — route ADA & di-guard `AuthGuard`, konsisten pola module lain).

**Base URL:** `EXPO_PUBLIC_API_URL` yang sedang aktif — **saat ini backend LOKAL** (`.env` = `http://<IP LAN Mac>:3000`, BUKAN `https://api.demokrasikreatif.org`), karena modul ini belum di-push ke production (lihat komentar `.env` & progress-tracker.md Decisions 2026-08-23). Kembalikan ke production begitu di-deploy — semua path di bawah TANPA prefix `/api`, sama seperti modul lain.

**Endpoint (`@Controller('budgeting')`, `@UseGuards(AuthGuard)` semua route):**
| Endpoint | Query/Body | Response `data` |
| --- | --- | --- |
| `GET /budgeting/summary` | `scope` (`bulan`\|`total`), `periode` (`YYYY-MM`, **wajib kalau `scope=bulan`**, diabaikan/tidak dikirim kalau `total`) | `{total,used,left,usedPct,txCount,overCount}` |
| `GET /budgeting/pos` | sama seperti summary | `[{name,plafon,used,pct}]` — `name` salah satu dari `BUDGET_POS_LIST` backend, **cocok byte-for-byte** dengan `BUDGET_POS_VALUES` mobile (`types/budgeting.ts` sudah dikomentari "jangan diubah tanpa koordinasi rilis mobile" — dan backend ternyata comment sama persis dari sisi sana) |
| `GET /budgeting/trend` | sama seperti summary | `[{label,amount}]` — **TIDAK ada `heightPct`**, dihitung di mobile (`services/budgeting.ts`, relatif ke titik `amount` tertinggi dalam response) |
| `GET /budgeting/transactions` | `page,limit,pos?,status?,search?` (`PaginationDto`, default `page=1,limit=10`) | `BudgetTransaction[]` (`{id,pos,nominal,keterangan,status,userId,createdAt,updatedAt,user:{id,namaLengkap}\|null}` — relasi `user` di-include sejak fix backend 2026-08-24, lihat poin di bawah) + `meta:{page,limit,offset,totalPages,total}` |
| `POST /budgeting/transactions` | body `{pos,nominal,keterangan}` (`userId` dari JWT via `@User()`, **BUKAN** dikirim client) | record baru (juga bawa `user` ter-include via `.reload()`), `status` selalu default `"Menunggu"` |
| `PATCH /budgeting/transactions/:id/status` | body `{status: "Disetujui"\|"Ditolak"}` | record ter-update — **WIRED 2026-08-24** via tombol "Setujui"/"Tolak" di `BudgetTransactionRow` (admin-only, tanpa referensi desain, izin eksplisit user) |
| `POST /budgeting/plafon` | body `{periode,pos,nominal}` (`periode` = `YYYY-MM` atau literal `"TOTAL"`, diturunkan dari `scope` di `upsertBudgetPlafon()`, TIDAK dikirim dari screen) | plafon ter-upsert — **WIRED 2026-08-24** via `BudgetPlafonScreen` (admin-only, tanpa referensi desain, izin eksplisit user) |

**Response envelope:** `apiResponse()` (bukan raw return) dipanggil LANGSUNG di controller (`{message,data,statusCode,...others}`), lalu diproses lagi oleh `TransformInterceptor` global — tapi shape-nya sudah persis yang diharapkan interceptor, jadi HASIL AKHIR tetap `{message,data}` (+`meta` untuk endpoint transaksi) SATU LEVEL, TIDAK double-wrapped. Konsisten dengan pola semua endpoint lain yang sudah diverifikasi.

**Riwayat 2 keputusan dikonfirmasi user via pertanyaan eksplisit (2026-08-24), 1 sudah RESOLVED:**
1. ~~`GET /budgeting/transactions` record CUMA punya `userId` (angka)~~ **RESOLVED 2026-08-24** — user minta backend di-update (prompt disiapkan, lihat riwayat chat), lalu dikonfirmasi ulang baca `budgeting.service.ts`: kedua method (`findTransactions()` via `findAndCountAll({include:[...]})`, `createTransaction()` via `.reload({include:[...]})` setelah `create()`) sekarang include relasi `user` (`{ model: TimsesModel, as: 'user', attributes: ['id','namaLengkap'] }`, alias `'user'` cocok nama property `@BelongsTo` di model). `fetchBudgetTransactions()`/`createBudgetTransaction()` (`src/services/budgeting.ts`) sekarang WIRED penuh — `mapBudgetTransaction()` baca `record.user?.namaLengkap`, fallback `"Pengguna #<userId>"` kalau relasi null (harusnya tidak pernah terjadi, cuma safety net). **Belum dites live via curl** (butuh JWT token asli) — cuma diverifikasi baca kode server, konsisten dengan prompt yang diminta. Kalau device melaporkan "oleh" tetap kosong/salah, cek dulu apakah backend benar-benar sudah restart dengan kode baru.
2. ~~Tabel `budget_plafons` kemungkinan masih kosong (tidak ada UI "set plafon" di mobile)~~ **RESOLVED 2026-08-24** — user tanya "dimana saya bisa setting plafon", dijawab dengan build `BudgetPlafonScreen` (admin/adminsekret-only, entry point icon `options-outline` di `headerRight` `BudgetingKampanyeScreen`). Screen ini **TIDAK ADA di canvas** `budgeting-kampanye.dc.html` — dibangun tanpa referensi visual atas izin eksplisit user (Aturan #1 CLAUDE.md, opsi "izin build tanpa referensi" dipilih via `AskUserQuestion`). List 5 `BudgetPlafonRow` (1 row per pos dari `useBudgetPos(scope)`, prefilled nilai plafon saat ini), tiap row simpan independen via `POST /budgeting/plafon` (`useUpsertBudgetPlafon()`, `src/hooks/useBudgeting.ts`) — invalidate query `["budgeting","pos",scope]`/`["budgeting","summary",scope]` on success. Kalau tabel masih kosong di device, sekarang ada jalan untuk mengisinya dari app tanpa perlu akses DB manual.

**Judgment call (bukan pertanyaan ke user, dicatat karena non-obvious):**
- **`periode` bulan berjalan** — mobile tidak punya month-picker (scope toggle cuma "Bulan Ini"/"Total Kampanye"), jadi `scope=bulan` SELALU kirim bulan kalender berjalan (`currentPeriode()`, format `YYYY-MM` dari `new Date()`). Kalau nanti mau lihat bulan lain, butuh UI picker baru — di luar scope sesi ini.
- **`scopeLabel`** (dipakai `BudgetHeroCard` "Total Anggaran {scopeLabel}") tidak dibalas backend — dihitung client-side: nama bulan Indonesia + tahun untuk `scope=bulan` (mis. "Agustus 2026"), literal `"Keseluruhan"` untuk `scope=total`.
- **"Pengeluaran Terbaru" fetch 1 halaman saja** (`page=1, limit=20`, konstanta `RECENT_TRANSACTIONS_LIMIT`) — tidak ada UI pagination/infinite-scroll untuk section ini (beda dari DPT), desain cuma minta daftar ringkas terbaru. Revisit kalau nanti perlu lihat riwayat transaksi lebih lama.
- Auth guard: `AuthGuard` (JWT) di SEMUA route — konsisten, `apiClient` sudah kirim Bearer token otomatis, tidak perlu perubahan tambahan.

### Tokoh Masyarakat (dipakai `src/services/tokoh.ts`) — modul backend ADA, WIRED 2026-08-24

Modul `tokoh` (`/Users/asdarsaid/JSI/api/src/tokoh/`) **dibuat user 2026-08-24** mengikuti spec yang diturunkan langsung dari mock service mobile (`services/tokoh.ts`/`types/tokoh.ts`) — bukan hasil observasi web (tidak ada modul ini di web `client`, yang ada cuma `/tokoh` survei standalone dengan konsep beda total, lihat progress-tracker.md Decisions 2026-08-22). Strukturnya sengaja disamakan dengan modul `budgeting` (guard, DTO, konvensi field) karena itu modul terbersih yang sudah ada — dikonfirmasi baca `tokoh.controller.ts`/`tokoh.service.ts`/`dto/tokoh.dto.ts`/`database/models/tokoh/Tokoh.ts` langsung, **cocok persis dengan spec** yang diajukan, tidak ada gap/bug yang perlu dikoreksi (beda dari `dtdoor`/`gotv` lama).

**Base URL:** sama host dengan `budgeting` (`EXPO_PUBLIC_API_URL`, saat ini backend LOKAL `.env`, bukan production).

**Auth guard:** `AuthGuard` (JWT) di SEMUA route — `apiClient` sudah kirim Bearer token otomatis.

**`GET /tokoh`** — list SEMUA tokoh (tanpa pagination/query param, dataset kecil, order `createdAt DESC`). Response: `{message, data: TokohApiRecord[]}`.

**`POST /tokoh`** — body `{nama, kategoriId, pengaruh, dukungan, estimasiBasisMassa, alamat, kecamatan, desa, pekerjaan?, noTelpon?}` (SEMUA camelCase, TIDAK ada mismatch snake_case seperti `gotv`/`dtdoor` lama). `userId` (pencipta record) **TIDAK dikirim di body** — backend ambil dari JWT via `@User()` decorator (pola sama `createBudgetTransaction`), disimpan tapi TIDAK ditampilkan ke mobile (kolom `user_id` nullable, tidak ada relasi `include` di response saat ini — beda dari `budgeting` yang sudah include `user`). Response: `{message, data: TokohApiRecord}` (status 201).

**`TokohApiRecord`:** `{id,nama,kategoriId,pengaruh,alamat,kecamatan,desa,dukungan,estimasiBasisMassa,pekerjaan,noTelpon,createdAt}` — SEMUA camelCase (field DB snake_case di-mapping via `field:` Sequelize, transparan ke client). `kategoriLabel` **TIDAK ada di response** (cuma `kategoriId`, 5 nilai tetap 1-5 sesuai `KATEGORI_TOKOH_ID_LIST` backend = `TOKOH_KATEGORI_OPTIONS` mobile) — di-derive client-side di `mapTokoh()` (`services/tokoh.ts`), sama pola dengan mock.

**Verifikasi:** dikonfirmasi via baca kode server (model/controller/service/DTO cocok 1:1 dengan spec) + live curl `GET /tokoh` tanpa token → **401 Unauthorized** (route ADA & ter-guard, bukan 404). **Belum dites live dengan JWT asli** (butuh login sungguhan) — konsisten dengan pola verifikasi `budgeting` sebelumnya, user perlu tes end-to-end di device.

**Catatan:** "Tandai Tokoh dari DPT" (icon bintang `DptCard` → `TokohFormScreen` prefill) **TETAP flip `sudahTokoh` client-side saja** (`markDptTokoh`, lihat CRUD dasar DPT di atas) — modul `tokoh` baru ini TIDAK punya kolom linkage ke DPT (`idDpt` dkk.) sama sekali, jadi tidak ada relasi baca-balik sungguhan seperti `dtdoor`/`gotv`, `sudahTokoh` akan SELALU reset ke `false` tiap list DPT di-refetch dari server real (sudah didokumentasikan sebagai keterbatasan yang disengaja, bukan bug).

### Deteksi Rival Caleg (dipakai `src/services/rivalcaleg.ts`) — modul backend ADA, WIRED 2026-08-24

Modul `rivalcaleg` (`/Users/asdarsaid/JSI/api/src/rivalcaleg/`) **dibuat user 2026-08-24** dari spec siap-pakai yang diturunkan langsung dari `types/rivalcaleg.ts` mobile (bukan observasi web — fitur ini murni mobile, tidak ada padanan di `client`). Struktur disamakan modul `budgeting` (guard/DTO/konvensi field) — dikonfirmasi baca `rivalcaleg.controller.ts`/`.service.ts`/`dto/rivalcaleg.dto.ts`/3 model (`RivalCaleg`/`RivalWilayah`/`RivalAktivitas` di `database/models/rivalcaleg/`) langsung, **cocok 1:1 dengan spec**, tidak ada gap/bug (sama kelas hasil dengan `tokoh`).

**Base URL:** sama host dengan `budgeting`/`tokoh` (`EXPO_PUBLIC_API_URL`, saat ini backend LOKAL).

**Auth guard:** `AuthGuard` (JWT) di SEMUA route, tidak ada role guard (gating admin-only kalau perlu nanti murni client-side, pola sama modul lain).

**3 resource independen** (bukan 1 endpoint gabungan) — mobile panggil 4 GET paralel lalu gabung jadi 1 snapshot client-side, lihat `ui-registry.md` § RivalCalegScreen:

- **`GET /rivalcaleg`** — list `RivalCaleg`, urut `estimasiSuara DESC`. Response `{message, data: RivalCalegApiRecord[]}` — `{id,namaLengkap,partai,noUrut,isKita,ancaman,basis,estimasiSuara,suara2024,tren,wilayahBentrok,tokohBerpihak,isuDiangkat,strategi,createdAt,updatedAt}`, semua camelCase. **`isKita` (boolean) menandai TEPAT SATU record** sebagai caleg yang kita dukung sendiri — backend jamin keunikannya (`createCaleg`/`updateCaleg` matikan dulu semua `isKita=true` lain sebelum set true). Field selain `namaLengkap`/`partai`/`noUrut`/`isKita` semuanya NULLABLE (opsional di DTO) — mobile fallback ke `"-"`/`0`/`"stabil"` di `mapRivalCalegToSummary()`.
- **`POST /rivalcaleg`** — create rival baru, body sama shape dengan record (minus id/timestamps). **`PATCH /rivalcaleg/:id`** — update partial. **`DELETE /rivalcaleg/:id`** — hapus (tidak cascade ke `RivalAktivitas` yang mereferensikannya — kalau rival dihapus, aktivitas terkait jadi punya `rivalCalegId` yatim, relasi `include` di response `GET /rivalcaleg/aktivitas` akan balas `rival: null` untuk baris itu, sudah di-fallback `"-"` di `mapRivalAktivitas()`).
- **`GET /rivalcaleg/wilayah`** — list `RivalWilayah` (`{id,nama,rivalPenantang,status}`, `status` enum `unggul`/`rival-kuat`/`bentrok`, cocok persis `RivalWilayahStatus` mobile). **`POST`**/**`PATCH /rivalcaleg/wilayah/:id`** — create/update.
- **`GET /rivalcaleg/aktivitas`** — list `RivalAktivitas` urut `tanggal DESC`, include relasi `rival` (alias `rival`, `{id,namaLengkap}`) & `pelapor` (alias `pelapor`, → `TimsesModel`, `{id,namaLengkap}`, pola sama `TRANSACTION_USER_INCLUDE` budgeting). `tanggal` adalah `DATEONLY` (`"2026-08-22"`, bukan datetime) — mobile format di render (`RivalAktivitasCard.formatTanggal()`), BUKAN di service. **`POST /rivalcaleg/aktivitas`** — body `{rivalCalegId, jenis, deskripsi, wilayah, tanggal?}`, `userId` (pelapor) **TIDAK dikirim di body** — backend ambil dari JWT via `@User()` (pola sama `createBudgetTransaction`/`createTransaction`).
- **`GET /rivalcaleg/summary`** — hero stats DIHITUNG server-side dari data `RivalCaleg`/`RivalWilayah` yang sama (bukan tabel terpisah): `{totalTerpetakan, posisiKita, ancamanTinggiCount, wilayahBentrokCount, selisihKePeringkat1Pct}`. `totalTerpetakan` = count rival (exclude `isKita`). `posisiKita` = ranking record `isKita` di antara SEMUA record diurut `estimasiSuara DESC` (termasuk dirinya), balas `"-"` kalau tidak ada record `isKita` sama sekali. `selisihKePeringkat1Pct` = 0 kalau kita sudah #1 atau tidak ada record `isKita`.

**Verifikasi:** dikonfirmasi via baca kode server (cocok 1:1 dengan spec) + live curl ke SEMUA 4 route tanpa token → **401 Unauthorized** di semuanya (route ADA & ter-guard, bukan 404). **Belum dites live dengan JWT asli** — user perlu tes end-to-end di device, pola sama `budgeting`/`tokoh`.

**Catatan:** 2026-08-24 (lanjutan) — mobile SEKARANG punya form create/edit untuk `POST/PATCH /rivalcaleg` & `/rivalcaleg/wilayah`, dan create untuk `POST /rivalcaleg/aktivitas` (`RivalCalegFormScreen`/`RivalWilayahFormScreen`/`RivalAktivitasFormScreen`, `src/screens/rivalcaleg/`) — lihat `ui-registry.md` § RivalCalegFormScreen. `DELETE /rivalcaleg/:id` juga dipanggil dari `RivalDetailSheet`. TIDAK ADA endpoint edit/hapus untuk `RivalWilayah`/`RivalAktivitas` selain yang disebut di atas — form-nya menyesuaikan (`RivalWilayahFormScreen` tanpa tombol Hapus, `RivalAktivitasFormScreen` create-only).

### Isu & Aspirasi Warga (dipakai `src/services/isuaspirasi.ts`) — modul backend ADA, WIRED PENUH 2026-08-24

Modul `isuaspirasi` (`/Users/asdarsaid/JSI/api/src/isuaspirasi/`) **dibuat user 2026-08-24** dari spec siap-pakai yang diturunkan langsung dari `types/isuaspirasi.ts`/`services/isuaspirasi.ts`/UI mobile (bukan observasi web — fitur ini murni mobile). Struktur disamakan modul `rivalcaleg` (guard/DTO/konvensi field, 2 resource + 1 summary terhitung) — dikonfirmasi baca `isuaspirasi.controller.ts`/`.service.ts`/`dto/isuaspirasi.dto.ts`/2 model (`IsuAspirasi`/`IsuJanji` di `database/models/isuaspirasi/`) langsung, **cocok 1:1 dengan spec**, tidak ada gap/bug.

**Base URL:** sama host dengan `budgeting`/`tokoh`/`rivalcaleg` (`EXPO_PUBLIC_API_URL`, saat ini backend LOKAL).

**Auth guard:** `AuthGuard` (JWT) di SEMUA route, tidak ada role guard.

**2 resource independen** (bukan 1 endpoint gabungan) — mobile panggil 3 GET paralel lalu gabung jadi 1 `IsuAspirasiSnapshot` client-side, pola sama `rivalcaleg`:

- **`GET /isuaspirasi`** — list `IsuAspirasi` (laporan mentah aspirasi warga), order `createdAt DESC`, tanpa pagination (dataset masih kecil, pola sama `tokoh`). Response `{message, data: IsuAspirasiApiRecord[]}` — `{id,warga,alamat,kecamatan,desa,kategori,keluhan,status,dijadikanMateri,createdAt,updatedAt,relawan:{id,namaLengkap}|null}`, semua camelCase. `kategori` free text (bukan enum/FK), sama pola `programBantuan` di `dtdoor`. **`POST /isuaspirasi`** — body `{warga,alamat,kecamatan,desa,kategori,keluhan}`, `userId` (relawan) **TIDAK dikirim di body** — backend ambil dari JWT via `@User()` (pola sama `createAktivitas` rivalcaleg). `status` default `"Baru"`, `dijadikanMateri` default `false`. **`PATCH /isuaspirasi/:id`** — partial `{status?, dijadikanMateri?}`.
- **`GET /isuaspirasi/janji`** — list `IsuJanji` (usulan materi kampanye, dikurasi manual tim — TIDAK auto-generated dari `IsuAspirasi`), order `createdAt DESC`. Response `{id,janji,dampak,dasar,wilayah,createdAt,updatedAt}`. **`POST`**/**`PATCH /isuaspirasi/janji/:id`**/**`DELETE /isuaspirasi/janji/:id`** — CRUD penuh, body `{janji,dampak?,dasar,wilayah}`.
- **`GET /isuaspirasi/summary`** — dihitung server-side dari tabel `isu_aspirasi` SAJA (bukan `isu_janji`): `{totalAspirasi, isuDominan, ringkasanSumber, kategori:[{nama,jumlah,wilayahTerkuat}], petaWilayah:[{nama,totalAspirasi,isuDominan,level}]}`. `wilayahTerkuat`/`isuDominan` per grup = nilai dengan count terbanyak di dalam grup. `level` peta wilayah dihitung relatif ke desa count tertinggi (`percentOfMax>=70→"tinggi"`, `>=40→"sedang"`, sisanya `"rendah"`). `ringkasanSumber` **SENGAJA beda dari mock lama** (mock: "Dari 245 kunjungan D2D..." — butuh join ke modul `dtdoor`, di luar scope) — versi real pakai template self-contained `"{total} aspirasi tercatat dari {jumlahDesa} kelurahan · isu terbanyak: {isuDominan}"`.

**Verifikasi:** dikonfirmasi via baca kode server (cocok 1:1 dengan spec) + live curl ke `GET /isuaspirasi`, `/isuaspirasi/summary`, `/isuaspirasi/janji`, `POST /isuaspirasi` tanpa token → **401 Unauthorized** di semuanya (route ADA & ter-guard, bukan 404). **Belum dites live dengan JWT asli** — user perlu tes end-to-end di device, pola sama modul lain.

**Judgment call mobile-side (bukan pertanyaan ke user, dicatat karena non-obvious):**
- **`laporanSerupa`** (field di `IsuAspirasiDetailSheet`) **TIDAK dibalas backend** — dihitung client-side di `mapIsuAspirasiList()` (`services/isuaspirasi.ts`): jumlah record LAIN dengan `kategori`+`desa` yang sama dari list penuh yang sudah di-fetch. Kalau dataset besar & wajib server-side nanti, revisit (butuh endpoint agregat tambahan).
- **`tanggal`** = `createdAt` mentah (ISO), diformat saat render di `IsuAspirasiCard.tsx` (`Intl.DateTimeFormat("id-ID",...)`, pola sama `RivalAktivitasCard`) — TIDAK ada field `tanggal` terpisah seperti `RivalAktivitas` (tidak ada kebutuhan backdate untuk laporan warga, beda dari aktivitas rival kompetitor).

**Status wiring (2026-08-24):** **SEMUA endpoint WIRED PENUH.** `GET /isuaspirasi`, `GET /isuaspirasi/janji`, `GET /isuaspirasi/summary` (gabung jadi `useIsuAspirasiSnapshot()`), `PATCH /isuaspirasi/:id` (backing tombol "Tandai Ditindak"/"Jadikan Materi" di `IsuAspirasiDetailSheet`, via `useUpdateIsuAspirasi()`), `POST /isuaspirasi` (`IsuAspirasiFormScreen`, tombol "+ Catat Aspirasi Warga"), dan CRUD penuh `/isuaspirasi/janji` (`IsuJanjiFormScreen` dual create/edit + tombol Hapus inline — TIDAK ada detail sheet terpisah untuk Janji, `IsuJanjiCard` sudah menampilkan semua field-nya jadi tap langsung ke form, pola sama `RivalWilayahRow`). 2 screen form terakhir **dibangun TANPA referensi visual** (izin eksplisit user 2026-08-24, CLAUDE.md Aturan #1 — canvas artboard 15 cuma versi read-only untuk kedua alur ini).

### Quick Count (dipakai `src/services/quickcount.ts`) — modul backend ADA, WIRED PENUH 2026-08-24

Modul `quickcount` (`/Users/asdarsaid/JSI/api/src/quickcount/`) **dibuat user 2026-08-24** dari spec siap-pakai yang diturunkan dari `types/quickcount.ts`/`services/quickcount.ts` mobile (murni mobile, tidak ada padanan di web). Struktur disamakan `rivalcaleg`/`isuaspirasi` (guard/DTO/konvensi field) — dikonfirmasi baca `quickcount.controller.ts`/`.service.ts`/`dto/quickcount.dto.ts`/3 model (`QuickCountKandidat`/`QuickCountTps`/`QuickCountHasil` di `database/models/quickcount/`) langsung, **cocok 1:1 dengan spec**, tidak ada gap/bug.

**Base URL:** sama host dengan `budgeting`/`tokoh`/`rivalcaleg`/`isuaspirasi` (`EXPO_PUBLIC_API_URL`, saat ini backend LOKAL).

**Auth guard:** `AuthGuard` (JWT) di SEMUA route, tidak ada role guard (gating admin-only untuk mutasi kandidat/TPS dilakukan client-side).

**⚠️ Keputusan arsitektur penting (bukan default, hasil wawancara eksplisit `AskUserQuestion` 2026-08-24):** riset awal sempat menemukan modul `tps` (`GET /tps?kelId=X`) yang SUDAH ADA di backend & match skema wilayah asli (DPT) — tapi **user eksplisit menolak integrasi itu** ("saya tidak butuh integrasi ke tps di data dpt"). `QuickCountTps` di bawah **BERDIRI SENDIRI**, TIDAK ada FK/relasi apapun ke `RdppTps`/data wilayah DPT manapun — TPS-nya sendiri yang di-CRUD admin dari mobile. (Catatan terpisah, tidak relevan lagi untuk modul ini: `GET /tps` sendiri saat ditemukan ternyata masih crash 500 karena `RdppTps` belum terdaftar di `tablesWilayah`, `database.providers.ts` — bug itu di luar scope Quick Count, dibiarkan.)

**5 resource/endpoint** — mobile panggil 2 GET paralel (`tps`+`hasil`, TANPA filter) lalu gabung jadi 1 `QuickCountTps[]` client-side (pola sama `rivalcaleg`/`isuaspirasi`, filter wilayah untuk tab ini diterapkan CLIENT-SIDE dari hasil gabungan itu — dataset kecil, tanpa pagination), PLUS 2 endpoint agregasi baru (`summary`/`rekap`) yang DIPANGGIL DENGAN filter wilayah aktif (agregasinya cuma masuk akal dihitung server-side). Lihat `ui-registry.md` § QuickCountScreen:

- **`GET /quickcount/kandidat`** — list `QuickCountKandidat` (`{id,nama,partai,createdAt,updatedAt}`), order `id ASC`, tanpa pagination. **`POST`**/**`PATCH /quickcount/kandidat/:id`**/**`DELETE /quickcount/kandidat/:id`** — CRUD penuh, body `{nama, partai?}`. `partai` boleh string kosong (kandidat independen, mis. entri "Lainnya").
- **`GET /quickcount/tps?kabupaten=&kecamatan=&kelurahan=`** — list `QuickCountTps` (`{id,noTps,kabupaten,kecamatan,kelurahan,namaSaksi,totalDpt,createdAt,updatedAt}`), order `id ASC`. Query filter **exact match** (bukan pencarian teks), opsional & cascading (2026-08-24, ditambah user sendiri) — mobile TIDAK memakai filter ini untuk `GET /tps` biasa (fetch selalu unfiltered, filter diterapkan client-side, lihat di bawah). **`POST`**/**`PATCH /quickcount/tps/:id`**/**`DELETE /quickcount/tps/:id`** — CRUD penuh, body `{noTps,kabupaten,kecamatan,kelurahan,namaSaksi?,totalDpt?}`. `kabupaten` (`STRING(100) NOT NULL`) — target kampanye lintas kabupaten, mis. 1 dapil DPR RI menaungi beberapa kabupaten, WAJIB diisi di form.
- **`GET /quickcount/hasil?kabupaten=&kecamatan=&kelurahan=&tpsId=`** — list SEMUA `QuickCountHasil` (`{id,tpsId,status,suaraPerKandidat,totalSuaraSah,userId,createdAt,updatedAt,relawan:{id,namaLengkap}|null,tps:{...}}` — filter wilayah di-JOIN ke tabel `tps` di backend, filter `tpsId` langsung ke kolom sendiri; `suaraPerKandidat` JSON `{"<kandidatId>":jumlah}` — **key JSON selalu string**, mobile convert ke number saat mapping). Mobile fetch TANPA filter (sama alasan `/tps`). **`POST /quickcount/hasil`** — body `{tpsId, suaraPerKandidat:[{kandidatId,jumlah}]}`, `userId` **TIDAK dikirim di body** (JWT via `@User()`). Upsert by `tpsId` (unique constraint). `status` SELALU `"Menunggu"` (belum ada alur review korwil).
- **`GET /quickcount/summary?kabupaten=&kecamatan=&kelurahan=`** — **BARU 2026-08-24** (permintaan user "filtering dan summary... agar lebih mudah digunakan", ditambahkan user sendiri ke backend). Response `{totalTps, tpsMasuk, tpsBelumMasuk, persentaseTpsMasuk, totalDpt, totalSuaraSah, persentasePartisipasi, kandidat: [{kandidatId,nama,partai,totalSuara,persentase}]}` (kandidat sudah terurut `totalSuara DESC`) — dihitung server-side dari `QuickCountTps`+`QuickCountHasil` yang match filter. Mengganti perhitungan client-side lama di `QuickCountScreen` (yang sekarang cuma butuh `useQuickCountSummary(filter)`, tidak reimplementasi agregasi apapun).
- **`GET /quickcount/rekap?level=kabupaten|kecamatan|kelurahan&kabupaten=&kecamatan=&kelurahan=`** — **BARU 2026-08-24**, breakdown per wilayah pada `level` yang dipilih (default `kabupaten` di backend kalau tidak dikirim, mobile SELALU kirim eksplisit). Response array `{wilayah, totalTps, tpsMasuk, tpsBelumMasuk, persentaseTpsMasuk, totalDpt, totalSuaraSah, suaraPerKandidat: [{kandidatId,nama,partai,totalSuara}]}[]`, terurut `wilayah` alfabetis (BUKAN by performa). `filter` di query menyempitkan cakupan SEBELUM di-grup by `level` (mis. filter `kabupaten=Bandung` + `level=kecamatan` → breakdown per kecamatan HANYA di dalam Kab. Bandung). Dipakai tab baru "Rekap Wilayah" untuk bandingkan progres antar kabupaten/kecamatan/kelurahan.

**Verifikasi:** dikonfirmasi via baca kode server (cocok 1:1 dengan spec revisi) + live curl ke SEMUA route (termasuk `summary`/`rekap` baru) tanpa token → **401 Unauthorized** di semuanya (route ADA & ter-guard, bukan 404). **Belum dites live dengan JWT asli** — user perlu tes end-to-end di device.

**Catatan migrasi kolom `kabupaten` (2026-08-24, RESOLVED):** sempat diragukan karena `database-timses.providers.ts` pakai `sequelize.sync()` polos (TANPA `alter:true`, cuma bikin tabel baru — tidak auto-ALTER tabel yang sudah ada). User konfirmasi sudah selesai menerapkan sisi backend-nya. Live curl `GET`/`POST /quickcount/tps` tetap balas 401 (route sehat) — **kolom sungguhan di database belum dites end-to-end dengan JWT asli oleh siapa pun**, tetap worth diperhatikan kalau nanti "+ Tambah TPS" balas 500 soal kolom tidak ditemukan.

**Judgment call mobile-side (bukan pertanyaan ke user, dicatat karena non-obvious):**
- **Status TPS `"Belum masuk"`/`"Tanpa saksi"` TIDAK dibalas backend** (backend cuma punya 3 nilai status: `Menunggu`/`Terverifikasi`/`Selisih`, khusus row `QuickCountHasil` yang sudah ada) — dihitung client-side di `deriveStatusMeta()` (`services/quickcount.ts`) dari kombinasi ada/tidaknya row hasil untuk `tpsId` itu + ada/tidaknya `namaSaksi` di `QuickCountTps`. Field `meta` (teks deskriptif "Dikirim 13:42 · ...") juga full di-generate client-side dari status+`updatedAt`, bukan kolom backend.
- **`fetchQuickCountKandidatList()`/`fetchQuickCountTpsList()`/`submitQuickCountHasil()` semua ganti signature return** dibanding versi mock lama (`submitQuickCountHasil` dulu balas `Promise<QuickCountTps>`, sekarang `Promise<void>` — tidak ada consumer yang butuh nilai baliknya, invalidate query cukup untuk refetch).

**Status wiring (2026-08-24):** **SEMUA endpoint WIRED PENUH** — 2 GET (tps+hasil) digabung jadi `useQuickCountTpsList()`, `GET /quickcount/kandidat` jadi `useQuickCountKandidatList()`, `POST /quickcount/hasil` backing tombol "+ Input Hasil C1"/tap kartu TPS (`useSubmitQuickCountHasil()`). CRUD kandidat (`QuickCountKandidatScreen`/`QuickCountKandidatFormScreen`) & CRUD TPS baru (`QuickCountTpsManageScreen`/`QuickCountTpsFormScreen`, entry point icon `location-outline` di `headerRight` `QuickCountScreen`, di samping `people-outline` untuk kandidat) — SEMUA admin-only, **dibangun TANPA referensi visual** (izin eksplisit user 2026-08-24, sama izin dengan kandidat, CLAUDE.md Aturan #1).

### Anti-Fraud (dipakai `src/services/antifraud.ts` + `src/services/dtdoor.ts`) — modul backend ADA, WIRED PENUH 2026-08-25

Modul `antifraud` (`/Users/asdarsaid/JSI/api/src/antifraud/`) **dibuat user sendiri (5 stage) 2026-08-25**, mengikuti staged plan yang diturunkan dari gap-analysis mobile (`AntiFraudScreen` sebelumnya mock-only, lihat progress-tracker.md Decisions 2026-08-22). Dikonfirmasi baca `antifraud.controller.ts`/`.service.ts`/model (`database/models/antifraud/FraudCase.ts`) + perubahan terkait di `dtdoor.service.ts`/`DtdoorKunjungan.ts` langsung, cocok 1:1 dengan 5 stage yang direncanakan — **tidak ada gap** kecuali 1 catatan kecil di bawah (payload `POST /dtdoor` tidak balas kunjungan yang baru dibuat, diakali client-side).

**Base URL:** sama host dengan modul lain (`EXPO_PUBLIC_API_URL`, saat ini backend LOKAL).

**Auth guard:** `AuthGuard` (JWT) di semua route `/antifraud/*`.

**Prasyarat data (yang membuat sinyal fraud punya arti, bukan cuma UI kosong) — semuanya sekarang ADA di `DtdoorKunjungan`:** `timsesId` (FK ke relawan asli, gantikan `namaRelawan` string bebas untuk keperluan scoring/burst), `lat`/`long` (GPS titik input, `DECIMAL(10,7)`, nullable), `fotoPath`/`fotoHash`/`fotoSumber` (bukti foto + hash SHA256 untuk deteksi duplikat).

**4 endpoint:**
- **`GET /antifraud/summary`** — bentuk response **cocok 1:1** dengan `AntiFraudSnapshot` (`src/types/antifraud.ts`), TIDAK perlu mapping field: `{kunjunganTervalidasi, totalKunjungan, jenisAnomali:[{nama,jumlahKasus,severity}], cases:[{id,relawan,target,waktu,level,alasan,gpsStatus,fotoStatus,checks}], relawanSkor:[{id,nama,skor,jumlahKunjungan,jumlahDitandai}]}`. `cases` **TIDAK menyertakan field `status`** (lihat catatan gap di bawah).
- **`GET /antifraud/cases/:id`** — detail 1 `FraudCase` (model lengkap, TERMASUK `status`) — belum dipakai mobile (evidence sheet reuse item dari array `summary().cases`, bukan fetch terpisah).
- **`PATCH /antifraud/cases/:id/approve`** / **`PATCH /antifraud/cases/:id/reject`** — set `status`+`reviewedBy` (dari JWT `@User()`)+`reviewedAt`. Body kosong. `rejected` = reviewer bilang false-positive, dikeluarkan dari perhitungan `relawanSkor`/`kunjunganTervalidasi`; `pending`/`approved` masih dihitung bermasalah.

**⚠️ Gap kecil (belum di-fix, workaround client-side sudah jalan):** `AntiFraudService.getSummary()` (backend) **tidak menyertakan `status`** di tiap item array `cases` — jadi setelah approve/reject, kasus yang sama masih akan muncul lagi di response `GET /antifraud/summary` berikutnya tanpa penanda apa pun. **Workaround:** `AntiFraudScreen.tsx` menyimpan `reviewedIds` (state lokal, `Set<number>`) dan memfilter `cases` dengan itu — cuma bertahan selama screen tidak di-remount (reset kalau navigasi keluar-masuk atau restart app). `src/types/antifraud.ts` sudah menyiapkan field opsional `FraudCase.status` untuk migrasi mulus begitu backend menambah field ini ke mapping `getSummary()` (1 baris: `status: kasus.status` di `cases.map(...)`).

**Perubahan terkait di `POST /dtdoor` (dipakai `createDtdoor()`, `src/services/dtdoor.ts`):**
- `KunjunganInput` sekarang kirim `timsesId` (dari `session.user.id` — relawan yang login, BUKAN input teks `namaRelawan` yang tetap ada terpisah untuk display), `lat`/`long` (GPS diam-diam saat submit via `expo-location`, best-effort — kosong kalau permission ditolak/GPS mati, **BUKAN dianggap error**, backend menandainya "GPS nonaktif" secara otomatis, itu memang sinyal yang ingin ditangkap).
- **Foto WAJIB diambil dari kamera in-app** (`expo-image-picker`, `launchCameraAsync` — bukan galeri, enforcement di titik input) sebelum "Simpan Kunjungan" bisa ditekan (`DtdoorFormScreen.tsx`). Diunggah lewat endpoint **terpisah** `POST /dtdoor/kunjungan/:id/foto` (multipart, field file `foto` + field teks `fotoSumber`) — **`:id` di sini adalah id row `DtdoorKunjungan`, BUKAN `Dtdoor.id` maupun `idDpt`**.
- **Catatan penting:** `POST /dtdoor` (create) balas row `Dtdoor` top-level saja — **TIDAK menyertakan `kunjungans[]`** yang baru dibuat (`DtdoorService.create()` return `created` langsung dari `Dtdoor.create()`, sebelum relasi kunjungan di-eager-load). Jadi mobile tidak langsung tahu `kunjunganId` untuk dipakai upload foto. **Diakali di `createDtdoor()`** (`services/dtdoor.ts` § `uploadFotoKunjunganTerbaru`): setelah create sukses, fetch balik `GET /dtdoor/:idDpt` (pakai `idDpt` yang SAMA persis dipakai saat create, disimpan di variabel `idDptUsed` — bukan digenerate ulang) untuk dapat `kunjungans[0].id`, baru upload foto ke situ. Kegagalan upload foto (network/dsb.) **tidak membatalkan** kunjungan yang sudah tersimpan, cuma di-`console.error`.

**Verifikasi:** dikonfirmasi via baca kode server (`antifraud.controller.ts`/`.service.ts`/`DtdoorKunjungan.ts`/`dtdoor.service.ts`, cocok 1:1 dengan 5 stage) + live curl ke backend LOKAL tanpa token: `GET /antifraud/summary` & `/antifraud/cases` → **401 Unauthorized** (route ADA & ter-guard, bukan 404).

**Status wiring (2026-08-25):** **WIRED PENUH** — `AntiFraudScreen` baca `GET /antifraud/summary` (`useAntiFraudSnapshot`), tombol Setujui/Tolak di `AntiFraudEvidenceSheet` sekarang mutation sungguhan (`useApproveFraudCase`/`useRejectFraudCase`, PATCH approve/reject — bukan lagi dummy `Alert`). `DtdoorFormScreen` (Program Pemenangan → Door To Door) diperluas: capture GPS otomatis + foto wajib dari kamera saat submit kunjungan, supaya dashboard Anti-Fraud punya sinyal nyata (bukan selalu kosong/selalu ter-flag "GPS nonaktif"). `npx tsc --noEmit` + `npx expo export --platform ios` bersih. **Belum dikonfirmasi user bekerja end-to-end di device** (submit kunjungan baru → foto+GPS tersimpan → muncul/tidak muncul di dashboard Anti-Fraud sesuai ekspektasi).

### Kekuatan Wilayah (dipakai `src/services/dtdoor.ts` § `fetchKekuatanWilayahRekap`) — endpoint agregasi baru, WIRED 2026-08-25

Sebelumnya `KekuatanWilayahScreen` (chart klasifikasi kelurahan Kuat/Sedang/Lemah, `KekuatanZoneStackedBar`) pakai `fetchDtdoorAll()` yang **SELALU `throw`** di mode API real (tidak ada endpoint dump-semua-record tanpa pagination) — screen ini rusak total sejak `EXPO_PUBLIC_USE_MOCK_API=false`. User awalnya diajak diskusi peta geografis (ditemukan model `GeojsonKel`/`GeojsonKec` dengan kolom `geometry` di `/Users/asdarsaid/JSI/api/src/database/models/`, TAPI **tidak ada controller/service yang expose-nya ke HTTP manapun** — grep "geojson" di luar folder `database/models` nihil) — **user pilih TIDAK pakai peta/koordinat**, cukup chart klasifikasi per wilayah yang sudah ada, tinggal disambungkan ke data agregat asli.

**Endpoint baru dibuat user sendiri (prompt disiapkan, pola sama Anti-Fraud):** `GET /dtdoor/rekap-kekuatan-wilayah?kabId=&kecamatan=` (keduanya opsional, nama/tipe konsisten `GetDtdoorDto`). **Method terpisah dari `rekapGroup()` yang sudah ada** (shape hasilnya beda — `rekapGroup` group by 1 dimensi dengan wilayah sebagai filter, endpoint baru ini group by 2 dimensi sekaligus: `Dtdoor.desa` × `DtdoorKunjungan.tipePemilihId`). Route diletakkan SEBELUM `@Get(':idDpt')` di controller (pola sama route literal lain seperti `kabupatens`/`kepala-keluarga`) supaya tidak ke-capture sebagai param `idDpt` — dikonfirmasi live tidak ke-shadow (`GET /dtdoor/1` tetap balas 200, bukan disambar route baru).

**Response (dikonfirmasi live via curl, cocok 1:1 dengan spec):** `{message, data: [{desa, totalKunjungan, kategori: [{tipePemilihId, jumlah}]}]}` — cuma kategori dengan kunjungan >0 yang muncul (tidak diisi nol untuk kategori kosong). Auth: **tidak ada guard**, konsisten dengan seluruh `DtdoorController` yang memang tidak di-guard sama sekali (keputusan sengaja, bukan lupa — lihat catatan lama di § dtdoor).

**Perhitungan skor tetap 100% di mobile** (`lib/dtdoorScore.ts` § `computeWeightedTier()`, fungsi baru) — server cuma kirim breakdown jumlah per kategori, bukan skor. Rata-rata tertimbang (`Σ(skor kategori × jumlah) / Σjumlah`) **secara matematis identik** dengan rata-rata per-record lama (setiap record dalam 1 kategori yang sama selalu berkontribusi skor yang sama persis) — cuma pindah lokasi hitung, formula/threshold (`KATEGORI_SCORE`/`STRENGTH_THRESHOLDS`) TIDAK berubah. Kategori tak termapping tetap dikecualikan dari pembagi (bukan dianggap skor 0), sama seperti perilaku lama.

**Verifikasi:** live curl ke backend LOKAL — `GET /dtdoor/rekap-kekuatan-wilayah` (tanpa filter) balas 200 data asli (9 kelurahan, breakdown kategori benar); `?kecamatan=Cileunyi` balas `data:[]` (kecamatan itu memang tidak ada di data real saat ini — dataset seed DB lokal beda dari nama kecamatan demo di mock, filter-nya sendiri jalan benar, bukan bug); `GET /dtdoor/1` tetap 200 (route `:idDpt` tidak ke-shadow).

**Status wiring (2026-08-25):** **WIRED PENUH untuk `KekuatanWilayahScreen` SAJA** — hook baru `useKekuatanWilayahRekap()` menggantikan `useDtdoorAll()` di screen ini. **`useDtdoorAll()`/`fetchDtdoorAll()` SENGAJA TIDAK disentuh** (masih `throw` di real mode) — dipakai 3 screen lain (`KekuatanPemilihScreen`, `SwingVoterFollowUpScreen`, `DtdoorAnalyticsScreen`) yang butuh record MENTAH per-pemilih (nama, ranking individual), bukan agregat per-kelurahan — di luar scope task ini, masih rusak di real mode sampai ada endpoint serupa untuk kebutuhan itu. `npx tsc --noEmit` + `npx expo export --platform ios` bersih. **Belum dikonfirmasi user bekerja di device** dengan data real (baru dites live via curl, belum lewat UI).

### Kekuatan Pemilih (dipakai `src/services/dtdoor.ts` § `fetchKekuatanPemilihList`) — endpoint baru, WIRED 2026-08-25

`KekuatanPemilihScreen` (ranking individual top-10 pemilih per skor, distribusi Kuat/Sedang/Lemah, ranking kelurahan) juga pakai `useDtdoorAll()`/`fetchDtdoorAll()` — sama blocker dengan Kekuatan Wilayah (selalu `throw` di real mode). Beda dari Kekuatan Wilayah: screen ini butuh IDENTITAS individual (nama+tanggal kunjungan per pemilih untuk ranking top-10), bukan cuma agregat — jadi TIDAK bisa dipenuhi lewat `rekap-kekuatan-wilayah` (yang cuma balas hitungan per kelurahan, tanpa identitas per-orang).

**Endpoint baru dibuat user sendiri:** `GET /dtdoor/kekuatan-pemilih?kecamatan=` — daftar individual RINGKAS (bukan agregat), JOIN `DtdoorKunjungan`→`Dtdoor` (pola sama `rekapKekuatanWilayah()`), filter `tipePemilihId IS NOT NULL`, **tanpa `GROUP BY`** (raw+nest, per-baris). Route juga diletakkan sebelum `@Get(':idDpt')` (dikonfirmasi live tidak ke-shadow, sama pola Kekuatan Wilayah).

**Response (dikonfirmasi live, 24 baris):** `{message, data: [{id, namaLengkap, desa, kecamatan, tipePemilihId, createdAt}]}` — **`id` di sini adalah id row `DtdoorKunjungan`** (bukan `Dtdoor.id`), **`createdAt` adalah tanggal kunjungan itu sendiri** (bukan tanggal rumah tangga/`Dtdoor` dibuat). Tidak ada sorting/scoring di backend — murni data mentah, skor tetap dihitung 100% di mobile (`getDtdoorScore()`/`getStrengthTier()`, TIDAK ada fungsi baru — reuse persis logic lama, cuma field `kategoriId`→`tipePemilihId`). Auth: tidak ada guard, konsisten `DtdoorController`.

**Keputusan wiring (mobile-side, bukan pertanyaan ke user):** endpoint ini **SELALU di-fetch UNFILTERED** (parameter `kecamatan`-nya TIDAK dipakai dari mobile) — dropdown filter kecamatan di screen butuh opsi dari SELURUH dataset, jadi tetap fetch semua lalu filter CLIENT-SIDE seperti logic lama (`selectedKecamatan` di `KekuatanPemilihScreen`), bukan re-fetch server-side tiap ganti pilihan. `GET /dtdoor/kecamatans` (endpoint lama yang sudah ada) TIDAK dipakai untuk dropdown ini — opsi kecamatan tetap di-derive dari hasil fetch yang sama (pola sama sebelum wiring), supaya tidak perlu 2 request terpisah untuk 1 screen.

**Verifikasi:** live curl — unfiltered balas 24 baris data asli (shape tepat), `?kecamatan=Sukajadi` balas 4 baris ter-filter benar, `GET /dtdoor/1` tetap 200 (`:idDpt` tidak ke-shadow).

**Status wiring (2026-08-25):** **WIRED PENUH** — `useKekuatanPemilihList()` (hook baru) menggantikan `useDtdoorAll()`. `KekuatanPemilihCard` re-tipe prop `item` dari `Dtdoor` ke `KekuatanPemilihRecord` (component ini cuma dipakai di screen ini, aman diubah). **`SwingVoterFollowUpScreen`/`DtdoorAnalyticsScreen` SENGAJA TIDAK disentuh (saat itu)** — lihat 2 entri di bawah untuk kelanjutannya. `npx tsc --noEmit` + `npx expo export --platform ios` bersih. **Belum dikonfirmasi user bekerja di device** (baru dites live via curl).

### Swing Voter Follow-up (dipakai `src/services/dtdoor.ts` § `fetchDtdoorByTipePemilih`) — TIDAK PERLU endpoint baru, WIRED 2026-08-25

Beda dari Kekuatan Wilayah/Pemilih, fitur ini **tidak butuh backend baru sama sekali** — `GET /dtdoor` (list, sudah ada) TERNYATA sudah mendukung filter `tipePemilihId` di `GetDtdoorDto` (dipakai `findAll()` untuk filter nested `kunjungans.tipePemilihId`), dikonfirmasi live: `GET /dtdoor?tipePemilihId=7&limit=5` balas cuma record kategori "Belum Menentukan" (1 baris di data saat ini), shape sama persis `Dtdoor` yang sudah dipetakan `mapDtdoor()`.

**Mobile:** `fetchDtdoorByTipePemilih(tipePemilihId)` (baru, `services/dtdoor.ts`) — loop SEMUA halaman `GET /dtdoor?tipePemilihId=X&limit=50&page=N` sampai `meta.totalPages` habis, lalu gabung jadi `Dtdoor[]`. **Aman di-loop penuh** (beda dari `fetchDtdoorAll()` lama yang throw) karena scope-nya sudah SEMPIT — cuma 1 kategori dari 7, bukan seluruh tabel. `useSwingVoterList()` (hook baru, kategori 7 hardcode di situ) menggantikan `useDtdoorAll()` di `SwingVoterFollowUpScreen`. Sorting `createdAt` ascending tetap di mobile (backend tidak sort).

**Status wiring:** **WIRED PENUH.** `npx tsc --noEmit` + `npx expo export --platform ios` bersih. Belum dikonfirmasi user bekerja di device.

### Ringkasan Data / DtdoorAnalyticsScreen — 3 dari 4 kebutuhan REUSE endpoint existing, 1 gap kecil (breakdown jenis kelamin) BELUM diimplementasi (2026-08-25)

Riset sebelum minta endpoint baru: TERNYATA 3 dari 4 section screen ini sudah bisa dipenuhi tanpa endpoint baru:
- **Distribusi Kategori** → `GET /dtdoor/rekap-group/tipePemilihId` (sudah ada, dites TANPA filter — balas breakdown 7 kategori penuh + `jumlahDtdoor`/`jumlahWajibPilih` per kategori, zero-filled untuk kategori kosong).
- **Program Bantuan Terpopuler** → `GET /dtdoor/rekap-group/programBantuanId` (sudah ada, sama pola) — **ini sekaligus FIX bug lama**: kolom top-level `programBantuan1/2/3` yang dipakai screen ini SUDAH TIDAK DIISI oleh create baru (skema 2024, lihat § dtdoor), jadi section ini sebenarnya SELALU KOSONG/SALAH sejak wiring dtdoor ke skema baru — ganti ke endpoint ini otomatis memperbaikinya (data dari relasi `kunjungan.programBantuanId`, sumber yang benar).
- **Total Kelurahan** → `GET /dtdoor/kelurahans` (sudah ada) — `data.length` = jumlah kelurahan unik.
- **Total Kunjungan & Total Wajib Pilih** → derivable dari MENJUMLAHKAN `jumlahDtdoor`/`jumlahWajibPilih` across 7 baris `rekap-group/tipePemilihId` (dikonfirmasi live: total 24, cocok dengan hitungan independen dari `kekuatan-pemilih`). **Catatan edge-case (bukan bug, cuma keterbatasan diketahui):** kalau ada `Dtdoor` row TANPA kunjungan sama sekali (harusnya tidak terjadi di alur create normal), baris itu tidak ikut ke-hitung di sini tapi BISA ikut ke-hitung di `kelurahans()` (yang tidak require kunjungan) — potensi selisih kecil antara "Total Kunjungan" dan cakupan "Total Kelurahan", diterima sebagai keterbatasan bukan diblokir.

**Gap yang TERSISA (backend belum ada, endpoint kecil dibutuhkan):** **Distribusi Gender (L/P)** — tidak ada endpoint manapun yang expose ini. Pola identik 3 method one-liner yang sudah ada di `DtdoorService` (`kabupatens()`/`kecamatans()`/`kelurahans()`, `Dtdoor.findAll({attributes:['jenisKelamin',[fn('COUNT','jenis_kelamin'),'total']], group:['jenisKelamin']})`) — tinggal 1 method+route baru dengan pola yang SAMA PERSIS, ganti kolomnya jadi `jenisKelamin`.

**Endpoint gender selesai dibuat user (2026-08-25), dikonfirmasi live**: `GET /dtdoor/jenis-kelamin` → `{message, data: [{jenisKelamin:"L"|"P", total}]}` (pola sama persis `kabupatens()`/`kecamatans()`/`kelurahans()`, null TIDAK difilter di backend, difilter di mobile saat mapping).

**Status wiring (2026-08-25):** **WIRED PENUH** — `fetchDtdoorAnalytics()` (baru, `services/dtdoor.ts`) menggabungkan 4 endpoint (`Promise.all`, pola sama isuaspirasi/quickcount) jadi 1 `DtdoorAnalyticsSnapshot`: `totalKunjungan`/`totalWajibPilih` dari SUM baris `rekap-group/tipePemilihId`, `totalKelurahan` dari panjang array `kelurahans()`, `kategori`/`programBantuan` langsung dari 2 endpoint `rekap-group` (zero-filled), `jenisKelamin` dari endpoint baru (null difilter di mobile). `useDtdoorAnalytics()` (hook baru) menggantikan `useDtdoorAll()` di `DtdoorAnalyticsScreen`. **2 penyesuaian client-side saat wiring** (bukan gap backend): (1) "belum dikategorikan" akan SELALU 0 dengan sumber data ini (setiap kunjungan wajib py `tipePemilihId`, beda dari skema lama) — field dipertahankan di kode, cuma tidak pernah muncul lagi; (2) baris `programBantuan` dengan `jumlah=0` (zero-fill backend) difilter dulu sebelum diambil top-5, supaya "Program Bantuan Terpopuler" tidak menampilkan program yang belum pernah dipakai (parity dengan perilaku lama yang cuma menghitung program yang benar-benar terjadi). Mock mode (`fetchDtdoorAnalyticsMock`) meniru PERSIS agregasi lama dari `mockDtdoorList` — perilaku demo tidak berubah. `npx tsc --noEmit` + `npx expo export --platform ios` bersih. **Belum dikonfirmasi user bekerja di device** (baru dites live via curl per endpoint, belum lewat UI gabungan).

### Pengumuman (dipakai `src/services/pengumuman.ts`) — modul backend ADA, service+hook mobile WIRED 2026-08-26, UI BELUM (menunggu referensi desain)

Modul `pengumuman` (`/Users/asdarsaid/JSI/api/src/pengumuman/`) **dibuat user 2026-08-26** dari prompt spec yang diajukan (fitur baru — pengumuman satu-arah dari HQ/admin ke seluruh jajaran timses kabupaten→kecamatan→desa, menggantikan konsep "WhatsApp Broadcast" yang dulu dikeluarkan dari MVP karena tidak ada desain, lihat progress-tracker.md Decisions 2026-08-21). **Diverifikasi via baca kode langsung** (`pengumuman.controller.ts`/`.service.ts`/`dto/pengumuman.dto.ts`/model `Pengumuman.ts`/`types.ts` + registrasi di `app.module.ts` & `database-timses.providers.ts`) — **cocok 1:1 dengan spec yang diajukan**, tidak ada gap/bug. **⚠️ Belum dites live via curl** — backend lokal (`http://172.20.10.5:3000`, IP LAN Mac user) TIDAK reachable dari lingkungan tempat verifikasi ini dijalankan (kemungkinan server belum di-start, atau jaringan beda) — kalau device nanti melaporkan gagal konek, pastikan dulu `yarn start:dev` jalan di `/Users/asdarsaid/JSI/api` dan device+laptop 1 jaringan yang sama dengan IP di `.env` mobile.

**Base URL:** sama host dengan `budgeting`/`tokoh`/dst. (`EXPO_PUBLIC_API_URL`, backend LOKAL saat ini).

**Auth guard:** `AuthGuard` (JWT) di `GET /pengumuman`. `POST`/`DELETE` TAMBAH `RolesGuard`+`@Roles('admin','adminsekret')` — admin-only di level server (bukan cuma client-side gating seperti budgeting/quickcount), pola sama `POST /user`.

**3 endpoint:**
- **`GET /pengumuman`** — list, **SUDAH di-scope server-side** (`getRequesterScope()`, helper yang sama dipakai `GET /dtdoor`/`GET /gotv`/`GET /user/list`): admin/adminsekret lihat semua; role lain lihat pengumuman `targetLevel='semua'` DITAMBAH pengumuman yang target wilayahnya cocok dengan `kabId`/`kecId`/`kelId` requester sendiri (dibaca dari `TimsesModel` tiap request, bukan dari JWT). Requester tanpa wilayah sama sekali (`kabId`/`kecId`/`kelId` semua `null`) HANYA lihat `targetLevel='semua'`. Response `{message, data: PengumumanApiRecord[]}` — `{id,judul,isi,prioritas,targetLevel,targetKabId,targetKecId,targetKelId,createdAt,pembuat:{id,namaLengkap}|null}`, semua camelCase, order `createdAt DESC`, tanpa pagination (pola sama isuaspirasi/tokoh).
- **`POST /pengumuman`** — admin/adminsekret only. Body `{judul,isi,prioritas?,targetLevel,targetKabId?,targetKecId?,targetKelId?}`. **Validasi hierarki target di service** (bukan cuma DTO): `targetLevel='semua'` → ketiga targetXxxId harus kosong; `'kabupaten'` → `targetKabId` wajib, `targetKecId`/`targetKelId` harus kosong; `'kecamatan'` → `targetKabId`+`targetKecId` wajib, `targetKelId` harus kosong; `'desa'` → ketiganya wajib. `userId` (pembuat) TIDAK dikirim di body, diambil dari JWT via `@User()`.
- **`DELETE /pengumuman/:id`** — admin/adminsekret only.

**Status wiring mobile (2026-08-26):** `types/pengumuman.ts` + `services/pengumuman.ts` (mock+real, pola sama `tokoh.ts`) + `hooks/usePengumuman.ts` (`usePengumumanList`/`useCreatePengumuman`/`useDeletePengumuman`) **SUDAH ADA**. **UI (screen tampilan untuk relawan, form buat pengumuman untuk admin) BELUM dibangun** — tidak ada referensi desain untuk fitur ini di `context/designs/` (shortcut "Broadcast" di Home selama ini cuma `Alert` placeholder), menunggu jawaban user (screenshot/canvas, atau izin eksplisit build dari `ui-rules.md`+`ui-tokens.md` saja — Aturan #1 CLAUDE.md, tidak ada pengecualian di repo ini). `npx tsc --noEmit` bersih untuk 3 file baru ini.

---

## Query Patterns

- Selalu handle error (lihat contoh di `code-standards.md` bagian Services)
- Jangan `select` semua field kalau API mendukung field selection — kalau tidak, terima payload penuh dari backend dan biarkan
