# Progress Tracker

Update file ini setiap selesai satu feature. Claude Code yang membaca file ini harus langsung tahu: apa yang sudah selesai, apa yang sedang dikerjakan, apa yang berikutnya.

---

## Status Saat Ini

**Phase:** 2/3 campuran — lihat detail per item
**Terakhir selesai (checklist):** 09 Hasil Rekap — **sebagian: tipe DPR RI 2024 SELESAI & WIRED KE BACKEND REAL** (Dapil→Kabupaten→Kecamatan→Kelurahan, `EXPO_PUBLIC_USE_MOCK_API=false`, lihat Decisions 2026-08-23 & `api-standards.md` § Hasil Rekap). Sebelumnya: 11 Kekuatan Pemilih (2026-08-22) — skor per-pemilih (distribusi, ranking kelurahan, top 10 pemilih), di-push dari Program → Door To Door, lihat Decisions untuk 2 keputusan yang dikonfirmasi user. Di luar checklist, SEMUA dikonfirmasi user bekerja di device asli: `HomeScreen` dapat progress bar DPT (data demo) + section "Aktivitas Terbaru" + polish visual (card putih, hapus "JSI", gradient biru halus pakai `expo-linear-gradient`), `CustomerServiceScreen` baru (FAQ statis, bukan chatbot), **Target Suara** (drill-down Kabupaten→Kecamatan→Kelurahan→TPS + laporan/analitik agregat), **Saksi TPS + Real Count C1** (Saksi nested di Target Suara TPS; Real Count JADI FITUR TERSENDIRI dengan drill-down wilayahnya sendiri), **3 fitur saran konsultan politik** (Rival Internal Separtai — CRM rival caleg + assessment wilayah; Prioritas Follow-up Pemilih Swing — list kategori "Belum Menentukan" + tombol Hubungi fungsional; Ringkasan Data/DtdoorAnalyticsScreen — distribusi kategori/gender/program bantuan; **Budgeting Kampanye** — dashboard anggaran + form "Catat Pengeluaran", referensi desain ditarik via `DesignSync` dari project Claude Design user, lihat Decisions), dan redesign grid icon 4-kolom untuk tombol-tombol analitik di tab Door To Door (ganti tumpukan `Button` full-width). Bug lintas-app "TextInput tidak center vertikal di Android" juga sudah di-fix (lihat Decisions & `ui-registry.md` § Input untuk pola fix wajib di TextInput baru).
**Berikutnya:** 08 DPT — **CRUD dasar (Tambah/Edit/Hapus) SELESAI**, dan **"Tandai ikut Door To Door" (Form Door To Door terintegrasi DPT) SELESAI diimplementasi (2026-08-22, mock data, belum dikonfirmasi user bekerja di device)** — lihat Decisions & `ui-registry.md` § DptVoterActionSheet/DtdoorFormScreen. "Tandai ikut Social Event" (baris ke-2 di canvas yang sama) **masih di-skip** — user cuma minta Door To Door kali ini. Base URL/path/query param 5 endpoint GET DPT juga masih belum dikonfirmasi (backend production masih diblokir Cloudflare, lihat Decisions sebelumnya). **Tokoh Masyarakat (di luar checklist Phase 1-3, permintaan user langsung) — SELESAI penuh**: summary/filter + "Daftar Tokoh" (`TokohMasyarakatScreen`, ref `tokoh1.png`+`tokohlist.png`) + alur "Identifikasi Tokoh Baru" (form baru `TokohFormScreen`, bisa standalone atau dari icon bintang di `DptCard`) — lihat Decisions & `build-plan.md` § Fase Berikutnya. **Quick Count (fitur baru lain, di luar checklist Phase 1-3) — SELESAI diimplementasi** (`QuickCountScreen`, ref artboard "12 · SAKSI & QUICK COUNT" — cuma bagian Quick Count yang dibangun, manajemen Saksi di artboard yang sama diabaikan sesuai instruksi eksplisit user), shortcut baru di Akses Cepat Home. **Verifikasi Kunjungan / Anti-Fraud (fitur baru lain) — SELESAI diimplementasi, SENGAJA read-only** (`AntiFraudScreen`, ref artboard "14 · VERIFIKASI KUNJUNGAN" — permintaan eksplisit user "generate UI-nya saja dulu, nanti saya buatkan API-nya", tidak ada mutation approve/reject), shortcut baru "Anti-Fraud" di Akses Cepat Home. **Isu & Aspirasi Warga (fitur baru lain, di luar checklist Phase 1-3) — SELESAI diimplementasi, SENGAJA read-only** (`IsuAspirasiScreen`, ref artboard "15 · ISU & ASPIRASI WARGA", dibaca via `DesignSync` — permintaan eksplisit user "Generate UI nya saja dulu tidak apa apa, nanti saya buatkan API nya", tidak ada mutation), shortcut baru "Isu & Aspirasi" di Akses Cepat Home — `npx tsc --noEmit` bersih, **belum dikonfirmasi user bekerja di device asli**. **09 Hasil Rekap — tipe DPR RI 2024 SELESAI** (`RekapStack`, menggantikan `RekapPlaceholderScreen` di tab "Rekap"), lihat Decisions untuk detail lengkap — **DPRD Provinsi & DPRD Kabupaten (2024) BELUM dikerjakan** (chip-nya `Alert` "Segera hadir"), begitu juga tahun 2019 untuk ketiga tipe (dihapus dari toggle, bukan cuma di-disable). "Biaya per suara" (tujuan akhir Budgeting Kampanye) masih tertunda — butuh data Target Suara/Real Count lengkap dulu.

---

## Progress

### Phase 1 — Foundation
- [x] 01 Setup Project
- [x] 02 Auth — Login dengan GPS
- [x] 03 Profile

### Phase 2 — Modul dengan Backend Terkonfirmasi
- [x] 04 Program Pemenangan — Door To Door
- [x] 05 Program Pemenangan — Social Event / GOTV
- [x] 06 Timses
- [x] 07 Lacak Relawan / Tracking

### Phase 3 — Modul dengan Backend Belum Terkonfirmasi
- [ ] 08 DPT — **sebagian**: 3 screen (Provinsi→Kabupaten→List) + filter+search (server-side) + CRUD dasar + "Tandai ikut Door To Door" **WIRED ke backend real 2026-08-23** (`https://api.demokrasikreatif.org`, bukan mock lagi — lihat Decisions untuk 3 landmine backend & belum diverifikasi di device). "Tandai ikut Social Event" belum dikerjakan.
- [ ] 09 Hasil Rekap — **sebagian**: tipe DPR RI 2024 selesai & **WIRED ke backend real** (`https://api.demokrasikreatif.org`, bukan mock lagi). DPRD Provinsi/DPRD Kabupaten 2024 & seluruh tahun 2019 belum dikerjakan (kontraknya sudah didokumentasikan di `api-standards.md` untuk sesi berikutnya).
- [x] 10 Kekuatan Wilayah — skor client-side dari `kategoriId` dtdoor (lihat Decisions), di-push dari Program → Door To Door
- [x] 11 Kekuatan Pemilih — skor per-pemilih, reuse model skor Kekuatan Wilayah, di-push dari Program → Door To Door (lihat Decisions)

---

## Keputusan Selama Build (Decisions)

_Catat keputusan penting di sini saat dibuat. Format: tanggal — keputusan — alasan._

- 2026-08-21 — Repo `jsi-mobile` dibuat sebagai kanal mobile tambahan untuk dashboard kampanye JSI, meniru struktur claude-kit (`CLAUDE.md` + `context/` + `.claude/commands/`) dari repo lain milik user (`temutani-mobile`) — permintaan eksplisit user.
- 2026-08-21 — Backend: NestJS API di `/Users/asdarsaid/JSI/api` sudah punya modul auth/dtdoor/gotv/ruangpublik/timses. Modul DPT dan Hasil Rekap **belum** punya repo yang dibagikan user — jangan implementasi mendalam untuk keduanya sebelum path repo-nya diberikan (lihat `CLAUDE.md` Aturan #6, `api-standards.md`).
- 2026-08-21 — Referensi desain: **tidak ada** izin standing untuk build UI tanpa referensi visual di repo ini (beda dari `temutani-mobile` yang punya izin ini) — user akan menaruh file desain dari Claude Design canvas ke `context/designs/` secara bertahap. `ui-tokens.md` saat ini cuma placeholder dari `client/src/css/tailwind.config.js` untuk keperluan scaffolding, bukan token final.
- 2026-08-21 — Scope sesi scaffold ini dibatasi ke: Expo project jalan (navigasi kosong, axios client, token storage) + seluruh dokumen claude-kit. Login dan screen lain sengaja **tidak** dibangun sesi ini (keputusan eksplisit user) — dikerjakan sesi berikutnya via `/new-feature`.
- 2026-08-21 — `babel-preset-expo` harus di-install manual (tidak otomatis ikut template `blank-typescript` SDK 57) — tanpa ini Metro gagal transform. Lihat `library-docs.md`.
- 2026-08-21 — Ditambahkan `declare module "*.css";` di `nativewind-env.d.ts` untuk mengatasi TS2882 pada `import "./global.css"` di `App.tsx` (versi NativeWind terpasang tidak mendeklarasikan ambient module ini). Lihat `library-docs.md`.
- 2026-08-21 — 8 dari 9 screen MVP sudah punya referensi desain nyata (export manual dari Claude Design canvas "JSI Mobile Dashboard.dc.html" milik user) di `context/designs/`: `login.png`, `home-dashboard.png`, `dpt.png`, `hasil-rekap.png`, `timses.png`, `program-pemenangan.png`, `lacak-relawan.png`, `profile.png`. Sekilas dari `login.png`: header gelap yang dipakai desain nyata dekat dengan token placeholder `primary`/`background` yang sudah ada — tetap **audit penuh terhadap `ui-tokens.md` wajib dilakukan** saat `/build-ui` jalan pertama kali (Step 3 di `ui-workflow.md`), jangan asumsikan token placeholder sudah 100% cocok.
- 2026-08-21 — **WhatsApp Broadcast dikeluarkan dari scope MVP** (bukan "menunggu desain") karena tidak ada file desain untuk screen ini — dipindah ke luar MVP di `project-overview.md`/`build-plan.md`. Item `10 WhatsApp Broadcast` dihapus dari checklist Phase 3 di atas.
- 2026-08-21 — **Kontrak `POST /auth/login` di `api-standards.md` (hasil observasi web) TERBUKTI SALAH** setelah dibaca langsung dari `/Users/asdarsaid/JSI/api/src/auth`. User dikonfirmasi via pertanyaan eksplisit: repo NestJS ini memang backend live di `https://api.demokrasikreatif.org`. Detail koreksi lengkap ada di `api-standards.md` Auth Flow — ringkasnya: base URL butuh suffix `/api` (global prefix NestJS), body field `nik` bukan `username`, response flat snake_case (`payload.acces_token`) bukan `data.data.*`, field `location` dikirim tapi diabaikan backend (GPS tetap wajib secara UX, gate client-side saja), tidak ada endpoint profile terpisah, dan endpoint ini **selalu balas HTTP 500** untuk kegagalan apapun (termasuk password salah) — tidak bisa dibedakan dari status code, hanya dari `message` di body.
- 2026-08-21 — `.env` (real, tidak dicommit) diisi `EXPO_PUBLIC_API_URL=https://api.demokrasikreatif.org/api` — URL live diberikan user langsung, bukan hasil asumsi.
- 2026-08-21 — Audit token warna pertama (`ui-tokens.md`/`tailwind.config.js`) terhadap `login.png` via pixel-sampling (`python3`+PIL): `background` diubah `#ffffff` → `#f8fafc` (screen bg sedikit off-white, beda dari `surface` yang tetap putih murni untuk input/card). 3 token baru ditambah: `surface-inverse:#0f172a` (header/hero gelap login — beda dari `primary:#1e293b` yang dipakai tombol), `text-inverse:#ffffff`, `text-inverse-muted:#94a3b8`. Token lain (`accent:#3b82f6`, `warning-soft`, `success-soft`) cocok **persis** dengan sample piksel desain nyata — placeholder web-seeded ternyata sudah akurat untuk token-token itu.
- 2026-08-21 — `Button` primitive (`src/components/ui/Button.tsx`) dapat varian ke-3 `brand` (`bg-primary`, dark navy) khusus CTA full-width auth/hero, karena tombol "Masuk" di `login.png` ternyata gelap bukan biru `accent` seperti spec generik di `ui-rules.md`. Varian `primary`/`secondary` existing TIDAK diubah (baru 1 data point dari 1 screen) — audit ulang kalau desain screen lain menunjukkan pola CTA full-width yang beda.
- 2026-08-21 — `@expo/vector-icons` ternyata **tidak** otomatis terinstall di scaffold ini meski `ui-rules.md` bilang "bundled Expo, zero install" — sempat bikin `tsc --noEmit` error `TS2307`. Sudah di-install manual (`npx expo install @expo/vector-icons`, masuk `package.json`). Dokumen sudah dikoreksi.
- 2026-08-21 — 3 pill status ("Meminta izin"/"Diizinkan"/"Ditolak") di `login.png` diinterpretasikan sebagai **legend desain** yang menunjukkan 3 kemungkinan state dari SATU banner GPS yang sama (bukan 3 element yang tampil bersamaan di screen final) — pola umum di canvas desain untuk mendokumentasikan varian warna. Diimplementasikan sebagai `GpsStatusBanner` dinamis 1 instance, warna/teks berubah sesuai state asli dari `expo-location`. Kalau asumsi ini salah, mudah dikoreksi (lihat `src/components/auth/GpsStatusBanner.tsx`).
- 2026-08-21 — "Ingat saya" (checkbox) dan "Lupa password?" (link) muncul di `login.png` tapi **tidak ada di scope build-plan Feature 02 manapun** (tidak juga di fase manapun untuk forgot-password). Keputusan: render sesuai visual desain (Aturan #1) tapi non-fungsional — checkbox murni toggle UI lokal (tidak gating logic apapun, sesi tetap selalu persist sesuai kriteria verifikasi build-plan), link "Lupa password?" memicu `Alert.alert` "Segera hadir" saat ditekan (bukan navigasi ke screen yang belum ada). Bukan izin permanen — kalau feature forgot-password masuk build-plan nanti, ini perlu diganti jadi fungsional.
- 2026-08-21 — Field "Username" di desain (placeholder "yayat.hidayat") dikirim ke backend sebagai `nik` sesuai kontrak asli (lihat poin di atas) — label & placeholder visual ikut desain apa adanya, key JSON request ikut backend. Tidak divalidasi numeric-only karena desain menyiratkan format username bebas, bukan NIK 16 digit murni seperti di web.
- 2026-08-21 — **Backend production (`https://api.demokrasikreatif.org`) diblokir Cloudflare Managed Challenge** untuk seluruh domain (dites `GET /api` dan `POST /api/auth/login`, keduanya balas HTTP 403 + `cf-mitigated: challenge` + halaman HTML "Just a moment...", bukan JSON dari NestJS) — client non-browser (axios/curl) tidak bisa menembus, ini kenapa login gagal terus meski NIK/password benar. Masalah infrastruktur Cloudflare, di luar scope repo ini. Atas keputusan user: **Mock Mode diaktifkan** (`EXPO_PUBLIC_USE_MOCK_API=true` di `.env`) — `loginRequest` (`src/services/auth.ts`) sekarang punya cabang mock dengan 2 akun demo (`admin.jsi`/`admin123` → admin, `yayat.hidayat`/`timses123` → timses), hint akun demo tampil di `LoginScreen` hanya saat mock aktif. Pola & alasan lengkap didokumentasikan di `api-standards.md` § Mock Mode — **modul berikutnya (dtdoor, gotv, timses, dst.) wajib ikut pola cabang-mock yang sama selama flag ini `true`**. Matikan flag begitu Cloudflare dibuka aksesnya.
- 2026-08-22 — **Feature 03 (Profile) — 2 gap ditemukan antara `profile.png` dan implementasi nyata, dikonfirmasi ke user via pertanyaan eksplisit sebelum build:**
  1. Export `profile.png` berhenti tepat di tab bar (tidak menunjukkan tombol Simpan/Logout) — kemungkinan konten di bawah scroll terpotong saat export dari canvas. User memilih: tombol **"Simpan Perubahan"** ditambahkan di akhir grup field editable, dan tombol **"Keluar" (Logout)** ditambahkan di bawah info card, sebelum tab bar — keduanya elemen baru yang tidak ada di file desain, posisinya hasil keputusan user bukan observasi visual.
  2. Field **"Email"** di desain tidak punya kolom di backend (`Timse` entity di `/Users/asdarsaid/JSI/api/src/timses/entities/timse.entity.ts` tidak punya `email` sama sekali, juga tidak ada di payload login — lihat `api-standards.md` Auth Flow poin 6). User memilih: field tetap tampil sesuai desain tapi **`editable={false}`** (bg `surface-secondary`, placeholder "Belum didukung backend"), tidak dikirim ke backend sama sekali — supaya tidak menjanjikan penyimpanan yang sebenarnya tidak terjadi.
- 2026-08-22 — **Data profil (No. HP, Wilayah Tugas/kecamatan) tidak ada di payload login** (`payload: { id, nik, nama_lengkap, roles }` saja, lihat Auth Flow poin 4/6 `api-standards.md`) — ProfileScreen fetch data lengkap terpisah lewat `GET /timses/:id` (endpoint sudah ada & dipakai, module `timses` punya repo backend terkonfirmasi). Ditemukan juga: `TimsesService.findOne` (backend) balas entity utuh **termasuk hash `password`** — tidak dipetakan/ditampilkan ke UI (lihat `mapProfile` di `src/services/profile.ts`), murni observasi, bukan sesuatu yang diperbaiki di repo ini (backend bukan scope mobile). `TimsesService.update` (dipanggil lewat `PATCH /timses/:id`) balas `UpdateResult` TypeORM, bukan record ter-update — `updateProfile` fetch ulang via GET setelah PATCH sukses agar dapat data akurat.
- 2026-08-22 — **Toggle "Notifikasi" di ProfileScreen sengaja tidak dikirim ke backend** (local state murni) — tidak ada kolom preferensi notifikasi di entity `timses`, dan fitur Push Notification eksplisit ada di `build-plan.md` § Fase Berikutnya (di luar MVP). Toggle "Lokasi GPS Aktif" selalu `disabled`, nilainya cermin status asli dari `useLocationPermission()` (bukan preference tersendiri) — konsisten dengan pola GPS gate client-side yang sudah ditetapkan di Feature 02.
- 2026-08-22 — Mock data profil (`src/services/profile.ts` § `MOCK_PROFILES`) ditambahkan untuk 2 akun demo yang sama dengan `MOCK_ACCOUNTS` di `services/auth.ts` (id 1 = admin.jsi, id 2 = yayat.hidayat/Kec. Cileunyi) — konsisten dengan kewajiban pola mock-mode di `api-standards.md` § Mock Mode untuk service baru selama `EXPO_PUBLIC_USE_MOCK_API=true`.
- 2026-08-22 — **Feature 05 (Program Pemenangan — Social Event/GOTV) — reuse shell tab "Social Event" dari `ProgramPemenanganScreen` (sebelumnya placeholder inert dari Feature 04, sekarang fully implemented).** Beda dari Feature 04, tab ini punya referensi visual langsung 1:1 di `program-pemenangan.png` (screenshot desain aktif di tab ini), tapi entity backend `Gotv` (`/Users/asdarsaid/JSI/api/src/gotv`) ternyata jauh lebih sederhana dan punya gap sendiri terhadap desain — diselesaikan dengan judgment call yang konsisten dengan preseden Feature 04 (skip field tanpa data backing, dokumentasikan), TIDAK ditanyakan ulang ke user karena kategori pertanyaannya sama persis dengan yang sudah dijawab untuk tab sebelah:
  1. **Badge status "Selesai"/"Terjadwal"** di desain — relasi `status` (`StatusGoTv`) ada di file entity tapi **di-comment-out**, bukan kolom aktif. Sengaja tidak ditampilkan di `GotvCard`, sama pola dengan field `status` yang di-skip di Feature 04.
  2. **"82 peserta"** di desain — tidak ada kolom "jumlah peserta" terpisah, dipakai ulang `jumlah_wajib_pilih` (satu-satunya field angka orang di entity) sebagai proxy.
  3. **Progress bar "45%"** — sama seperti Feature 04, tidak ada sumber data target/kuota untuk gotv, diganti hitungan sederhana "{count} kegiatan tercatat" dari `GET /gotv/count`.
  - **⚠️ Bug backend berbeda dari dtdoor, sama akibat:** `POST /gotv` juga lookup by `idDpt` sebelum insert, TAPI kalau ketemu match, langsung `throw` error "DPT telah di input sebelunya" (bukan diam-diam update seperti dtdoor). Karena `idDpt` kosong bikin lookup match row apapun yang sudah ada, create kedua dst. akan selalu gagal kalau tidak dikirim `idDpt` unik. Diakali dengan util bersama `generateSyntheticIdDpt()` yang di-extract ke `src/lib/api/syntheticId.ts` (dipakai `dtdoor.ts` & `gotv.ts`).
  - 7 field wajib di form (`nama_kegiatan`, `tps`, `desa`, `kecamatan`, `kabupaten`, `nama_lengkap`, `jumlah_wajib_pilih` — semua `NOT NULL` di entity `Gotv`) jauh lebih banyak dari dtdoor (cuma 2 wajib) — form ini murni `Input`, tidak butuh `Select` sama sekali karena tidak ada field kategori/relasi aktif di entity.
- 2026-08-22 — **Feature 04 (Program Pemenangan — Door To Door) — 3 gap ditemukan antara desain/ekspektasi fitur dan kenyataan backend `dtdoor`, dikonfirmasi ke user sebelum build:**
  1. **Tidak ada linkage DPT.** Web client selalu mengikat kunjungan ke record DPT (pilih dari daftar DPT), tapi DPT belum ada repo backend di sesi ini. Entity `Dtdoor` sendiri tidak mewajibkan linkage ini (`idDpt` cuma string opsional). User memilih: **bangun standalone** — form input pakai field ketik manual, `idDpt` diisi otomatis dengan string sintetis unik (lihat poin bug di bawah). Kunjungan ber-linkage-DPT jadi fitur terpisah nanti setelah repo DPT dibagikan.
  2. **Tidak ada endpoint lookup untuk `kategori`/`status`.** User memberi nilai valid via screenshot untuk **Kategori** (7 nilai: Simpatisan, Simpatisan Aktif, Relawan, Saksi, Tim Lain, Pemilih Kompetitor, Belum Menentukan) dan **Program Bantuan** (8 nilai: Kesehatan, Olahraga, Pendidikan, Pertanian, Rutilahu, Sembako, UMKM, Lainnya — field string bebas di entity, BUKAN relasi ke `BantuanDtDoor` yang ternyata tidak pernah dipakai `Dtdoor`). **Field `status` (relasi `StatusDtDoor`) sengaja TIDAK ada di form** — dikonfirmasi user: nilai ini berasal dari relasi DPT↔dtdoor ("Sudah Dikunjungi" begitu DPT ke-link), bukan sesuatu yang di-set fitur standalone ini.
  3. **Tidak ada sumber data progress/kuota** untuk bar persentase ("45%") yang tampil di desain — itu punya tab Social Event, tidak ada padanan target/kuota untuk dtdoor di backend maupun web. Diganti hitungan sederhana "{count} kunjungan tercatat" dari `GET /dtdoor/count`.
  - **⚠️ Bug backend penting yang diakali:** `POST /dtdoor` (`/Users/asdarsaid/JSI/api/src/dtdoor/dtdoor.controller.ts`) melakukan lookup upsert-style berdasar `{ idDpt, kabId }` SEBELUM insert — kalau `idDpt` kosong, lookup ini berisiko match row ARBITRARY yang sudah ada dan silently overwrite lewat `update()` (yang juga cuma balas `UpdateResult` mentah, bukan entity). Diakali dengan generate `idDpt` sintetis unik tiap submit (`generateSyntheticIdDpt()` di `src/services/dtdoor.ts`) supaya cabang `create()` yang selalu dieksekusi.
  - **⚠️ Asumsi belum diverifikasi:** id numerik untuk 7 nilai Kategori di atas diasumsikan berurutan (Simpatisan=1 … Belum Menentukan=7) mengikuti urutan screenshot user — TIDAK ada endpoint untuk konfirmasi ke tabel `kategori_dt_door` asli. Bug ini silent dari sisi mobile (list render label dari daftar lokal berdasar id yang dipilih user, bukan hasil round-trip server) — **wajib diverifikasi ke DB sebelum production**, lihat `src/types/dtdoor.ts`.
  - Modul `dtdoor` (semua endpoint) **tidak punya auth guard sama sekali** (guard global-nya no-op) — backend gap, di luar scope repo ini untuk diperbaiki, dicatat di `api-standards.md`.
- 2026-08-22 — **Feature 04 dikonfirmasi bekerja oleh user setelah tes di device.** 1 revisi kecil diminta: heading in-content (`"Profil Saya"`, `"Program Pemenangan"`) terlalu jauh dari safe area atas dengan `pt-lg` (48px) — diubah ke `pt-sm` (12px) di `ProfileScreen.tsx` & `ProgramPemenanganScreen.tsx`. Disimpan sebagai preferensi standing (bukan cuma 2 screen ini) — lihat memory `feedback_screen_heading_spacing`: screen baru dengan heading in-content (`SafeAreaView edges={["top"]}`, `headerShown:false`) default ke `pt-sm`, bukan `pt-lg`.
- 2026-08-22 — Feature 04 memperkenalkan pattern `useInfiniteQuery` (TanStack Query) pertama di codebase ini (`src/hooks/useDtdoorList.ts`, `FlatList` + `onEndReached`) dan primitive `Select` pertama (`src/components/ui/Select.tsx`, hand-rolled pakai RN `Modal` bawaan, sengaja tidak menambah dependency baru seperti `@react-native-picker/picker`). Folder baru: `src/screens/program/`, `src/components/dtdoor/` — mengikuti pola folder-per-fitur di `architecture.md`.
- 2026-08-22 — **Feature 06 (Timses) — 2 gap backend ditemukan, dikonfirmasi ke user via pertanyaan eksplisit sebelum build:**
  1. **Status online "realtime" backend-nya tidak fungsional.** `AuthService` (`/Users/asdarsaid/JSI/api/src/auth/auth.service.ts`) di-decorate `@WebSocketGateway` tapi tidak punya `@SubscribeMessage`/`server.emit` apapun — gateway kosong, cuma nerima koneksi tanpa pernah mengirim event. Kolom `status_online` di entity `Timse` cuma di-set `'online'` sekali saat login (`AuthService.validateUser`), **tidak pernah** direset ke `'offline'` di manapun di seluruh repo backend (grep `status_online`/`handleDisconnect`/`OnGatewayDisconnect`/`@SubscribeMessage` nihil selain titik set itu) — begitu seseorang pernah login, dia permanen tercatat "online". User memilih: **snapshot, bukan realtime** — tidak connect `socket.io-client` sama sekali untuk fitur ini, list di-refresh via `useFocusEffect` (refetch tiap screen focus) + `RefreshControl` pull-to-refresh.
  2. **Tidak ada kolom `kabupaten` di entity `Timse`** (`/Users/asdarsaid/JSI/api/src/timses/entities/timse.entity.ts`) — cuma `kecamatan`, `desa`, `dusun`. Breadcrumb 3-level "Kab. Bandung Barat › Kec. Cileunyi › Semua Desa" di `timses.png` level pertamanya tidak ada data pendukung. User memilih: **skip level Kabupaten** — breadcrumb & drill-down cuma 2 level (Kecamatan → Desa), lihat `TimsesBreadcrumb` di `ui-registry.md`.
  - **Temuan tambahan (tidak perlu ditanya, kategori sama dengan gap di atas):** endpoint `GET /timses/timseskabupaten`/`timseskecamatan`/`timsesdesa` ternyata filter berdasar ROLE (`relawankabupaten` dst., lihat `TimsesService.findyByTimsesKabupaten` dst.), BUKAN filter "anggota di wilayah X" seperti nama endpoint-nya menyiratkan. Hierarki Kecamatan→Desa dikelompokkan di client dari `GET /timses` (`findAll`, full list) — tidak ada endpoint filter hierarkis by nilai wilayah sama sekali.
  - Role admin/adminsekret bisa browsing bebas semua kecamatan; role lain dibatasi ke kecamatan sendiri (dari `GET /timses/:id` via `useProfile()`, reuse hook Feature 03) — client-side only (backend `TimsesController` cuma di-guard `JwtAuthGuard`, tidak ada role/wilayah guard), konsisten dengan pola `AccessScopeNotice` di `ProfileScreen`.
  - Tombol "+" di desain (`timses.png`) sengaja **non-fungsional** (`Alert.alert` "Segera hadir") — build-plan Feature 06 cuma scope "Lihat hierarki & status online", tidak ada "tambah anggota". Sama pola deviasi dengan "Lupa password?" di `LoginScreen` (Feature 02).
  - Audit token warna `timses.png` (pixel-sampling): kartu ringkasan gelap = `primary` #1e293b (exact match), teks/dot online = `success` #16a34a (exact match), tombol "+" = `accent` #3b82f6 (exact match). 2 deviasi kecil (pola sama seperti badge di `profile.png`, TIDAK ditambah token baru): avatar member `#eff6ff` vs token `accent-soft` #dbeafe, dot/teks offline `#94a3b8` vs token `text-muted` #64748b.
- 2026-08-22 — **Feature 07 (Lacak Relawan / Tracking) — temuan besar sebelum build, dikonfirmasi ke user via 2 pertanyaan eksplisit:**
  1. **Field `lat`/`long` di entity `Timse` (`/Users/asdarsaid/JSI/api/src/timses/entities/timse.entity.ts`) tidak pernah ditulis di manapun** — bukan cuma backend, web `client` juga tidak pernah menulisnya (`FormInputTimses.jsx`/`FormEditTimses.jsx` tidak punya field lat/long; `Tracking.jsx` cuma MEMBACA `GET /timses/online` lalu plot `item.lat`/`item.long` ke Leaflet). Artinya fitur tracking di web pun kemungkinan besar selalu tampil peta kosong di produksi. User memilih opsi **"Mobile kirim GPS sendiri"**: app kirim lokasi device sendiri (`expo-location`, permission sudah granted sejak login wajib GPS di Feature 02) ke `PATCH /timses/:id` secara berkala — scope tambahan di luar teks literal build-plan ("Map view + list status"), diizinkan eksplisit oleh user. Diimplementasikan sebagai `useLocationBeacon.ts` (interval 30 detik, guard `AppState==="active"`, silent-fail) dipasang SEKALI di `RootNavigator.tsx` (bukan di dalam `LacakRelawanScreen` atau `AdminTabs`/`TimsesTabs`) — supaya jalan untuk SEMUA role yang login, bukan cuma admin, karena admin cuma yang MELIHAT peta, timses/relawan* lapangan yang jadi TITIK di peta itu.
  2. **Tidak ada `react-native-maps`/native map setup di project ini** — belum ada `expo-dev-client` atau plugin native map apapun di `app.json`, cuma jalur Expo Go biasa. Menambah `react-native-maps` untuk map asli (Apple/Google Maps) butuh custom dev client (EAS Build) + API key Google Maps untuk Android — perubahan tooling besar di luar scope satu feature. User memilih **WebView + Leaflet/OpenStreetMap** (`react-native-webview`, HTML internal di `TrackingMapView.tsx`) — persis library yang dipakai web `client` (`react-leaflet`, versi Leaflet disamakan 1.9.4), jalan penuh di Expo Go tanpa API key/native rebuild.
  - **Tab "Lacak Relawan" cuma ditambah ke `AdminTabs.tsx`, TIDAK ke `TimsesTabs.tsx`** — bukan pertanyaan terpisah ke user, diturunkan langsung dari observasi kode web (`client/src/pages/tracking/Tracking.jsx` hard-redirect `if (roles !== "admin" && roles !== "adminsekret") navigate("/")`), konsisten dengan prinsip "ikuti aturan bisnis web" di CLAUDE.md.
  - **Realtime socket.io TIDAK dipakai** — root cause identik dengan temuan Feature 06 (`AuthService` gateway kosong, tidak pernah `emit`), kategori pertanyaan yang sama persis dengan yang sudah dijawab user di Feature 06 → **tidak ditanyakan ulang** (pola sama seperti keputusan mandiri di Feature 05). Snapshot via `useFocusEffect` refetch tiap fokus screen + pull-to-refresh, pill "Real-time" di `lacak-relawan.png` di-relabel jujur jadi "Update berkala" (visual pill dipertahankan — nambah prop `dot` ke `Badge.tsx` yang sudah ada, bukan component baru).
  - Reuse penuh `useTimsesList`/`fetchTimsesList` (Feature 06) untuk data roster — cuma extend `TimsesMember` dengan `lat`/`long: number | null` dan tambah `updateOwnLocation()` di `services/timses.ts`, tidak bikin service/hook list baru.
  - Mock data: `MOCK_TIMSES` diubah jadi mutable (`mockTimsesList`, pola sama `mockDtdoorList`/`mockGotvList`) supaya `updateOwnLocation` mock terlihat efeknya; cuma 3/8 anggota diberi `lat`/`long` demo (representasi realistis — mayoritas belum pernah kirim lokasi). Kuirk pre-existing: id `MOCK_TIMSES` (101-108) tidak match id akun demo login (`admin.jsi`=1, `yayat.hidayat`=2) — beacon akun demo tidak akan terlihat memperbarui roster mock, di luar scope diperbaiki di sini.
- 2026-08-21 — **Expo SDK didowngrade dari 57 ke 54** atas permintaan eksplisit user, supaya sejajar dengan `temutani-mobile`. Semua package terkait ikut disesuaikan (`expo-font`, `expo-secure-store`, `expo-location`, `expo-splash-screen`, `expo-status-bar`, `react-native-safe-area-context`, `react-native-screens`, `react`, `react-native`, `@types/react`, `typescript`) — versi akhir cocok/dekat dengan `temutani-mobile` (lihat `library-docs.md`). Ditemukan &amp; diperbaiki gotcha baru selama proses: NativeWind's `react-native-css-interop` menarik `react-native-reanimated`/`react-native-worklets` versi nested yang beda dari root kalau `react-native-reanimated` tidak dideklarasikan langsung — menyebabkan `tsc` error massal `className does not exist` (module augmentation `react-native` tidak ke-merge). Fix: `react-native-reanimated` ditambahkan sebagai dependency langsung + `react-native-worklets/plugin` ditambahkan ke `babel.config.js` + full clean reinstall. Detail penuh gotcha ini ada di `library-docs.md` entry NativeWind &amp; Expo — **baca itu dulu** kalau ada masalah `className`/tipe RN yang mirip di masa depan.
- 2026-08-22 — **TEMUAN NAVIGASI BESAR (ditemukan saat mulai Feature 08, dikonfirmasi ke user via pertanyaan eksplisit):** semua desain yang punya tab bar (`profile.png`, `program-pemenangan.png`, `dpt.png`) ternyata **konsisten cuma punya 5 tab: Home/DPT/Rekap/Program/Profil** — TIDAK ada Timses maupun Lacak Relawan. Kedua screen itu (Feature 06/07) sempat salah diimplementasikan sebagai tab tersendiri di sesi sebelumnya (heading in-content, tanpa native header) — padahal `timses.png`/`lacak-relawan.png` sendiri punya tombol "‹ back" yang harusnya jadi sinyal "ini pushed screen, bukan tab root". Dikonfirmasi definitif oleh `home-dashboard.png` (baru dibuka sesi ini): grid "Akses Cepat" di situ punya 8 shortcut termasuk **TM (Timses)** dan **TR (Tracking)** — itu tempat mereka seharusnya berlabuh. User pilih **"Ikuti desain"**: direstrukturisasi lewat `HomeStack.tsx` baru (dipakai bersama Admin/TimsesTabs, pola sama `ProgramStack`) — `HomeScreen` (root, `headerShown:false`, heading custom) → `Timses`/`LacakRelawan` (pushed, native header title+back chevron menggantikan heading in-content lama). `AdminTabs.tsx`/`TimsesTabs.tsx` ditulis ulang total jadi 5 tab: Home(`HomeStack`)/DPT(`DptStack`, baru)/Rekap(placeholder, Feature 09 belum digarap)/Program(`ProgramStack`, tidak berubah)/Profil. Konsekuensi kode: `TimsesScreen.tsx` & `LacakRelawanScreen.tsx` di-edit (heading in-content dihapus, `SafeAreaView edges` dari `["top"]` → `["bottom"]`, tombol "+" Timses dipindah jadi `headerRight` via `navigation.setOptions()`). **Akses Lacak Relawan TETAP admin-only** (cuma dirender di grid Akses Cepat untuk role admin/adminsekret) — konsisten dengan keputusan Feature 07, cuma titik aksesnya yang pindah dari tab ke shortcut.
- 2026-08-22 — **Feature 08 (DPT) dimulai — user memberi contoh response asli lewat `contohdpt.ts`** (5 data: Provinsi, Kabupaten per-provinsi, Kecamatan per-kabupaten "hanya untuk filtering", Kelurahan per-kecamatan "hanya untuk filtering", list DPT per-kabupaten dengan objek `kab` scope + `meta.page`). User juga menjelaskan alur: **3 tahap** — pilih Provinsi → pilih Kabupaten → langsung tampil data DPT kabupaten itu, difilter lebih lanjut oleh Kecamatan/Kelurahan/TPS (bukan drill-down list terpisah, cuma filter di atas list yang sudah ke-load). 2 keputusan dikonfirmasi via pertanyaan eksplisit sebelum build:
  1. **Screen "List Provinsi" & "List Kabupaten" (tahap 1 & 2) belum ada desainnya** — user pilih **"saya siapkan desainnya dulu"**. Sebagai jembatan: KETIGA tahap digabung di **satu screen** (`DptListScreen.tsx`, referensi `dpt.png` yang sebenarnya screen tahap-3), tahap 1 & 2 pakai `TimsesRegionPickerModal` **reuse apa adanya tanpa modifikasi visual** (memenuhi syarat skip Step 1 `ui-workflow.md` karena primitive murni) — auto-terbuka berurutan saat screen fokus pertama kali. **Wajib direfactor jadi 2 screen asli + native header (back chevron sungguhan) begitu desainnya tersedia** — lihat catatan arsitektur sementara di `DptListScreen.tsx` & `ui-registry.md`.
  2. **Endpoint request (base URL/path/query param) untuk 5 data ini belum dikonfirmasi**, cuma bentuk response-nya — user pilih **"pakai mock dulu"**. `src/services/dpt.ts` full mock (data asli porting dari `contohdpt.ts`, bukan karangan), cabang API asli sengaja `throw Error` pesan jelas alih-alih menebak endpoint (Aturan #6). Mock cuma punya cakupan penuh 1 kabupaten (BANTAENG, wilId 7303, Prov. SULAWESI SELATAN 73 — satu-satunya yang datanya lengkap di `contohdpt.ts`); provinsi/kabupaten lain balas list kosong. 1 entry kelurahan sintetis ditambah (`wilId 7303011002 "BONTO LEBANG"`) supaya konsisten dengan `idKel` yang dipakai sample DPT (`contohdpt.ts` sendiri tidak menyertakan kelurahan itu di sample kelurahan-nya — inkonsistensi kecil antar-sample, diperbaiki demi demo end-to-end yang koheren).
  - **CRUD dasar & "tandai partisipasi program"** (bagian dari scope Feature 08 di `build-plan.md`) **BELUM diimplementasi** — tidak ada contoh request write dari user. Tombol "+ Tambah Data DPT", 2 ikon download, dan menu "..." per kartu DPT sengaja non-fungsional (`Alert` "Segera hadir"), field `dtdoor`/`gotv` per record dipetakan jadi flag boolean (`sudahDtdoor`/`sudahGotv`) karena responsenya selalu `null` di contoh yang ada.
  - Filtering & pagination DPT list saat ini **client-side** (fetch penuh 1 kabupaten, filter Kecamatan/Kelurahan/TPS/nama di memori) — cukup untuk mock, **wajib diganti server-side** begitu endpoint asli & kontrak query param dikonfirmasi (dataset asli per-kabupaten bisa >100rb baris).
  - `Badge.tsx` (Feature 02) dapat varian ke-5 `muted` (`bg-surface-secondary`/`text-text-muted`) untuk badge status "belum" (mis. "Belum D2D"/"Belum SE") — extend primitive existing, bukan component baru, pola sama dengan penambahan prop `dot` di Feature 07.
  - `HomeScreen` (baru, ganti `HomeScreen` stub placeholder dari Feature 01) **scope sengaja minimal** — user pilih **"Termasuk stat Program Pemenangan"**: welcome banner (role-aware) + 2 kartu statistik Door To Door/Social Event (reuse `useDtdoorCount`/`useGotvCount`, murah karena sudah ada dari Feature 04/05) + grid 8 shortcut Akses Cepat. Progress bar "Persentase Data DPT" di `home-dashboard.png` **di-skip** (belum ada agregat/scoping data pendukungnya), begitu juga hamburger menu & notifikasi (`Alert` "Segera hadir", belum ada fitur di baliknya).
- 2026-08-22 — **Grid "Akses Cepat" di HomeScreen diganti dari singkatan teks literal (desain asli: "DPT"/"RK"/"TM" dst.) jadi icon `Ionicons`** — permintaan eksplisit user ("carikan icon yang cocok menurutmu"), lebih konsisten dengan pola icon berwarna kontekstual di `ui-rules.md` yang sudah dipakai di semua tab bar/tombol lain. Pemetaan: DPT→`document-text`, Rekap→`bar-chart`, Timses→`people`, Program→`megaphone`, Tracking→`locate`, Broadcast→`paper-plane`, Survey→`clipboard`, Tokoh→`star`. `HomeQuickAccessItem` prop diubah dari `abbr: string` jadi `icon: ComponentProps<typeof Ionicons>["name"]`.
- 2026-08-22 — **User memberi `context/designs/dptlistdaerah.png`** ("DPT — Pilih Provinsi": search + list card nama/total pemilih/jumlah sub-wilayah, tab root, tab bar terlihat) — ini MENYELESAIKAN gap arsitektur sementara yang dicatat sebelumnya. Ditanya pendapat UX (modal vs list) — jawaban: **list**, karena (a) desain user sendiri sudah menunjukkan itu, (b) modal tidak scalable untuk puluhan provinsi/ratusan kabupaten, beda dari Kecamatan/Kelurahan/TPS yang datanya kecil (di-scope 1 kabupaten). **Refactor `DptStack.tsx`** dari 1 screen jadi 3: `DptProvinsiScreen` (root, ref `dptlistdaerah.png`) → `DptKabupatenScreen` (pushed, **tidak ada file desain sendiri** — pola visual diinferensi dari `dptlistdaerah.png` karena struktur data identik, didokumentasikan sebagai inferensi bukan observasi langsung, lihat `ui-registry.md`) → `DptListScreen` (pushed, ref `dpt.png`, sekarang terima `kabWilId` lewat route params, header native title+back chevron+headerRight download/filter menggantikan heading in-content). Chip Provinsi/Kabupaten di `DptListScreen` sekarang navigasi balik (`popToTop()`/`goBack()`) bukan buka modal lagi — chip Kecamatan/Kelurahan/TPS tetap modal (filter dalam satu kabupaten, bukan tahap navigasi).
  - **2 file desain baru ditemukan bersamaan** (`kekuatanwilayah.png` "Peta Kekuatan Wilayah", `kekuatanpemilih.png` "Skoring Kekuatan Pemilih") — analitik skor kekuatan pemilih/wilayah dari data Door To Door, **tidak ada di build-plan manapun** sebelumnya, field "skor" juga tidak ada di entity `Dtdoor` manapun yang terverifikasi. User pilih **"catat dulu, bukan sekarang"** — sudah ditambah ke `build-plan.md` § Fase Berikutnya, TIDAK dikerjakan sesi ini.
- 2026-08-22 — **Revisi tombol "Tambah Data DPT" di `DptListScreen`** (permintaan eksplisit user) — deviasi dari `dpt.png`: tombol besar di footer bawah diganti icon kecil (`Ionicons "add"`, `accent`) di `headerRight`, sejajar download/filter. Tombol download yang tadinya duplikat di footer bawah **dihapus** — footer bawah screen ini sekarang tidak ada sama sekali.
- 2026-08-22 — **2 bug ditemukan & diperbaiki di `DptListScreen` (laporan user):**
  1. **Filter Kelurahan/Desa & TPS tidak bisa dipakai sama sekali** — chip-nya cuma render begitu ada value terpilih, tapi tidak ada entry point untuk memilih pertama kali (beda dari Kecamatan yang bisa dibuka lewat ikon filter di header). Fix: komponen baru `DptAddFilterChip` (chip putus-putus "+ Kecamatan"/"+ Kelurahan/Desa"/"+ TPS") sebagai entry point — Kecamatan selalu tampil, Kelurahan/TPS baru muncul setelah Kecamatan dipilih (mengikuti scoping data).
  2. **"Background abu-abu bekas tombol Tambah Data DPT"** yang dilaporkan user — root cause sebenarnya BUKAN sisa footer lama (sudah bersih dihapus di revisi sebelumnya), melainkan `FlatList` yang tidak `flex-1` sehingga tidak stretch mengisi sisa layar, menyisakan gap `bg-background` kosong di bawah list pendek (7 data mock). Fix: tambah `className="flex-1"` ke `FlatList`.
- 2026-08-22 — **Strip abu-abu MASIH terlihat setelah fix `flex-1`** (laporan user, lebih spesifik: "diatas navbottom") — root cause SEBENARNYA: `SafeAreaView edges={["bottom"]}` di `DptListScreen` & `DptKabupatenScreen` menambah padding bottom untuk home indicator device, TAPI kedua screen ini di-push di dalam tab "DPT" yang tab bar-nya SUDAH menyediakan ruang aman bawah sendiri — `useSafeAreaInsets` mengukur dari tepi fisik device, tidak tahu ada tab bar sebagai sibling, jadi hasilnya padding DOBEL (terlihat sebagai strip `bg-background` antara akhir list dan tab bar). Fix: `edges={["bottom"]}` dihapus total dari kedua screen itu (jadi `<SafeAreaView className="flex-1 bg-background">` tanpa prop `edges`).
  - **⚠️ Bug laten yang sama kemungkinan ada di screen pushed-di-dalam-tab lain**: `TimsesScreen`/`LacakRelawanScreen` (HomeStack, Feature 06/07) dan `DtdoorFormScreen`/`GotvFormScreen` (ProgramStack, Feature 04/05) — semua masih pakai `edges={["bottom"]}` dan nested di dalam tab bar. **Belum diperbaiki** (di luar laporan user kali ini, sudah dianggap "selesai" di feature masing-masing) — audit kalau ada laporan visual serupa ("ada gap/strip di bawah") di screen-screen itu.
- 2026-08-22 — **Gap kosong sebelum search bar terlalu tinggi** (laporan user, `DptListScreen` "Cari nama pemilih" & `DptKabupatenScreen` "Cari kabupaten/kota" — 3 iterasi sebelum ketemu root cause sebenarnya):
  1. `pt-sm` (12px) → `pt-xs` (4px) — belum cukup.
  2. `pt-xs` dihapus total (prop `edges={["bottom"]}` juga dihapus jadi tanpa `edges` sama sekali) — user lapor **masih banyak kosong**, malah lebih parah dari sebelumnya.
  3. **Root cause ketemu**: `SafeAreaView` dari `react-native-safe-area-context` (cek langsung source-nya di `node_modules`, bukan asumsi) default ke **semua edge `'additive'`** kalau prop `edges` **tidak diisi sama sekali** (`undefined`) — BUKAN "tidak ada edge" seperti asumsi wajar. Waktu `edges={["bottom"]}` dihapus tanpa diganti `edges={[]}`, screen balik dapat inset top+bottom+left+right otomatis (termasuk top, padahal native header sudah handle top sendiri) — itu penyebab gap besar yang dilaporkan iterasi 2, BUKAN soal nilai `pt-*` sama sekali. Fix final: `edges={[]}` (array kosong, eksplisit) di `DptListScreen` & `DptKabupatenScreen` — bukan `edges={["bottom"]}`, bukan juga prop dihilangkan.
  4. **Setelah root cause beres, user minta padding-top tetap ada** (bukan 0) supaya konten tidak mepet ke native header — `pt-sm` (12px) dipasang kembali di kedua screen (`DptListScreen` & `DptKabupatenScreen`), sekarang jadi jarak yang wajar karena base inset-nya sudah benar (nol), bukan numpuk kayak sebelumnya.
  - **Pelajaran untuk screen native-header lain** (`TimsesScreen`, `LacakRelawanScreen`, `DtdoorFormScreen`, `GotvFormScreen` — masih pakai `edges={["bottom"]}`, lihat entry Decisions sebelumnya soal bug laten strip abu-abu): kalau nanti diperbaiki, pastikan pakai `edges={["bottom"]}`atau `edges={[]}` EKSPLISIT sesuai kebutuhan, JANGAN pernah hilangkan prop `edges` sama sekali dari `SafeAreaView`.
- 2026-08-22 — **`DptSummaryCards` "kurang enak dilihat"/"terlalu mepet" saat 3 kartu** (laporan user, kondisi filter Kecamatan→Kelurahan→TPS penuh) — angka/label DPT asli (`contohdpt.ts`) jauh lebih panjang dari contoh visual di `dpt.png` ("151.952"/"Kec. BISSAPPU" vs "18.150"/"Kec. Cileunyi" di desain), gampang wrap ke 2 baris atau kepotong di kolom 1/3 lebar. Fix: padding (`p-md`→`p-sm`) & ukuran font value (`text-headline-md`→`text-body-lg`) mengecil otomatis begitu `items.length >= 3`; value & label dikunci `numberOfLines={1}` (value tambahan `adjustsFontSizeToFit`, label `ellipsizeMode="tail"`) — tidak akan wrap 2 baris lagi berapa pun panjang datanya.
- 2026-08-22 — **UI filter Kecamatan/Kelurahan/TPS di `DptListScreen` melewati 3 iterasi sampai final** (semua permintaan eksplisit user, hari yang sama):
  1. Deretan chip per-level (`DptFilterChip` + `DptAddFilterChip`) — user bilang "kurang enak dilihat".
  2. Disederhanakan jadi 1 card icon-only kecil — TERNYATA beda dari yang user maksud, user kirim mockup screenshot untuk klarifikasi.
  3. **Final, sesuai mockup user:** 1 card putih full-width "Filter Wilayah Aktif" — icon chip (`bg-accent-soft`) + breadcrumb 1 baris gabungan Provinsi›Kabupaten›Kecamatan›Kelurahan›TPS (truncate `...`) + teks "Ubah" (`accent`) di kanan. Tap → modal Kecamatan ("modal interim"), cascade otomatis ke Kelurahan→TPS begitu pilih nilai spesifik (bukan "Semua ..."). Chip Provinsi/Kabupaten individual (yang bisa di-tap balik navigasi) ikut dihapus — breadcrumb sekarang murni informasi, ganti Provinsi/Kabupaten lewat back native header.
  - **`DptFilterChip.tsx` & `DptAddFilterChip.tsx` dihapus dari codebase** (orphaned, tidak dipakai lagi) — lihat `ui-registry.md` § DptListScreen untuk detail lengkap evolusi UI ini.
  - **Pelajaran proses:** waktu ambiguitas UI ("card putih saja") tidak jelas dari deskripsi teks, lebih efisien user kirim mockup/screenshot langsung daripada iterasi tebak-tebakan — dipakai di sesi ini setelah iterasi ke-2 meleset.
- 2026-08-22 — **Feature 10 (Kekuatan Wilayah) dikerjakan lewat `/EnterPlanMode`** (user minta "kita rencanakan implementasi" — direncanakan dulu sebelum kode, plan disimpan & disetujui sebelum eksekusi). Ringkasan keputusan (detail lengkap ada di plan yang disetujui):
  1. **Skor 0-100 di `kekuatanwilayah.png` TIDAK ADA di backend manapun** — diverifikasi langsung: baca `Dtdoor` entity (`/Users/asdarsaid/JSI/api/src/dtdoor/entities/dtdoor.entity.ts`) + grep `skor|score|kekuatan` di SELURUH backend & web client, nihil semua. Ditanyakan ke user via pertanyaan eksplisit (Aturan #6) — **dipilih: dihitung client-side dari `kategoriId`** (field yang sudah dikumpulkan form Dtdoor Feature 04, 7 nilai kategori). Mapping id→skor & threshold tier (Kuat≥75/Sedang 45-74/Lemah<45, persis teks di `kekuatanpemilih.png`) ada di `src/lib/dtdoorScore.ts` — **nilai mapping adalah PROPOSAL saya, BELUM dikonfirmasi user/campaign team**, gampang dikoreksi di satu tempat itu.
  2. **Entry point (dipilih user):** di-push dari `ProgramPemenanganScreen` tab "Door To Door" (tombol baru "Lihat Kekuatan Wilayah", `ProgramStack.tsx` route `KekuatanWilayah`) — bukan tab/shortcut Home.
  3. **`MOCK_DTDOOR` (`services/dtdoor.ts`) diperluas dari 3 jadi 21 entri**, disebar di 4 kelurahan yang sama dengan `MOCK_TIMSES` (Cileunyi Kulon/Cinunuk/Cimekar/Cileunyi Wetan — konsisten lintas fitur), kategoriId divariasikan supaya tiap kelurahan jatuh ke tier berbeda. `fetchDtdoorAll()` (baru, unpaginated) ditambah untuk agregasi — cabang API asli sengaja `throw` (tidak ada endpoint agregat terkonfirmasi), sama pola dengan `services/dpt.ts`.
  4. **Scoping wilayah:** admin/adminsekret lihat semua kelurahan; role lain dibatasi ke kecamatan sendiri (`useProfile().kecamatan`) — client-side saja, pola sama dengan `TimsesScreen`/`DptListScreen`, BUKAN pertanyaan baru (kategori sama, tidak ditanya ulang).
  5. **Kelurahan tanpa satupun kunjungan berkategori DIKECUALIKAN** dari grid & list (tidak bisa dikasih tier tanpa skor) — termasuk record dengan `desa: null` (contoh: mock id 3 "Dedi Suherman", sengaja dipertahankan sebagai demonstrasi kasus ini).
  6. **⚠️ Asumsi belum diverifikasi user:** toggle "List" di `kekuatanwilayah.png` cuma ada 1 screenshot (state "Peta" aktif) — tidak ada mockup untuk state "List". Diasumsikan "List" cuma menyembunyikan grid warna+legenda ("Peta"), "Detail per Kelurahan" (selalu ada) jadi tampilan list-nya. Koreksi kalau salah.
  7. Grid warna ("Peta") **data-driven** (1 kotak = 1 kelurahan asli dengan skor), BUKAN grid 2×2 dekoratif tetap seperti di mockup — mockup sendiri cuma sample ilustratif (4 kotak ditampilkan tapi total "4 Kuat+6 Sedang+2 Lemah"=12 di kartu ringkasan, jadi tidak 1:1 dari awal). Tekstur garis diagonal di background mockup disederhanakan jadi `bg-surface-secondary` polos (dekoratif, tidak ada asset/SVG untuk itu).
  - Reuse: `Badge` (varian `success`/`warning`/`danger` sudah ada, tidak perlu varian baru) untuk badge tier di list.
  - File baru: `src/lib/dtdoorScore.ts` (folder `src/lib/` langsung, bukan di bawah `src/lib/api/` — ini pure business logic, bukan transport API) — dicatat sebagai penambahan struktur folder baru sesuai `architecture.md`.
- 2026-08-22 — **Bug Android: teks di semua `TextInput` tidak center vertikal, turun ke bawah** (laporan user + screenshot device asli, field Profil "Nama Lengkap"/"No. HP"/dst. lewat `Input.tsx`) — 3 iterasi:
  1. `textAlignVertical="center"` (prop Android-only) saja — belum cukup.
  2. Ditambah `style={{ includeFontPadding: false, paddingVertical: 0 }}` — masih belum cukup di device asli user (baru ketahuan lewat screenshot, verifikasi sebelumnya cuma `tsc`+bundle CI, tidak ada device fisik).
  3. **Fix final:** tambah `lineHeight: 20` eksplisit di style yang sama. Root cause: font kustom **Inter** (`@expo-google-fonts/inter`) di Android tidak mendistribusikan slack `lineHeight` (`text-body-md` = 24px, lebih besar dari fontSize 16px) secara simetris di dalam line-box glyph — `includeFontPadding`/`paddingVertical` cuma ngatur padding EditText, bukan posisi glyph di dalam lineHeight-nya sendiri. Override `lineHeight` jadi nilai lebih ketat (20) menghilangkan slack itu.
  - Pola 3-prop ini (`textAlignVertical="center"` + `includeFontPadding:false` + `paddingVertical:0` + `lineHeight:20`) sekarang WAJIB dipakai di **setiap** `TextInput` baru yang pakai `text-body-md` — lihat `ui-registry.md` § Input untuk detail & daftar lokasi yang sudah dipakai (`Input.tsx`, 3 search bar DPT, input pesan `CustomerServiceScreen`).
- 2026-08-22 — **`CustomerServiceScreen` baru ditambahkan** (`src/screens/home/CustomerServiceScreen.tsx`, di luar build-plan awal, permintaan user langsung di sesi ini) — referensi `context/designs/customer-service.png` ternyata screenshot app lain ("halo tiket"/tiket.com, chatbot AI), BUKAN mockup JSI. Cuma pola layout yang dipakai (bubble sambutan, card list topik FAQ, input pesan di bawah); konten diganti FAQ statis seputar app JSI ini (6 topik nyata dari fitur yang sudah ada), TANPA klaim "AI"/chatbot beneran (dikonfirmasi eksplisit ke user sebelum build — repo ini tidak punya backend chat). Icon hamburger di `HomeScreen` diganti `headset-outline`, push ke screen ini; tab bar disembunyikan khusus di screen ini via `navigation.getParent()?.setOptions({ tabBarStyle: { display: "none" } })` (di-restore saat unmount).
- 2026-08-22 — **Feature "Kekuatan Pemilih" (Skoring Kekuatan Pemilih) dikerjakan** — dipindah dari `build-plan.md` § Fase Berikutnya ke aktif (permintaan user). File baru: `src/screens/program/KekuatanPemilihScreen.tsx` + 4 component `src/components/kekuatan/` (`KekuatanWilayahSelector`, `KekuatanDistribusiRow`, `KekuatanRankingRow`, `KekuatanPemilihCard`). 2 keputusan dikonfirmasi via pertanyaan eksplisit sebelum build: (1) entry point — tombol baru "Lihat Skor Pemilih" di `ProgramPemenanganScreen` tab Door To Door, sejajar "Lihat Kekuatan Wilayah"; (2) filter wilayah eksplisit ("Wilayah: Kec. X") — admin bebas pilih kecamatan manapun dari data (default "Semua Kecamatan"), role lain terkunci/disabled ke kecamatan sendiri. Reuse penuh model skor `lib/dtdoorScore.ts` (sama dengan Kekuatan Wilayah) dan `DptSummaryCards` untuk 2 kartu ringkasan. 1 keputusan mandiri (kategori sama dengan preseden "45%→hitungan sederhana" Feature 04/05, tidak ditanya ulang): "Pemilih Skor Tertinggi" dibatasi top 10, bukan seluruh pemilih.
- 2026-08-22 — **HomeScreen dapat 3 penambahan visual** (permintaan user "tampilan Home terasa hampa/mati", dikonfirmasi bertahap via pertanyaan eksplisit untuk tiap elemen sebelum build):
  1. Progress bar "Persentase Data DPT" (ada di `home-dashboard.png`, sebelumnya sengaja di-skip karena belum ada data DPT sama sekali) — dikembalikan pakai **data demo eksplisit** (kabupaten 7303/BANTAENG, satu-satunya kabupaten dengan mock DPT lengkap, `DPT_DEMO_KAB_WIL_ID` di `HomeScreen.tsx`), caption jujur "Contoh data (demo)" — TIDAK diklaim sebagai wilayah tugas user asli (tidak ada konsep kabupaten-milik-user di data profil manapun).
  2. Section "Aktivitas Terbaru" baru (di luar `home-dashboard.png`, **izin eksplisit user** untuk build dari `ui-rules.md`/`ui-tokens.md` tanpa file desain — Aturan #1 opsi c) — reuse `useDtdoorList`/`useGotvList` (cache sama dengan `ProgramPemenanganScreen`), digabung+diurutkan ulang client-side by `createdAt`, top 5. Component baru `src/components/home/HomeActivityRow.tsx`.
  3. Section "Persentase Data DPT" & "Akses Cepat" dibungkus card putih (`rounded-lg border border-border bg-surface`) atas permintaan user — section lain (Program Pemenangan, Aktivitas Terbaru) TIDAK dibungkus lagi karena sudah berisi card individual di dalamnya (hindari nesting >2 level border-radius, `ui-rules.md`). Tulisan "JSI" di header dihapus (permintaan user).
- 2026-08-22 — **Fitur "Target Suara" (di luar build-plan awal, permintaan user langsung) — dikonfirmasi bekerja di device asli.** Drill-down Kabupaten→Kecamatan→Kelurahan→TPS, tiap level bisa diisi angka target suara sendiri + lihat persentase dari total DPT. 2 keputusan dikonfirmasi via pertanyaan eksplisit sebelum build (beda dari Kekuatan Wilayah/Pemilih yang skornya cuma DIHITUNG dari data yang sudah ada — ini angka BARU yang diinput manusia, butuh keputusan penyimpanan):
  1. **Penyimpanan local-only di device** (`expo-secure-store`, `src/lib/targetSuara.ts`, pola sama `lib/api/token.ts`) — TIDAK ada endpoint backend apapun untuk konsep ini (bukan cuma DPT/Hasil Rekap yang belum ada repo — 5 modul confirmed pun tidak punya). Konsekuensi: target BELUM sinkron antar device/anggota tim, cuma tersimpan di HP masing-masing. Kalau backend-nya ada nanti, cukup ganti isi `lib/targetSuara.ts` — hook (`useTargetSuara.ts`) & screen tidak perlu berubah (kontrak `useTargetSuaraMap()`/`useSetTargetSuara()` sudah abstraksi yang tepat).
  2. **Hierarki wilayah reuse DPT** (`services/dpt.ts`, bukan hierarki Kecamatan/Desa dari Timses/Dtdoor yang dipakai Kekuatan Wilayah/Pemilih) — dipilih karena DPT satu-satunya sumber dengan 4 level lengkap sampai TPS. Konsekuensi: cuma **Kabupaten Bantaeng (7303)** yang datanya lengkap sampai TPS (sama keterbatasan dengan `DPT_DEMO_KAB_WIL_ID` di Home) — kabupaten lain tampil kosong di tiap level.
  - File baru: `src/lib/targetSuara.ts`, `src/hooks/useTargetSuara.ts`, `src/components/targetsuara/TargetSuaraHeaderCard.tsx`, 4 screen `src/screens/targetsuara/TargetSuara{Kabupaten,Kecamatan,Kelurahan,Tps}Screen.tsx`. 4 route baru di-attach ke `DptStack.tsx` yang SAMA (bukan stack terpisah) — reuse route params kabWilId dari DptListScreen. Entry point: icon "flag" baru di `headerRight` DptListScreen (sejajar "+"/download yang sudah ada).
  - TPS tidak ada endpoint list tersendiri (sama seperti filter TPS di `DptListScreen`) — diturunkan dari record DPT (`useDptList(kabWilId)`, filter `idKel`, group by `noTps`), `totalPemilih` per TPS = COUNT record (bukan field asli).
- 2026-08-22 — **Laporan/Analitik Target Suara ditambahkan** (diminta user setelah drill-down dikonfirmasi bekerja) — `src/screens/targetsuara/TargetSuaraReportScreen.tsx`, agregasi 1 kabupaten penuh via `src/hooks/useTargetSuaraReport.ts` (scan semua Kecamatan→Kelurahan paralel + TPS dari record DPT, digabung dengan target lokal). **Keputusan desain (mandiri, tidak ditanya — cukup jelas dari logika data yang ada):** laporan SENGAJA TIDAK menjumlahkan target lintas-level jadi 1 angka "grand total" — kab/kec/kel/tps diisi independen (bukan hierarkis auto-sum), menjumlahkannya akan double-count (mis. target Kabupaten + target Kelurahan di dalamnya terhitung dua kali). Ditampilkan sebagai: (1) completion rate per level ("X dari Y wilayah sudah diisi", `TargetSuaraCompletionRow`), (2) daftar semua entry yang sudah diisi (lintas level, urut target terbesar, tap → drill balik ke screen input aslinya). Entry point: tombol "Lihat Laporan Target Suara" di `TargetSuaraKabupatenScreen`.
- 2026-08-22 — **Fitur "Saksi TPS" + "Real Count C1" (di luar build-plan awal, permintaan user langsung) — dikonfirmasi bekerja di device asli, lewat 2 iterasi.**
  1. **Iterasi 1 (build awal):** Kedua fitur di-push dari `TargetSuaraTpsScreen` (reuse TPS yang sama, sudah punya `kelWilId`/`noTps`/`namaTps`). Permintaan eksplisit user: **"cukup UI-nya dulu, API dikembangkan belakangan"** — beda dari Target Suara (local-only `SecureStore`), fitur ini pakai **pola mock service STANDAR** (`services/saksi.ts`, `services/realcount.ts` — array in-memory + `isMockApiEnabled()`, sama persis pola `dtdoor.ts`/`gotv.ts`; cabang non-mock sengaja `throw` pesan jelas "endpoint belum ada", CLAUDE.md Aturan #6) — dipilih karena user berencana bikin endpoint asli sendiri, jadi swap ke API nyata harus jadi drop-in replacement fungsi service, bukan migrasi dari local-storage.
     - File: `src/types/saksi.ts`, `src/types/realcount.ts`, `src/services/saksi.ts`, `src/services/realcount.ts`, `src/hooks/useSaksi.ts`, `src/hooks/useRealCount.ts`, `src/components/saksi/SaksiCard.tsx` (status diubah lewat `Select` reuse apa adanya), `src/screens/saksi/{SaksiTpsScreen,SaksiFormScreen}.tsx`, `src/screens/realcount/RealCountC1Screen.tsx` (form dengan cross-check otomatis: suara calon + suara lain vs total suara sah, warning kalau tidak cocok — bantu saksi tangkap salah transkrip C1).
  2. **Iterasi 2 (revisi user, hari yang sama):** **"Real Count C1 dipisah, tanpa ikut di dalam Target Suara"** — dibangun alur drill-down wilayah SENDIRI untuk Real Count: `RealCountKabupatenScreen` → `RealCountKecamatanScreen` → `RealCountKelurahanScreen` (reuse hierarki DPT yang sama seperti Target Suara, TAPI TANPA header card "target level ini" — C1 murni konsep TPS, tidak ada nilai di level di atasnya) → `RealCountC1Screen` (leaf, TIDAK BERUBAH). Entry point pindah jadi icon terpisah di `headerRight` `DptListScreen` (bukan lagi tombol di `TargetSuaraTpsScreen`, tombol itu **dihapus**). `RealCountKelurahanScreen` sekaligus kasih tanda "✓ Sudah ada C1 · N suara" di daftar TPS (1 query per kelurahan via `useRealCountByKelurahan`, bukan N query per TPS) — nilai tambah kecil, bukan diminta eksplisit tapi konsisten pola Target Suara yang juga kasih status inline di list. **Saksi TPS TETAP nested di `TargetSuaraTpsScreen`** (user cuma minta Real Count yang dipisah, bukan Saksi).
  - **Icon headerRight `DptListScreen` (4 total sekarang):** "flag" (Target Suara) tetap; icon Real Count awalnya "clipboard" — **DIGANTI "calculator"** (permintaan user "icon yang relevan") karena "clipboard" bentrok makna dengan icon "Survey" di grid Akses Cepat `HomeScreen` (`icon="clipboard"`), sementara kalkulator lebih tepat untuk "menghitung suara".
- 2026-08-22 — **Grid kotak-kotak polos di "Peta Kekuatan Wilayah" DIGANTI stacked bar chart** (permintaan user "chart yang keren"), dikerjakan lewat skill `dataviz`. Job data = part-to-whole 3 kategori → stacked bar (BUKAN pie/donut, sesuai `choosing-a-form.md`). Warna reuse token status yang sudah ada (success/warning/danger), BUKAN palet baru — `validate_palette.js` sempat FAIL pasangan warning↔danger (ΔE normal-vision 9.9 < 15), tapi itu legal karena sudah ada 2 secondary encoding: gap 2px antar segmen + legend berlabel teks (bukan swatch doang). Component baru `KekuatanZoneStackedBar.tsx`; `KekuatanZoneCard.tsx` (grid kotak lama) **DIHAPUS** dari codebase (orphan).
- 2026-08-22 — **3 saran fitur "kalau saya konsultan politik" — direncanakan dulu di `build-plan.md` § Fase Berikutnya, lalu dikerjakan berurutan sesuai prioritas user, SEMUA dikonfirmasi bekerja di device:**
  1. **Rival Internal Separtai** (`src/screens/rivalcaleg/`, `src/services/rivalcaleg.ts`) — khas pileg Indonesia (proporsional terbuka, caleg bersaing suara dengan caleg LAIN DARI PARTAI SENDIRI di dapil sama). CRM sederhana: List Rival Caleg → tambah (nama, no. urut, catatan) → Detail → assessment per wilayah (kecamatan/desa FREE TEXT + level ancaman Rendah/Sedang/Tinggi + catatan, upsert). 2 keputusan dikonfirmasi via pertanyaan eksplisit sebelum build: (a) wilayah pakai hierarki Kecamatan→Desa (pola Kekuatan Wilayah/Pemilih, Timses/Dtdoor) BUKAN hierarki DPT — assessment strategis level kecamatan, tidak perlu granular TPS; kecamatan/desa sengaja free-text (bukan picker dari data Dtdoor kita sendiri) karena justru mencatat wilayah KUAT LAWAN yang mungkin belum pernah kita kunjungi; (b) penyimpanan **mock service standar** (pola sama Saksi TPS/Real Count — array in-memory, cabang non-mock `throw`), BUKAN local-storage seperti Target Suara. Entry point: shortcut baru "Rival Caleg" (icon mata) di grid Akses Cepat `HomeScreen`, route baru di `HomeStack.tsx` (bukan `DptStack`/`ProgramStack` — beda keluarga fitur).
  2. **Prioritas Follow-up Pemilih Swing** (`src/screens/program/SwingVoterFollowUpScreen.tsx`) — beda dari 2 fitur lain, **murni derive dari data Dtdoor yang sudah ada**, tidak ada schema/service baru sama sekali. Filter kategoriId=7 ("Belum Menentukan") — sengaja TIDAK termasuk "Pemilih Kompetitor" (sudah condong lawan, ROI follow-up rendah). Urutan: `createdAt` ascending (paling lama belum di-follow-up duluan) — satu-satunya sinyal urgensi yang ada tanpa mengarang field baru. Badge urgensi client-side (`Urgent` ≥7 hari, `Perlu Follow-up` 3-6 hari, `Baru Dikunjungi` <3 hari). Tombol **"Hubungi" BENERAN fungsional** (`Linking.openURL("tel:...")`, first use `Linking` di codebase ini) — bukan placeholder, murni kemampuan device. Entry point: grid icon di `ProgramPemenanganScreen` tab Door To Door.
  3. **"Ringkasan Data"** (`src/screens/program/DtdoorAnalyticsScreen.tsx`) — analisa deskriptif dari Dtdoor, murni derive client-side (reuse `useDtdoorAll()`, tidak ada data/service baru). **⚠️ Klarifikasi penting:** permintaan awal user pakai contoh "pilihan pileg ada berapa orang" — Dtdoor **TIDAK PUNYA field pilihan kandidat/partai sama sekali**. Proxy terdekat yang benar-benar ada di data = `kategoriId` (klasifikasi dukungan internal, `lib/dtdoorScore.ts`), BUKAN pilihan per-kandidat — dijelaskan ke user, dibangun sebagai "Distribusi Kategori" bukan "hasil pileg". Isi: 3 kartu ringkasan (total kunjungan/wajib pilih tercakup/kelurahan tercakup) + Distribusi Kategori (7 kategori, warna ikut tier kekuatan kategori — reuse skema, bukan palet baru) + Distribusi Gender + Program Bantuan Terpopuler (top 5 dari `programBantuan1/2/3`, warna netral `accent` karena tidak ada makna status baik/buruk — component baru `DtdoorMagnitudeRow.tsx`, beda dari `DtdoorCategoryRow.tsx` yang warnanya ikut tier).
  - **Grid icon "Akses Cepat" di tab Door To Door `ProgramPemenanganScreen`** direstrukturisasi 2x seiring fitur baru masuk: dari 3 tombol `Button` full-width bertumpuk (dilaporkan user "kurang enak dilihat") → grid 3 kolom reuse `HomeQuickAccessItem` → grid 4 kolom (nambah "Ringkasan Data"). `HomeQuickAccessItem` (`src/components/home/HomeQuickAccessItem.tsx`) dapat prop `widthClass` opsional (default `w-1/4`, tidak mengubah Home) supaya reusable di grid kolom berapapun.
  - **Background root `HomeScreen` diganti `LinearGradient`** (`expo-linear-gradient`, dependency baru — SDK 54 compatible via `npx expo install`) — permintaan eksplisit user "gradient biru halus", pengecualian dari `ui-rules.md` § Larangan ("jangan tambah gradient kecuali diminta eksplisit"). 2 iterasi: mulai `#eff6ff→#f8fafc` (locations 0/0.35), dinaikkan ke `#dbeafe→#f8fafc` (locations 0/0.4) atas permintaan user "lebih keras 15% lagi" — `#dbeafe` = token `accent-soft` yang sudah ada, bukan hex baru.
- 2026-08-22 — **Item #3 "Pengeluaran/Anggaran Kampanye" (saran konsultan politik terakhir di daftar yang sama) — BELUM dikerjakan, di-parkir SEBELUM tulis kode apapun.** Tidak ada file desain untuk screen ini di `context/designs/` — ditanyakan ke user via pertanyaan eksplisit sesuai Aturan #1 (tidak ada pengecualian di repo ini). **User pilih "saya siapkan desainnya dulu"** — opsi (c) Aturan #1 (izin build dari `ui-rules.md`/`ui-tokens.md` tanpa referensi visual) DITOLAK untuk fitur ini, beda dari pola Rival Caleg/Swing Voter sebelumnya. Jadi TIDAK ADA kode/screen/service ditulis sesi ini, murni menunggu file desain/Figma link dari user. 2 keputusan skema SUDAH dikonfirmasi lebih dulu supaya build lebih cepat begitu desain masuk (tidak perlu tanya ulang saat itu):
  1. **Struktur data ikut kategori resmi LPSDK/LPPDK** (jenis pengeluaran sesuai PKPU — bukan kategori bebas/custom yang dikarang). Detail kategori resmi (iklan, rapat umum, bahan kampanye, dll.) perlu diverifikasi ke user/dokumen PKPU aktual saat implementasi — kategori LPSDK/LPPDK bisa berubah per periode pemilu, JANGAN diisi dari training data.
  2. **Penyimpanan: mock service standar** (pola sama `services/rivalcaleg.ts`/`saksi.ts`/`realcount.ts` — array in-memory + `isMockApiEnabled()`, cabang non-mock `throw` pesan jelas), BUKAN local-only `SecureStore` seperti Target Suara — tidak ada backend/repo untuk modul ini (konsisten Aturan #6).
  - **Sesi berikutnya yang lanjut item ini: cek dulu apakah `context/designs/` sudah punya file baru untuk screen ini** sebelum mulai — jangan asumsi masih kosong.
- 2026-08-22 — **UPDATE: desain "Budgeting Kampanye" ditemukan & diekstrak** dari project Claude Design user ("Desain Mobile JSI Dashboard", projectId `160ea937-2f8a-4ff4-9977-f8bc398c90a0`, file "JSI Mobile Dashboard.dc.html" — SATU project yang sama dengan semua desain fitur lain, artboard ke-12 dari 13) via tool **`DesignSync`** (`get_project`/`list_files`/`get_file`), bukan lewat browser/screenshot manual seperti biasanya. Disimpan di `context/designs/budgeting-kampanye.dc.html` (markup + data asli sebagai komentar — LEBIH presisi dari PNG karena hex/teks eksak, bukan pixel-sampled). Preview interaktif juga dipublikasi sebagai Artifact untuk user (link di respons chat, tidak disimpan ke repo). Status Aturan #1 **UNBLOCKED** untuk fitur ini. Yang masih perlu diputuskan SEBELUM tulis kode (belum dijawab user):
  1. **Konflik kategori pos anggaran** — mockup punya 5 pos operasional ("Atribut & Alat Peraga", "Operasional Timses", "Door To Door", "Social Event", "Digital & Broadcast"), BUKAN kategori resmi LPSDK/LPPDK (KPU/PKPU) yang sudah dikonfirmasi user di sesi sebelumnya sebagai pilihan skema data. Dua sistem klasifikasi ini beda tujuan (operasional internal vs pelaporan regulasi resmi) — perlu dikonfirmasi mana yang dipakai, atau apakah keduanya perlu (kategori internal untuk UI, mapping ke kategori resmi untuk keperluan pelaporan LPSDK/LPPDK terpisah).
  2. **Entry point mockup pakai pola "drawerItems"** (menu hamburger berisi list menu sekunder: Broadcast WhatsApp, Lacak Relawan, Timses, Skoring Kekuatan Pemilih, Peta Kekuatan Wilayah, Budgeting Kampanye, Survey, Tokoh Masyarakat, Zona Pemenangan, Bigdata Pendukung) — TAPI app nyata sudah tidak pakai drawer sama sekali sejak "TEMUAN NAVIGASI BESAR" (lihat entry di atas), semua fitur sekunder itu sudah/akan masuk lewat grid Akses Cepat Home atau headerRight icon (DptListScreen). Perlu tentukan entry point definitif sebelum build, bukan ikut pola drawer mockup apa adanya.
  - Model data lengkap (field per state, warna threshold, isi bottom sheet "Catat Pengeluaran") sudah terdokumentasi penuh di file referensi & Artifact preview — begitu 2 poin di atas dijawab, build bisa langsung jalan tanpa riset ulang.
- 2026-08-22 — **2 klarifikasi di atas SUDAH DIJAWAB user, menggantikan keputusan skema sebelumnya:**
  1. **Kategori pos anggaran ikut mockup apa adanya** (5 pos operasional: Atribut & Alat Peraga, Operasional Timses, Door To Door, Social Event, Digital & Broadcast) — **BUKAN** kategori resmi LPSDK/LPPDK yang sempat dikonfirmasi di keputusan sebelumnya (keputusan itu SEKARANG SUPERSEDED). Alasan tidak ditanya ulang ke user secara eksplisit di titik ini, cukup jelas dari pilihan "Ikuti mockup": begitu ada contoh UI real, ikuti visual apa adanya (Aturan #1) menang atas asumsi skema abstrak sebelumnya.
  2. **Entry point: shortcut baru di grid Akses Cepat `HomeScreen`** (pola sama Rival Caleg/Tracking — bukan drawer seperti mockup, bukan juga headerRight `DptListScreen`) — push ke route baru di `HomeStack.tsx`.
  - **Belum dikerjakan** — ini baru keputusan skema, item #3 masih di `build-plan.md` § Fase Berikutnya sampai sesi build sungguhan dijalankan (`/new-feature`).
- 2026-08-22 — **Budgeting Kampanye dikerjakan via `/new-feature` (prompt di atas) DAN dikonfirmasi user bekerja di device.** Item #3 saran konsultan politik sekarang **SELESAI** (ditandai ✅ di `build-plan.md` § Fase Berikutnya, pola sama Rival Caleg/Swing Voter — bukan checklist Phase 1-3 di atas, konsisten dengan fitur "di luar build-plan awal" lain). Lihat `ui-registry.md` § BudgetingKampanyeScreen untuk detail komponen/keputusan build (5 pos anggaran operasional, bottom sheet mockup dijadikan pushed screen, transaksi baru default status "Menunggu"). "Biaya per suara" (tujuan akhir item ini) tetap tertunda — butuh data Target Suara/Real Count lengkap dulu.
- 2026-08-22 — **08 DPT: CRUD dasar (Tambah/Edit/Hapus) dikerjakan atas permintaan eksplisit user, dengan 1 keputusan yang MENG-OVERRIDE catatan sebelumnya:** user secara eksplisit bilang **tidak perlu tunggu contoh request write/endpoint asli** — cukup mock data dulu. Ini beda dari "Berikutnya" sebelumnya yang bilang CRUD "butuh contoh request write dari user" — bukan asumsi yang salah, memang keputusan user berubah/dipercepat di titik ini. "Tandai partisipasi program" (bagian lain dari scope 08 DPT) **TIDAK diminta & TIDAK dikerjakan** sesi ini — field `sudahDtdoor`/`sudahGotv` tetap read-only, selalu `false` untuk record DPT baru.
  1. **Pola mock service standar** (`services/dpt.ts` — `mockDptByKab: Record<number, DptRecord[]>` mutable per-kabupaten, `mockDptNextId` mulai dari `900001` biar gampang dibedakan dari id asli contohdpt.ts yang 191xxx-an) — sama pola dengan `rivalcaleg.ts`/`saksi.ts`/`realcount.ts`, cabang non-mock tetap `throw` (Aturan #6, endpoint DPT masih belum dikonfirmasi).
  2. **Referensi desain untuk bottom sheet aksi kartu ("dptSheetVoter") DITEMUKAN, bukan dikarang** — bagian dari file `.dc.html` yang SAMA yang dibaca penuh via `DesignSync` untuk Budgeting Kampanye sesi sebelumnya (1 project canvas mencakup semua screen, termasuk DPT). Canvas asli punya 4 baris aksi (2 "Tandai ikut..." + Edit + Hapus) — 2 baris "Tandai ikut" SENGAJA di-skip dari `DptVoterActionSheet` karena scope sesi ini cuma Edit/Hapus, bukan tandai partisipasi.
  3. **Form Tambah/Edit (`DptRecordFormScreen`) TIDAK punya mockup spesifik di canvas** (yang ada cuma bottom sheet menu + list card) — dibangun generik dari `ui-rules.md`/`ui-tokens.md`, pola sama persis `RivalCalegFormScreen`/`GotvFormScreen` (zod + Input/Select + Button). **Kecamatan/Kelurahan WAJIB dipilih dari `Select`** (reuse `useDptKecamatanList`/`useDptKelurahanList` yang sama dipakai filter `DptListScreen`), BUKAN free-text — keputusan mandiri (bukan ditanya user), supaya record DPT baru konsisten kefilter benar oleh Kecamatan/Kelurahan (yang match berdasar `idKec`/`idKel`, bukan string nama).
  4. **Form dipakai dual-purpose create+edit** (route params `record?: DptRecord` opsional menentukan mode) — pola sama `RivalAssessmentFormScreen`, bukan 2 screen terpisah.
  5. **Hapus pakai `Alert` konfirmasi 2 tombol** (Batal/Hapus destructive) sebelum benar-benar menghapus dari mock array — pola sama tombol "Keluar" `ProfileScreen`.
- 2026-08-22 — **"Form Door To Door terintegrasi DPT" dikerjakan (permintaan eksplisit user: "buatkan form door to door sama seperti di web dimana dtd ini terintegrasi ke dpt").** Riset dulu ke `client/src/pages/dtdoor/dtdoor-modal.jsx` + `FormDataKunjungan.jsx` (form legacy, field-nya cocok 1:1 dengan entity backend `Dtdoor` di `/Users/asdarsaid/JSI/api`) — **BUKAN** `dtdoor-modal2024.jsx`/`FormDataKunjungan2024.jsx` yang dipakai `dpt-page.jsx` sekarang: variant "2024" itu manggil endpoint (`/dtdoor/tipe-pemilih`, `/dtdoor/pilihan-pileg`, `/dtdoor/program-bantuans`, `/dtdoor/:idDpt` GET, field `kunjungans` array) yang **TIDAK ADA SAMA SEKALI** di `DtdoorController` backend yang sudah dikonfirmasi (dicek langsung — cuma ada `data`/`grupby*`/`count`/`status`/`nik/:nik`/CRUD by id) — variant "2024" kemungkinan besar kode mati/backend beda deployment, jadi diabaikan sebagai referensi (Aturan #6).
  1. **Temuan penting (dikonfirmasi ke user via pertanyaan eksplisit sebelum coding):** riset ini juga menemukan bahwa 7 nilai "Kategori" (Simpatisan…Belum Menentukan) di web (`Status` array, `client/src/data/dataArea.js`) sebenarnya dikirim ke field **`status`** (relasi `StatusDtDoor`), BUKAN `kategori` — sementara `kategori` (`KategoriDtDoor`) di web dipakai untuk 5 nilai "Pilihan Dipileg" (preferensi partai/caleg) yang sama sekali belum ada di mobile. Mobile (`DtdoorFormScreen`, sudah dikonfirmasi jalan di device sejak Feature 04) mengirim 7 nilai itu ke `kategori` — kemungkinan tertukar sejak awal (progress-tracker sempat menandai ini "asumsi belum diverifikasi", sekarang ketemu bukti konkret). **User memilih: TETAP kirim ke `kategori`** — tidak ada perubahan ke behavior yang sudah dikonfirmasi jalan; field "Pilihan Dipileg" tidak ditambahkan (di luar scope). Temuan ini murni dicatat untuk keputusan lanjutan nanti, BUKAN diperbaiki sesi ini.
  2. **`DptRecord` (`types/dpt.ts`) dapat 2 field baru dari data yang sebenarnya SUDAH ADA di `contohdpt.ts` tapi belum dimodelkan:** `nik: string | null` dan **`idDpt: number`** — field terpisah dari `id`. `id` (191483 dst.) = primary key besar record DPT; `idDpt` (1, 2, 3…) = sequence kecil yang dipakai WEB sebagai kunci linkage ke Dtdoor (`dpt.idDpt`, BUKAN `dpt.id` — lihat `FormDataKunjungan.jsx` baris `idDpt: idDpt`). Mock seed (`MOCK_DPT_KAB_7303_SEED`) di-update dengan `idDpt` asli dari `contohdpt.ts` (1,2,3,4,5,7,8 — idDpt 6 memang tidak ada di sample asli). Record baru dari `DptRecordFormScreen` (create manual, bukan data pemerintah) dapat `idDpt` sintetis = `id`-nya sendiri (range 900000+, tidak akan tabrakan dengan idDpt asli 1-8an).
  3. **Entry point: `DptVoterActionSheet` row "Tandai ikut Door To Door" DIKEMBALIKAN** (sebelumnya sengaja di-skip di CRUD dasar) — referensi desainnya di-re-fetch langsung via `DesignSync` dari project canvas yang sama (`160ea937-2f8a-4ff4-9977-f8bc398c90a0`), bukan dikarang. "Tandai ikut Social Event" (baris sebelahnya di canvas) **TETAP di-skip** — user cuma minta Door To Door.
  4. **`DtdoorFormScreen` (sebelumnya cuma terdaftar di `ProgramStack`) sekarang di-reuse APA ADANYA (component sama) di `DptStack` juga** — route "DtdoorForm" didaftarkan di kedua stack, param list-nya diketik lokal ke file screen itu sendiri (`DtdoorFormParams`, diekspor) supaya tidak terikat ke `ProgramStackParamList`. Dibuka dari DptStack (`{ dptRecord, kabWilId, kabNama }`) → field alamat/identitas di-prefill dari record DPT (tetap editable, pola sama web) dan `idDpt` submit pakai `dptRecord.idDpt` (bukan synthetic). Dibuka dari ProgramStack (params `undefined`, tombol "+ Input Kunjungan Baru") → perilaku LAMA tidak berubah sama sekali (tetap synthetic idDpt, tidak ada prefill).
  5. **`useMarkDptDtdoor(kabWilId)` (hook baru, `useDptRecordMutations.ts`) dipanggil setelah `createDtdoor` sukses** kalau ada `dptRecord` — flip `sudahDtdoor: true` di mock array DPT (`services/dpt.ts` § `markDptDtdoor`, pola sama `updateDptRecordMock`) supaya badge "Door To Door ✓" di `DptCard` langsung berubah tanpa restart. Ini **murni simulasi client-side** — TIDAK ada relasi baca-balik sungguhan antara mock Dtdoor dan mock DPT (2 array in-memory terpisah, sama seperti keterbatasan mock lain di app ini), akan otomatis benar begitu backend asli & endpoint DPT dikonfirmasi (relasi `dpt.dtdoor` akan terisi dari server, tidak perlu flip manual lagi).
  6. **Tapping "Tandai ikut Door To Door" SELALU buka form kosong ter-prefill** (mode "kunjungan baru"), TIDAK mencoba fetch/edit kunjungan Dtdoor yang sudah ada untuk record itu (beda dari web yang punya `GET /dtdoor/:idDpt` untuk itu) — keputusan mandiri (bukan ditanya user), karena mobile belum punya service "cari Dtdoor by idDpt" dan scope permintaan user cuma "buat form ... terintegrasi ke DPT", bukan alur edit-kunjungan-existing. Mudah ditambah nanti kalau dibutuhkan.
  - Verifikasi non-visual: `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 801ms index.ts (1693 modules)`, HTTP 200 dari `/index.bundle?platform=ios&dev=true`, dikonfirmasi memuat module baru `DtdoorFormParams`/`markDptDtdoor`/`useMarkDptDtdoor` + row baru `DptVoterActionSheet`). **Belum diverifikasi visual/fungsional di device** — user perlu tes: (1) drill ke DPT BANTAENG seperti biasa, (2) tap "..." di kartu pemilih → row baru "Tandai ikut Door To Door" muncul PALING ATAS (di atas "Edit data pemilih") dengan icon jalan kaki biru, (3) tap → screen "D2D — {nama}" terbuka, field Nama/NIK/Jenis Kelamin/TPS/RT/RW/Desa/Kecamatan/Kabupaten sudah terisi data pemilih itu (tetap bisa diedit), (4) submit → kembali ke list DPT, badge kartu itu berubah jadi "Door To Door ✓" (hijau), (5) buka tab Program → Door To Door → "+ Input Kunjungan Baru" tetap seperti biasa (form kosong, tidak ke-prefill apapun — pastikan entri standalone tidak ke-regresi).
- 2026-08-22 — **Permintaan "aktivasi fitur Tokoh di Akses Cepat" (tandai DPT sebagai Tokoh Masyarakat via bintang + kategori) — DIPARKIR, TIDAK ada kode ditulis, sesuai Aturan #1 (tidak ada pengecualian di repo ini).** Riset dulu: modul `/tokoh` di web (`client/src/pages/tokoh/Tokoh.jsx`, `FormInputTokoh.jsx`) ternyata entitas SURVEI STANDALONE tanpa linkage DPT dan tanpa konsep kategori sama sekali (field: nama_lengkap/no_telpon/dusun/desa/kecamatan/jenis_kelamin/pekerjaan) — TIDAK bisa dijadikan referensi untuk permintaan ini, beda konsep total. Backend `/Users/asdarsaid/JSI/api` juga tidak ada modul `tokoh`. Canvas Claude Design user (project `160ea937-2f8a-4ff4-9977-f8bc398c90a0`, di-cek ulang via `DesignSync`) cuma punya "Tokoh"/"Tokoh Masyarakat" sebagai LABEL shortcut grid/drawer — tidak ada artboard visual untuk screen atau bintangnya. 2 pertanyaan diajukan eksplisit ke user sebelum coding apapun (Aturan #1):
  1. **Referensi desain → user pilih "saya siapkan desainnya dulu"** (BUKAN opsi (c) izin build tanpa referensi, beda dari preseden Rival Caleg/Saksi/Budgeting-sebelum-desain-ketemu) — jadi sesi ini **TIDAK membangun screen/component apapun** untuk fitur ini. Ditunda sampai user kasih screenshot/Figma/canvas.
  2. **Pola interaksi (direkam untuk dipakai NANTI begitu desain masuk) → icon bintang langsung di `DptCard`** (selalu terlihat di pojok kartu, tap → buka picker kategori), BUKAN lewat menu "..." `DptVoterActionSheet` (beda dari pola "Tandai ikut Door To Door" yang baru dibangun).
  3. **4 kategori tokoh yang diminta user** (custom, bukan dari observasi web manapun): **Tokoh Agama, Tokoh Adat, Tokoh Pemuda & Organisasi, Tokoh Ahli/Profesi.**
  - Rencana implementasi begitu desain tersedia sudah dicatat di `build-plan.md` § Fase Berikutnya (mock service standar `services/tokoh.ts`, entry point shortcut "Tokoh" di `HomeScreen.tsx` yang sudah ada icon-nya tapi masih `handleNotImplemented`) — sesi berikutnya tinggal cek apakah `context/designs/` sudah ada file baru sebelum mulai, jangan asumsi masih kosong (pola sama pesan yang dicatat untuk Budgeting Kampanye sebelumnya).
- 2026-08-22 — **User upload `tokoh1.png`/`tokoh2.png` — TokohMasyarakatScreen (bagian summary/filter) SELESAI diimplementasi, mock data, belum dikonfirmasi user bekerja di device.** Sebelum build: `tokoh2.png` (harusnya bagian LIST) ternyata **file duplikat persis dari tokoh1.png** (dicek `md5`, hash identik) — dikonfirmasi ke user via pertanyaan eksplisit, user pilih **"generate bagian summary/filternya saja dulu"** (bukan tunggu upload ulang, bukan juga rancang list sendiri) — jadi bagian LIST **TIDAK dibangun** sesi ini, murni screen summary/filter dari `tokoh1.png`.
  1. **Riset dulu ke web `client/src/pages/tokoh/`** (`Tokoh.jsx`/`FormInputTokoh.jsx`) sebagai kemungkinan referensi backend/schema (Aturan #6) — ternyata entitas survei STANDALONE tanpa DPT/kategori/dukungan sama sekali (field: nama_lengkap/no_telpon/dusun/desa/kecamatan/jenis_kelamin/pekerjaan, POST `/tokoh`). TIDAK dipakai — konsep beda total dari yang diminta user, dan tidak ada modul `tokoh` di `/Users/asdarsaid/JSI/api` juga. Fitur ini murni baru, mock service standar (pola sama Rival Caleg/Saksi/Real Count).
  2. **5 kategori tokoh di mockup (Tokoh Agama/Pemuda/Adat/Pendidikan/Perempuan) BEDA dari 4 kategori yang disebut user secara VERBAL sebelum desain di-upload** (Agama/Adat/Pemuda & Organisasi/Ahli-Profesi) — diambil APA ADANYA dari mockup (visual menang atas deskripsi verbal begitu ada contoh nyata, pola sama Budgeting Kampanye). Lihat `types/tokoh.ts` § `TOKOH_KATEGORI_OPTIONS` — gampang dikoreksi di satu tempat kalau ternyata user maksudnya tetap 4 kategori yang disebutkan di awal.
  3. **"Filter Alamat Tokoh" (mockup: breadcrumb 3-level Provinsi›Kabupaten›Kecamatan, data placeholder "Jawa Barat › Bandung Barat › Cileu...") disederhanakan jadi 1 level Kecamatan** — keputusan mandiri (bukan ditanya user, kategori sama dengan preseden `TimsesBreadcrumb` yang juga memangkas level Kabupaten karena tidak ada data pendukung nyata). Data placeholder di mockup ("Jawa Barat") tidak match geografi manapun yang sudah ada di app — 18 tokoh mock disebar di 4 kelurahan yang SAMA dengan `MOCK_DTDOOR`/`MOCK_TIMSES` (Cileunyi Kulon/Cinunuk/Cileunyi Wetan/Cimekar, Kec. Cileunyi) supaya konsisten lintas fitur.
  4. **Search "Cari nama tokoh..." dirender tapi belum fungsional** (tidak ada list untuk difilter di screen ini) dan **tombol "+ Identifikasi Tokoh Baru" non-fungsional** (`Alert` "Segera hadir") — keduanya menunggu: (a) `tokoh2.png` yang benar (list), (b) keputusan alur "identifikasi tokoh baru" (dari DPT via bintang di `DptCard`, seperti yang sempat dibahas sebelum tokoh1.png ditemukan — ATAU form manual terpisah seperti di mockup ini — belum diputuskan ulang setelah desain masuk).
  - Verifikasi non-visual: `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 1035ms index.ts (1704 modules)`, HTTP 200, dikonfirmasi memuat module baru `TokohMasyarakatScreen`/`TokohSummaryCard`/`TokohKategoriRow`/`TokohKelurahanBar`/`useTokohList`/`fetchTokohList`). **Belum diverifikasi visual/fungsional di device.**
- 2026-08-22 — **User upload `context/designs/tokohlist.png` (referensi list yang benar, BEDA dari `tokoh1.png`/`tokoh2.png` duplikat sebelumnya) + minta 2 hal sekaligus: (1) bangun bagian "Daftar Tokoh", (2) alur "Identifikasi Tokoh Baru" dari DPT pakai icon bintang di `DptCard`. KEDUANYA SELESAI diimplementasi, mock data, belum dikonfirmasi user bekerja di device.**
  1. **`tokohlist.png` ternyata bagian LANJUTAN scroll dari screen yang SAMA dengan `tokoh1.png`** (header/search/filter yang tampil di kedua file adalah elemen YANG SAMA, bukan diulang di 2 screen terpisah) — dikonfirmasi cocok dengan penjelasan awal user ("1 halaman, cuma di-export 2 file karena kepanjangan"). Section "Daftar Tokoh" (heading+`"N hasil"` + list `TokohCard`) ditambahkan LANGSUNG ke `TokohMasyarakatScreen` yang sudah ada (bukan screen baru) — di bawah "Sebaran per Kelurahan", di atas footer "+ Identifikasi Tokoh Baru".
  2. **`Tokoh` type dapat 2 field baru dari `tokohlist.png`** yang tidak ada di `tokoh1.png`: `alamat` (jalan+RT/RW, free text) dan `pengaruh` ("Rendah"/"Sedang"/"Tinggi" — mockup cuma contohkan "Tinggi"/"Sedang", "Rendah" ditambah sendiri untuk 3-tier lengkap, pola sama `AncamanLevel`). 18 data mock diperluas dengan kedua field ini.
  3. **Search "Cari nama tokoh..." SEKARANG fungsional** — cuma memfilter "Daftar Tokoh", SENGAJA TIDAK ikut mengubah kartu ringkasan/kategori/kelurahan (tetap scoped murni oleh kecamatan) — keputusan mandiri, supaya angka KPI tidak berubah-ubah cuma karena mengetik nama.
  4. **Alur "Identifikasi Tokoh Baru dari DPT" (permintaan eksplisit user "pake icon bintang saja di card list dpt"):** `DptCard` dapat icon bintang baru (prop `onStarPress`, di sebelah kiri tombol "...") — `star`/`warning` (terisi) kalau `sudahTokoh`, `star-outline`/`text-muted` (kosong) kalau belum. Tap → push `TokohFormScreen` (screen BARU, form generik Input/Select/Button — TIDAK ada mockup untuk form ini sendiri, field diturunkan dari model `Tokoh` yang sudah terbukti dari `tokoh1.png`/`tokohlist.png`, pola sama `RivalCalegFormScreen`) ter-prefill Nama/Alamat/Kecamatan/Kelurahan dari record DPT (pola sama persis "Form Door To Door terintegrasi DPT" sebelumnya — linkage via prefill+flag, BUKAN via idDpt karena Tokoh tidak API-linked ke DPT sama sekali, murni identitas). Submit sukses → `useMarkDptTokoh(kabWilId)` flip `DptRecord.sudahTokoh` jadi `true` (field BARU di `types/dpt.ts`, murni client-side, sama pola `markDptDtdoor`).
  5. **`TokohFormScreen` di-reuse (component sama) di `HomeStack` (tombol "+ Identifikasi Tokoh Baru" `TokohMasyarakatScreen`, params kosong) DAN `DptStack` (icon bintang `DptCard`, params `{ dptRecord, kabWilId }`)** — pola identik `DtdoorFormScreen` (param list lokal `TokohFormParams`, tidak terikat ke satu stack).
  6. **Kecamatan/Kelurahan/Desa di `TokohFormScreen` free-text** (BUKAN reuse picker `idKec`/`idKel` DPT) — keputusan mandiri, konsisten dengan model `Tokoh` yang sejak awal (`tokoh1.png`) sudah pakai kecamatan/desa string bebas (sama pola dengan `kecamatan`/`desa` di `Dtdoor`), bukan hierarki wilayah terstruktur seperti DPT.
  - Verifikasi non-visual: `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 1020ms index.ts (1703 modules)`, HTTP 200, dikonfirmasi memuat module baru `TokohFormScreen`/`TokohCard`/`useCreateTokoh`/`useMarkDptTokoh`/`markDptTokoh`/`createTokoh`/`sudahTokoh`). **Belum diverifikasi visual/fungsional di device** — user perlu tes: (1) buka "Tokoh Masyarakat" dari Home → scroll ke bawah "Sebaran per Kelurahan" → section "Daftar Tokoh" + "N hasil" + kartu tokoh (avatar inisial, nama, badge dukungan, kategori+pengaruh, alamat, desa+basis massa) tampil, (2) ketik di "Cari nama tokoh..." → list kefilter live, kartu ringkasan TIDAK ikut berubah, (3) tap "+ Identifikasi Tokoh Baru" → form kosong terbuka → isi semua field wajib → submit → kembali ke list, tokoh baru muncul paling atas "Daftar Tokoh" (dan ikut masuk hitungan ringkasan/kategori/kelurahan), (4) drill ke DPT (BANTAENG seperti biasa) → kartu pemilih sekarang ada icon bintang (outline abu-abu) di sebelah "..." → tap → form "Identifikasi Tokoh Baru" terbuka dengan Nama/Alamat/Kecamatan/Kelurahan SUDAH terisi dari data pemilih itu → lengkapi Kategori/Pengaruh/Dukungan/Basis Massa → submit → kembali ke list DPT, bintang di kartu itu SEKARANG terisi (warna oranye/gold).
- 2026-08-22 — **Permintaan "hilangkan navbottom di halaman Tokoh Masyarakat/Budgeting/Rival Caleg" dikerjakan.** Pola sembunyikan tab bar sudah ada preseden di `CustomerServiceScreen` (Feature "Customer Service", inline `navigation.getParent()?.setOptions({tabBarStyle:{display:"none"}})` + restore saat unmount) — kali ini dipakai di 3 screen sekaligus, jadi **diekstrak jadi hook `useHideTabBar()`** (`src/hooks/useHideTabBar.ts`) supaya constant restore default (`{ backgroundColor: "#ffffff", borderTopColor: "#cbd5e1" }`, HARUS sama persis dengan `AdminTabs.tsx`/`TimsesTabs.tsx`) tidak disalin manual di 4 file — beda dari duplikasi kecil lain di codebase ini yang sengaja TIDAK diabstraksi (di sini ada risiko salah-ketik constant correctness-critical, bukan cuma soal DRY). `CustomerServiceScreen` ikut di-refactor pakai hook yang sama (bukan cuma 3 screen baru), tidak ada 2 pola berbeda untuk hal yang identik.
  - Dipasang di `TokohMasyarakatScreen`, `BudgetingKampanyeScreen`, `RivalCalegListScreen` (masing-masing screen ROOT/entri dari fiturnya, dipush dari Akses Cepat Home) — sub-screen di dalamnya (`TokohFormScreen`, `BudgetTransactionFormScreen`, `RivalCalegForm/Detail/AssessmentForm`) TIDAK perlu dipasang terpisah, karena tab bar sudah disembunyikan oleh screen root yang tetap mounted di bawahnya (stack navigator tidak unmount screen saat di-push ke atas) — begitu balik ke Home (screen root ikut ter-unmount), tab bar otomatis kembali.
  - Verifikasi non-visual: `npx tsc --noEmit` bersih, bundle iOS berhasil (`iOS Bundled 825ms index.ts (1708 modules)`, HTTP 200, `useHideTabBar` dikonfirmasi termuat 16x di bundle). **Dikonfirmasi user bekerja di device (2026-08-22)** — tab bar bawah hilang di ketiga screen sesuai permintaan.
- 2026-08-22 — **Fitur baru "Quick Count" dikerjakan (permintaan eksplisit user via DesignSync: "tiru khusus quick count saja").** Project Claude Design user ("Desain Mobile JSI Dashboard", 160ea937-2f8a-4ff4-9977-f8bc398c90a0) di-re-fetch — ketemu artboard BARU sejak fetch terakhir sesi ini: "12 · SAKSI & QUICK COUNT (SEKUNDER — DRAWER)" (project sekarang 14 artboard, sebelumnya 13 — konfirmasi project ini terus di-update user di luar sesi Claude Code). Satu artboard ini menggabungkan 2 konsep (manajemen Saksi + Quick Count) — **cuma Quick Count yang dibangun sesuai instruksi eksplisit user**, manajemen Saksi diabaikan (sudah ada `SaksiTpsScreen`/`SaksiFormScreen` sendiri, tidak disentuh).
  1. **Mock service baru `services/quickcount.ts` + `types/quickcount.ts`** — 5 kandidat (nama+partai persis mockup: H. Rahmat Wijaya/PNB, Siti Marlina/PHR, Agus Permana/PKB, Tatang Suryana/PAS, Lainnya) + 5 TPS (nama/kelurahan/saksi/DPT/status/meta persis `qcTpsList` di mockup, kelurahan reuse geografi Cileunyi Kulon/Cinunuk/Cileunyi Wetan/Cimekar yang sudah dipakai fitur lain).
  2. **Beda penting dari mockup: SEMUA angka utama (Suara Masuk, TPS Terlapor, Perolehan Suara Kandidat, C1 Terverifikasi/Selisih) BENERAN di-derive dari data**, bukan statis kayak mockup aslinya (`qcCandidates` di mockup hardcode angka tetap yang tidak terhubung ke `qcTpsList`). Field `hasilC1` di `QuickCountTps` DITAMBAH SENDIRI (tidak ada di mockup, keputusan mandiri) supaya submit C1 baru beneran mengubah total suara kandidat & progress TPS terlapor — dianggap lebih sesuai semangat fitur "Quick Count" (yang intinya harus LIVE/reaktif) daripada replikasi persis angka statis mockup.
  3. **"Suara tidak sah" (32, mock) SENGAJA statis** — mockup tidak punya mekanisme input untuk angka ini di sheet C1 (cuma 5 baris kandidat), pola sama preseden "mock statis" di Budgeting Kampanye.
  4. **Tombol footer "+ Input Hasil C1" diubah dari demo statis (`qcSheetIdx: 2` hardcode di mockup) jadi perilaku nyata**: buka TPS pertama yang belum kirim hasil (fallback TPS pertama kalau semua sudah). Keputusan mandiri.
  5. **Submit hasil C1 selalu set status "Menunggu"** — mockup tidak punya alur review korwil (yang menentukan status akhir Terverifikasi/Selisih), di luar scope sesi ini. TPS "Tanpa saksi" tetap bisa dibuka sheet-nya, sama seperti semua row lain (mockup tidak membedakan perilaku tap per status — `t.openSheet` identik untuk semua baris).
  6. **Scoping wilayah (admin bebas, role lain terkunci ke kecamatan sendiri) diterapkan meski TIDAK ada UI switcher di mockup** (cuma teks statis "Kec. Cileunyi") — keputusan mandiri, kategori sama dengan preseden `KekuatanWilayahScreen`/`TokohMasyarakatScreen`, tidak ditanya ulang ke user.
  7. Entry point: shortcut baru "Quick Count" (`icon="stats-chart-outline"`) di grid Akses Cepat `HomeScreen`, route `QuickCount` di `HomeStack`. `useHideTabBar()` dipasang (keputusan mandiri, konsisten pola dashboard+footer-button yang baru ditetapkan untuk Tokoh/Budgeting/Rival Caleg di atas).
  - Verifikasi non-visual: `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 899ms index.ts (1715 modules)`, HTTP 200, dikonfirmasi memuat module baru `QuickCountScreen`/`QuickCountTpsCard`/`QuickCountKandidatRow`/`QuickCountInputSheet`/`useQuickCountTpsList`/`submitQuickCountHasil`/`MOCK_KANDIDAT`). **Belum diverifikasi visual/fungsional di device** — user perlu tes: (1) buka "Quick Count" dari Home → tab "Hasil Live" aktif default, kartu gelap "Suara masuk" + "TPS terlapor 3/5" + progress bar 60% tampil, (2) "Perolehan Suara Kandidat" — kandidat teratas warna biru, sisanya abu, "Suara tidak sah 32 suara" di bawah, (3) 2 kartu "C1 terverifikasi: 1" (hijau) / "Selisih perlu cek: 1" (merah) + banner kuning "1 TPS terdeteksi selisih data", (4) tap "Status TPS" → search bar + 5 kartu TPS dengan badge status warna sesuai, (5) tap kartu TPS mana pun (atau "+ Input Hasil C1" di footer) → sheet "Input Suara" terbuka, kalau TPS itu sudah pernah kirim C1 nilai ter-isi otomatis, kalau belum kosong → isi angka → "Total suara sah" auto-update → "Kirim Hasil C1" → sheet tutup, kartu TPS itu badge-nya jadi "Menunggu" dan angka di tab "Hasil Live" ikut berubah.
- 2026-08-22 — **Fitur baru "Verifikasi Kunjungan / Anti-Fraud" dikerjakan (permintaan eksplisit user via DesignSync).** Project canvas di-re-fetch lagi — ketemu artboard BARU lain sejak fetch Quick Count sebelumnya: "14 · VERIFIKASI KUNJUNGAN / ANTI-FRAUD (SEKUNDER — DRAWER)" (project terus bertambah artboard-nya, user aktif menambah desain baru di luar sesi Claude Code — sekarang ada 15 artboard termasuk Profile).
  1. **User eksplisit minta: "generate UI-nya saja dulu tidak apa apa, nanti saya buatkan API-nya"** — beda dari pola SEMUA fitur mock sebelumnya di app ini (Rival Caleg/Saksi/Real Count/Tokoh/Quick Count, dst.) yang selalu dapat mock service dengan fungsi CREATE/mutation supaya interaksinya "hidup". Fitur ini **SENGAJA cuma 1 fungsi fetch READ** (`services/antifraud.ts` § `fetchAntiFraudSnapshot`) — TIDAK ADA fungsi approve/reject/create. Ini kebetulan juga PERSIS perilaku kode asli mockup: tombol "Tolak Data" dan "Setujui" di bottom sheet SAMA-SAMA cuma `onClick="{{ closeFraudSheet }}"` (tidak ada logic approve/reject beneran bahkan di sumbernya) — jadi keputusan user & perilaku desain aslinya kebetulan align, tidak perlu penyesuaian tambahan.
  2. **Data mock (4 kasus anomali + 5 relawan + 4 jenis anomali) porting verbatim dari mockup** (nama relawan, target, alasan, checks, dst.) — relawan di list "Skor Kredibilitas" sengaja nama yang SAMA dengan relawan di kasus "Perlu Ditinjau" (Asep Saepudin/Neng Sari/Mira Anggraini/Dedi Kurniawan), konsisten dengan mockup aslinya (bukan kebetulan, data mockup memang didesain begitu).
  3. **Placeholder "Titik GPS input"/"Foto kunjungan" di bottom sheet bukti disederhanakan** jadi kotak `bg-surface-secondary` polos — mockup pakai tekstur garis diagonal CSS + 2 pin marker SVG-style, tidak ada library map/canvas di project ini untuk itu. Preseden SAMA PERSIS dengan simplifikasi "Peta Kekuatan Wilayah" sebelumnya (tekstur diagonal → `bg-surface-secondary` polos).
  4. Entry point: shortcut baru "Anti-Fraud" (`icon="shield-checkmark-outline"`) di grid Akses Cepat `HomeScreen`, route `AntiFraud` di `HomeStack`. `useHideTabBar()` dipasang (pola sama Quick Count/Tokoh/Budgeting/Rival Caleg). TIDAK ADA footer button (beda dari Quick Count) — mockup artboard ini memang tidak punya elemen footer sama sekali.
  - Verifikasi non-visual: `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 952ms index.ts (1719 modules)`, HTTP 200, dikonfirmasi memuat module baru `AntiFraudScreen`/`AntiFraudCaseCard`/`AntiFraudEvidenceSheet`/`AntiFraudJenisRow`/`AntiFraudRelawanRow`/`AntiFraudSummaryCard`/`useAntiFraudSnapshot`/`fetchAntiFraudSnapshot`). **Dikonfirmasi user "sudah sesuai" (2026-08-22).**
  - **Catatan kecil:** setelah build awal, `AntiFraudEvidenceSheet.tsx` sempat berubah beberapa kali di luar edit Claude Code (kemungkinan user mengedit langsung di IDE — file itu terbuka & baris terpilih di editor user) — indentasi berubah jadi tab, dan className tombol footer ("Tolak Data"/"Setujui") berubah dari `flex-row gap-sm pt-xs` + wrapper `flex-1` jadi `pb-sm gap-sm pt-xs` tanpa `flex-row`/`flex-1` di wrapper "Setujui". Sempat ditanyakan ke user apakah ini disengaja — **dikonfirmasi user "sudah sesuai"**, jadi state file saat ini (versi user, bukan versi asli Claude Code) yang final, TIDAK dikembalikan.

---

## Catatan (Notes)

_Workaround, pola yang menyimpang dari context files, hal yang perlu diingat session berikutnya._

- Verifikasi scaffold Phase 1: `npx tsc --noEmit` bersih, `npx expo start` (mode `CI=1`) boot Metro tanpa error, request manual ke `/index.bundle?platform=ios&dev=true` berhasil (`iOS Bundled 4642ms index.ts (1448 modules)`, HTTP 200). Tidak ada simulator/device fisik di environment ini — user perlu jalankan `npm run ios`/`npm run android` sendiri untuk verifikasi visual.
- `RootNavigator.tsx` saat ini pakai `useState<Session>(null)` yang di-hardcode — ini SENGAJA untuk membuktikan wiring navigasi jalan, bukan bug. Feature 02 (Auth) yang akan menggantinya dengan session asli dari `expo-secure-store` + fetch profile.
- **Feature 02 selesai diimplementasi (2026-08-21), verifikasi non-visual lolos:** `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 1572ms index.ts (1616 modules)`, HTTP 200 dari `/index.bundle?platform=ios&dev=true`). **Belum diverifikasi visual/fungsional** — tidak ada simulator/device fisik di environment build (sama seperti Phase 1). User perlu jalankan `npm run ios`/`npm run android` sendiri dan tes: (1) login dengan akun asli di `https://api.demokrasikreatif.org` → cek pesan error kalau salah (backend selalu balas 500, lihat Decisions), (2) tolak izin GPS → banner harus merah "Lokasi ditolak" & tombol "Masuk" disabled, izinkan → banner hijau & tombol aktif, (3) login sukses → harus lompat ke Admin/Timses tabs sesuai role, (4) restart app → sesi harus tetap login (tidak balik ke Login screen).
- `RootNavigator.tsx` sekarang baca `useAuth()` (dari `src/hooks/useAuth.tsx`) — `AuthProvider` dipasang di `App.tsx` membungkus `RootNavigator`. Kalau ada test/screen baru yang butuh `useNavigation` di luar `NavigationContainer`, ingat `AuthProvider` ada DI LUAR `NavigationContainer` (dipasang oleh `RootNavigator` sendiri), bukan di dalam.
- **Verifikasi ulang setelah downgrade SDK 57→54 (2026-08-21):** seluruh kode Feature 02 (LoginScreen, useAuth, useLogin, GpsStatusBanner, Button, Input, dst. — semua ditulis sebelum downgrade) tetap `npx tsc --noEmit` bersih dan bundle iOS sukses (`iOS Bundled 5665ms index.ts (1556 modules)`, HTTP 200) setelah downgrade + fix dedup NativeWind. Tidak ada perubahan kode aplikasi (`src/**`) yang diperlukan — murni perubahan dependency + `babel.config.js`. Tetap **belum ada verifikasi visual/device fisik** di environment ini; poin verifikasi manual untuk Feature 02 di atas masih berlaku sama persis di SDK 54.
- **Feature 03 (Profile) selesai diimplementasi (2026-08-22), verifikasi non-visual lolos:** `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 827ms index.ts (1564 modules)`, HTTP 200 dari `/index.bundle?platform=ios&dev=true`). **Belum diverifikasi visual/fungsional** — tidak ada simulator/device fisik di environment build. User perlu jalankan `npm run ios`/`npm run android` sendiri dan tes (role timses via akun demo `yayat.hidayat`/`timses123`, role admin via `admin.jsi`/`admin123` — mode mock aktif, lihat `api-standards.md`):
  1. Buka tab "Profil" (icon person, sebelah tab "Home") → avatar+inisial, nama, badge role tampil, field Nama Lengkap/No. HP/Wilayah Tugas terisi data (mock)
  2. Field "Email" harus terlihat non-aktif (bg abu-abu, tidak bisa diketik) dengan placeholder "Belum didukung backend"
  3. Ubah Nama Lengkap/No. HP → tekan "Simpan Perubahan" → muncul pesan "Perubahan tersimpan.", data tetap setelah screen di-refresh/re-focus
  4. Toggle "Notifikasi" bisa di-tap on/off bebas; toggle "Lokasi GPS Aktif" harus terlihat non-aktif (tidak bisa di-tap) dan nilainya sesuai izin GPS asli di device
  5. Tekan "Keluar" → muncul konfirmasi `Alert` → pilih "Keluar" → kembali ke Login screen, sesi hilang (restart app tetap di Login)
  6. Ulangi login dengan akun beda role (admin vs timses) → badge & teks "Akses Anda sebagai..." di info card berubah sesuai role (admin: "mencakup seluruh kabupaten", timses: dibatasi ke kecamatan)
- **Feature 04 (Program Pemenangan — Door To Door) selesai diimplementasi (2026-08-22), verifikasi non-visual lolos:** `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 850ms index.ts (1579 modules)`, HTTP 200 dari `/index.bundle?platform=ios&dev=true`). **Belum diverifikasi visual/fungsional** — tidak ada simulator/device fisik di environment build. User perlu jalankan `npm run ios`/`npm run android` sendiri dan tes (mode mock aktif):
  1. Buka tab "Program" (icon megaphone, antara Home dan Profil) → segment "Door To Door" aktif default → card ringkasan tampil angka "X kunjungan tercatat" + list 3 kunjungan mock (dengan badge kategori, alamat, PIC)
  2. Tap segment "Social Event" → tampil placeholder teks (bukan blank/crash) — fitur ini Feature 05
  3. Tap "+ Input Kunjungan Baru" → form terbuka (native header "Input Kunjungan Baru"), field "Nama Relawan" sudah ter-isi otomatis dari nama akun yang login
  4. Submit hanya isi "Nama Lengkap" + "Jumlah Wajib Pilih" (2 field wajib) → kembali ke list → kunjungan baru muncul di paling atas
  5. Coba dropdown Kategori/Program Bantuan (1/2/3)/Jenis Kelamin → modal bottom-sheet terbuka, bisa pilih & checkmark muncul di opsi terpilih
  6. Pull-to-refresh di list → tidak error
- **Feature 05 (Program Pemenangan — Social Event/GOTV) selesai diimplementasi (2026-08-22), verifikasi non-visual lolos:** `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 898ms index.ts (1588 modules)`, HTTP 200 dari `/index.bundle?platform=ios&dev=true`). **Belum diverifikasi visual/fungsional** — tidak ada simulator/device fisik di environment build. User perlu jalankan `npm run ios`/`npm run android` sendiri dan tes (mode mock aktif):
  1. Buka tab "Program" → tap segment "Social Event" → card ringkasan "X kegiatan tercatat" + list 2 kegiatan mock ("Sosialisasi Program Kesehatan", "Bagi Sembako") tampil, TANPA badge status (disengaja, lihat Decisions)
  2. Tap "+ Input Kegiatan Baru" → form terbuka (native header "Input Kegiatan Baru"), coba submit kosong → semua 7 field wajib harus tampilkan error validasi
  3. Isi 7 field wajib → submit → kembali ke list → kegiatan baru muncul di paling atas
  4. Bandingkan dengan tab "Door To Door" (Feature 04) → pastikan switch antar tab tidak saling ganggu state (list masing-masing independen)
- **Feature 06 (Timses) selesai diimplementasi (2026-08-22), verifikasi non-visual lolos:** `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 844ms index.ts (1598 modules)`, HTTP 200 dari `/index.bundle?platform=ios&dev=true`). **Belum diverifikasi visual/fungsional** — tidak ada simulator/device fisik di environment build. User perlu jalankan `npm run ios`/`npm run android` sendiri dan tes (mode mock aktif, 8 anggota demo lintas 2 kecamatan — Cileunyi & Rancaekek):
  1. Buka tab "Timses" (icon people, antara Program dan Profil) → breadcrumb "Kec. ... › Semua Desa" tampil, 2 kartu ringkasan (Total Anggota gelap, Online putih), list anggota dengan avatar inisial + role/wilayah + dot online/offline
  2. Login sebagai `yayat.hidayat`/`timses123` (role timses, kecamatan "Cileunyi" di mock) → breadcrumb Kecamatan TIDAK bisa di-tap (teks bukan biru), list cuma tampil anggota Cileunyi (5 orang), tidak ada opsi pindah ke Rancaekek
  3. Login sebagai `admin.jsi`/`admin123` (role admin) → breadcrumb Kecamatan BISA di-tap (teks biru `accent`) → buka modal "Pilih Kecamatan" → pilih "Rancaekek" → list & ringkasan berganti ke 3 anggota Rancaekek, breadcrumb desa reset ke "Semua Desa"
  4. Tap segmen "Semua Desa"/"Ds. ..." di breadcrumb → modal "Pilih Desa" terbuka, ada opsi "Semua Desa" di atas list desa dalam kecamatan aktif → pilih salah satu desa → list kefilter cuma anggota desa itu, kartu ringkasan ikut berubah
  5. Ketik di "Cari anggota timses..." (mis. nama sebagian) → list kefilter live, tidak query ulang server
  6. Pull-to-refresh di list → tidak error; keluar-masuk tab (unfocus/focus ulang) → list ikut refetch otomatis (bisa dicek dari network log kalau perlu)
  7. Tap tombol "+" → muncul `Alert` "Segera hadir" (bukan crash/form kosong)
- **Feature 07 (Lacak Relawan / Tracking) selesai diimplementasi (2026-08-22).** ⚠️ **Poin verifikasi navigasi di bawah SUDAH USANG** — ditulis waktu Lacak Relawan masih tab tersendiri; sejak Feature 08 (lihat Decisions "TEMUAN NAVIGASI BESAR") pindah jadi shortcut "Tracking" di Home (grid Akses Cepat), akses tetap admin-only. Poin fungsional map/list/beacon di bawah masih berlaku, cuma cara MASUK ke screen-nya yang beda — lihat verifikasi Feature 08 di bawah untuk langkah masuk yang benar:
  1. ~~Login sebagai admin → tab "Lacak Relawan" (icon peta) muncul antara "Timses" dan "Profil"~~ — LIHAT catatan navigasi di atas, sekarang lewat Home → Akses Cepat → "Tracking" (cuma tampil untuk admin/adminsekret)
  2. Buka screen Lacak Relawan → peta OpenStreetMap tampil (butuh internet), 3 marker berwarna (2 hijau online, 1 abu-abu offline) di sekitar Kec. Cileunyi — tap marker → popup nama + status + link "Buka di Google Maps"
  3. Bottom sheet putih menutupi ±45% bawah peta → "Anggota Tim (8)" + "5 online" (hijau), list 8 anggota dengan dot status + nama + wilayah, bisa di-scroll independen dari peta
  4. Pill "Update berkala" (hijau, ada titik) tampil di kanan atas body — BUKAN "Real-time" (disengaja, lihat Decisions)
  5. Pull-to-refresh di list bawah → tidak error; keluar-masuk screen (back lalu buka lagi) → list refetch otomatis
  6. Cek console/network log (opsional) → request `PATCH /timses/:id` dengan `lat`/`long` device muncul berkala (~30 detik) selama app di foreground, berhenti/tidak spam saat app di-background
- **Restrukturisasi navigasi + Feature 08 (DPT, sebagian, termasuk refactor 3-screen) — verifikasi non-visual lolos (2026-08-22):** `npx tsc --noEmit` bersih di setiap tahap (restrukturisasi navigasi, lalu refactor DPT 3-screen, lalu ganti icon Akses Cepat — 3x clean run terpisah), `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS **dan** Android berhasil tiap tahap (HTTP 200, bundle dikonfirmasi memuat module terbaru — `DptProvinsiScreen`/`DptKabupatenScreen`/`DptListScreen`/`DptStack`/`HomeScreen`/`HomeStack`/`HomeQuickAccessItem`). **Belum diverifikasi visual/fungsional** — tidak ada simulator/device fisik di environment build. User perlu jalankan `npm run ios`/`npm run android` sendiri dan tes (mode mock aktif):
  1. Tab bar sekarang **5 ikon**: Home/DPT/Rekap/Program/Profil (Timses & Lacak Relawan TIDAK muncul di tab bar lagi)
  2. Buka tab "Home" → header custom (hamburger, "JSI", bell — semua `Alert` "Segera hadir" kalau ditekan), welcome card gelap ("Selamat datang, {nama}" + badge role + teks wilayah kalau bukan admin), 2 kartu "Door To Door"/"Social Event" (angka sama dengan yang di tab Program), grid 8 shortcut "Akses Cepat" dengan **ICON** (bukan singkatan teks — document-text/bar-chart/people/megaphone/locate/paper-plane/clipboard/star)
  3. Tap shortcut "DPT" atau "Program" di grid → pindah ke tab yang sesuai (bukan push screen baru). Tap "Timses" → screen Timses terbuka dengan native header (judul "Timses" + back chevron) — BUKAN lagi custom heading. Tekan back → kembali ke Home
  4. Login sebagai admin → shortcut "Tracking" muncul di grid Akses Cepat, tap → screen Lacak Relawan terbuka. Login sebagai `yayat.hidayat`/`timses123` (role timses) → shortcut "Tracking" **TIDAK ADA SAMA SEKALI** di grid (disengaja)
  5. Shortcut "Rekap"/"Broadcast"/"Survey"/"Tokoh" → `Alert` "Segera hadir" (belum diimplementasi)
  6. Buka tab "DPT" → screen **"DPT — Pilih Provinsi"** (heading in-content, search "Cari provinsi...", tab bar tetap terlihat) → cari/tap "SULAWESI SELATAN" (satu-satunya yang datanya lengkap di mock, tampil "6.672.295 pemilih · 24 kabupaten/kota") → push ke screen **"Pilih Kabupaten/Kota"** (native header + back chevron, subtitle "Provinsi SULAWESI SELATAN") → cari/tap "BANTAENG" ("151.952 pemilih · 8 kecamatan") → push ke screen **"DPT"** (native header + back chevron + 2 ikon download/filter kanan atas) → list 7 pemilih tampil, kartu ringkasan gelap "151.952 / BANTAENG", chip abu-abu "SULAWESI SELATAN" & "BANTAENG" di atas list
  7. Tekan back dari screen "DPT" → kembali ke "Pilih Kabupaten/Kota" (bukan ke Provinsi). Tekan back lagi → kembali ke "Pilih Provinsi". Ulangi navigasi ke BANTAENG lagi untuk lanjut tes berikut
  8. Di screen "DPT": tap ikon filter (kanan atas) → modal "Pilih Kecamatan" terbuka → pilih "BISSAPPU" → list tetap 7 (semua data mock ada di kecamatan ini), chip biru "Kec. BISSAPPU ×" muncul, kartu ringkasan berubah jadi "26.255 / Kec. BISSAPPU"
  9. Tap chip "SULAWESI SELATAN" (scope, abu-abu) → langsung lompat balik ke screen "Pilih Provinsi" (bukan modal). Tap chip "BANTAENG" → lompat balik ke "Pilih Kabupaten/Kota". Tap chip biru "Kec. BISSAPPU" → modal kecamatan terbuka lagi (masih modal, disengaja — lihat Decisions). Tap "×" di chip itu → filter kecamatan hilang, list balik ke scope kabupaten
  10. Ketik di "Cari nama pemilih..." (mis. "usman") → list kefilter live tanpa query ulang server
  11. Tap "..." di kartu pemilih, tombol "+ Tambah Data DPT", atau ikon download (2 tempat) → semua `Alert` "Segera hadir" (CRUD belum ada kontrak endpoint, disengaja — lihat Decisions)
  12. Pull-to-refresh di list DPT → tidak error
- **Feature 10 (Kekuatan Wilayah) selesai diimplementasi (2026-08-22), verifikasi non-visual lolos:** `npx tsc --noEmit` bersih di percobaan pertama, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS **dan** Android berhasil (HTTP 200 keduanya, bundle dikonfirmasi memuat module baru — `KekuatanWilayahScreen`/`KekuatanZoneCard`/`KekuatanToggle`/`dtdoorScore`/`useDtdoorAll`). Dikerjakan lewat `/EnterPlanMode` (plan disetujui dulu sebelum kode — lihat Decisions untuk ringkasan keputusan). **Belum diverifikasi visual/fungsional** — tidak ada simulator/device fisik di environment build. User perlu jalankan `npm run ios`/`npm run android` sendiri dan tes (mode mock aktif):
  1. Buka tab "Program" → tab "Door To Door" → tombol baru "Lihat Kekuatan Wilayah" (di bawah "+ Input Kunjungan Baru") → tap → screen "Peta Kekuatan Wilayah" terbuka (native header + back chevron)
  2. Subtitle "Berdasarkan hasil kunjungan Door To Door per kelurahan" tampil, toggle "Peta"/"List" (Peta aktif default, border+bg biru muda)
  3. Mode "Peta": grid kotak warna (hijau=Kuat/oranye=Sedang/merah=Lemah) + legenda 3 warna di bawahnya tampil — 4 kotak untuk 4 kelurahan mock (Cileunyi Kulon, Cinunuk = hijau; Cimekar = oranye; Cileunyi Wetan = merah)
  4. Tap "List" → grid & legenda hilang, cuma 3 kartu ringkasan ("Zona Kuat"/"Zona Sedang"/"Zona Lemah", angka berwarna) + "Detail per Kelurahan" yang tersisa
  5. "Detail per Kelurahan": 4 kartu terurut skor tertinggi dulu — tiap kartu nama kelurahan + "{N} kunjungan · skor rata {X}" + badge tier (hijau/oranye/merah) di kanan
  6. Login sebagai `yayat.hidayat`/`timses123` (role timses, kecamatan Cileunyi di mock) → semua 4 kelurahan tetap tampil (kebetulan semua di Kec. Cileunyi). Kalau mau tes scoping ke kecamatan LAIN butuh data mock tambahan di luar Cileunyi (belum ada) — scoping-nya sendiri (`kecamatan === ownKecamatan`) sudah aktif di kode, cuma belum ada data mock untuk membuktikan secara visual bedanya dengan admin
  7. Pull-to-refresh → tidak error; tekan back → kembali ke tab Door To Door
- **Working tree waktu downgrade ini dikerjakan:** Feature 02 (Auth) sudah selesai diimplementasi tapi **belum di-commit** oleh session sebelumnya (menunggu konfirmasi visual user). Downgrade SDK ini dikerjakan di atas working tree yang sama tanpa mengubah kode Feature 02 — kalau mau commit, pertimbangkan pisahkan jadi 2 commit logis (Feature 02 Auth, lalu downgrade SDK) atau gabung, sesuai preferensi user.
- **Budgeting Kampanye (item #3 saran konsultan politik) selesai diimplementasi (2026-08-22) DAN dikonfirmasi user bekerja di device.** Verifikasi non-visual: `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 1467ms index.ts (1694 modules)`, HTTP 200 dari `/index.bundle?platform=ios&dev=true`, dikonfirmasi memuat module baru `BudgetingKampanyeScreen`/`BudgetTransactionFormScreen`/`BudgetHeroCard`/`BudgetScopeToggle`/`useBudgeting`). Langkah tes manual yang dijalankan user (mode mock aktif):
  1. Buka tab "Home" → grid Akses Cepat ada shortcut baru "Budgeting" (icon dompet) → tap → screen "Budgeting Kampanye" terbuka (native header + back chevron + icon download di kanan atas)
  2. Toggle "Bulan Ini"/"Total Kampanye" → semua angka (kartu gelap ringkasan, 2 stat kecil, 5 pos anggaran, chart tren, label "Mingguan"/"Bulanan") berganti sesuai scope, list "Pengeluaran Terbaru" TIDAK berubah (sama di kedua scope, disengaja — lihat Decisions)
  3. Kartu ringkasan gelap: "Total Anggaran Agustus 2026" Rp 450.000.000, progress bar 71%, "Terpakai Rp 318.500.000 · 71%" & "Sisa Rp 131.500.000" (scope "Bulan Ini")
  4. "Realisasi per Pos Anggaran": 5 baris, "Door To Door" harus tampil warna MERAH (108%, melebihi plafon), "Atribut & Alat Peraga" KUNING (92%, mendekati plafon), sisanya biru
  5. "Tren Pengeluaran Mingguan": 5 batang W1-W5, batang W2/W3/W4 warna navy tua (di atas rata-rata), W1/W5 biru muda
  6. "Pengeluaran Terbaru": 4 transaksi mock, badge status hijau/kuning/merah sesuai Disetujui/Menunggu/Ditolak
  7. Tap "+ Catat Pengeluaran" → form terbuka (native header "Catat Pengeluaran") → coba submit kosong → 3 field wajib (Pos Anggaran/Nominal/Keterangan) harus tampilkan error validasi
  8. Isi semua field → submit → kembali ke screen utama → transaksi baru muncul PALING ATAS di "Pengeluaran Terbaru" dengan badge "Menunggu" (kuning) — status baru selalu "Menunggu", tidak ada opsi ubah status di UI manapun (disengaja, lihat Decisions)
  9. Tap icon download (headerRight) → `Alert` "Segera hadir" (belum diimplementasi)
  10. Pull-to-refresh di screen utama → tidak error
- **08 DPT — CRUD dasar (Tambah/Edit/Hapus) selesai diimplementasi (2026-08-22), verifikasi non-visual lolos:** `npx tsc --noEmit` bersih, `npx expo start` (CI mode) boot Metro tanpa error, bundle iOS berhasil (`iOS Bundled 894ms index.ts (1697 modules)`, HTTP 200 dari `/index.bundle?platform=ios&dev=true`, dikonfirmasi memuat module baru `DptRecordFormScreen`/`DptVoterActionSheet`/`useDptRecordMutations`/`createDptRecord`/`updateDptRecord`/`deleteDptRecord`). **Belum diverifikasi visual/fungsional** — tidak ada simulator/device fisik di environment build. User perlu jalankan `npm run ios`/`npm run android` sendiri dan tes (mode mock aktif, drill ke Provinsi "SULAWESI SELATAN" → Kabupaten "BANTAENG" seperti biasa supaya sampai ke screen "DPT" dengan 7 data mock):
  1. Tap icon "+" (headerRight, kanan atas) → screen "Tambah Data DPT" terbuka (native header + back chevron)
  2. Coba submit kosong → semua field wajib (Nama/Jenis Kelamin/Usia/Alamat/RT/RW/Kecamatan/Kelurahan/No. TPS) harus tampilkan error validasi
  3. Pilih Kecamatan (mis. "BISSAPPU") → Select "Kelurahan/Desa" jadi aktif, opsinya cuma kelurahan dalam Kecamatan itu; ganti Kecamatan → Kelurahan yang sudah dipilih ikut ke-reset
  4. Isi semua field valid → submit → kembali ke list "DPT" → data baru muncul PALING ATAS, badge "Belum D2D"/"Belum SE" (record baru belum pernah ditandai partisipasi apapun)
  5. Tap "..." di salah satu kartu pemilih → bottom sheet terbuka (nama pemilih + 2 baris: "Edit data pemilih", "Hapus data pemilih" merah) — BUKAN lagi `Alert` generik "Segera hadir"
  6. Tap "Edit data pemilih" → screen "Edit Data DPT" terbuka, SEMUA field ter-isi data pemilih itu (termasuk Kecamatan/Kelurahan) → ubah salah satu field (mis. Nama) → submit → kembali ke list, perubahan langsung terlihat di kartu yang sama
  7. Tap "..." lagi di kartu lain → "Hapus data pemilih" → muncul `Alert` konfirmasi "Hapus data pemilih?" (tombol Batal/Hapus) → tekan "Hapus" → kartu itu hilang dari list, jumlah data berkurang 1
  8. Tekan "Batal" di konfirmasi hapus (uji sebaliknya) → tidak terjadi apa-apa, data tetap ada
  9. Data yang ditambah/diedit/dihapus HANYA bertahan selama app berjalan (mock in-memory, bukan `SecureStore`) — restart app akan reset ke 7 data mock awal, ini disengaja (bukan bug)
- 2026-08-22 — **Isu & Aspirasi Warga (fitur baru lain, di luar checklist Phase 1-3) — dikerjakan atas permintaan langsung user** ("saya ingin membuat fitur Isu dan Aspirasi di claude design ini sudah ada ui nya... Generate UI nya saja dulu tidak apa apa, nanti saya buatkan API nya"). Referensi desain: artboard "15 · ISU & ASPIRASI WARGA" di project Claude Design user ("Desain Mobile JSI Dashboard", 160ea937-2f8a-4ff4-9977-f8bc398c90a0), dibaca via `DesignSync` (`list_files`/`get_file`, bukan export manual ke `context/designs/` seperti fitur lain — pertama kali project ini dibaca langsung dari Claude Design API di sesi ini). Pola persis `AntiFraudScreen` (fitur read-only sejenis, permintaan sama persis): `IsuAspirasiScreen` (3 tab Ringkasan/Peta Isu/Aspirasi) + 6 component baru di `src/components/isuaspirasi/` — lihat `ui-registry.md` untuk detail lengkap tiap component. Entry point: shortcut baru "Isu & Aspirasi" (`icon="chatbubble-ellipses-outline"`) di grid Akses Cepat `HomeScreen`, route `IsuAspirasi` di `HomeStack`. `useHideTabBar()` dipasang (pola sama Quick Count/Tokoh/Anti-Fraud). **SENGAJA READ-ONLY** — `services/isuaspirasi.ts` cuma 1 fungsi fetch, tombol "+ Catat Aspirasi Warga" dan aksi di detail sheet ("Tandai Ditindak"/"Jadikan Materi") cuma `Alert` info, tidak ada mutation. Tab "Peta Isu" (mockup: overlay warna dekoratif di atas tekstur peta) disederhanakan jadi list wilayah berwarna — tidak ada library map di project ini, pola sama simplifikasi `AntiFraudEvidenceSheet`. `npx tsc --noEmit` bersih. **Belum diverifikasi visual/fungsional di device** — tidak ada simulator/device fisik di environment build (Expo dev server sudah jalan di session lain, port 8081, Fast Refresh akan otomatis pick up perubahan). User perlu buka Expo Go dan tes:
  1. Home → grid Akses Cepat → shortcut baru "Isu & Aspirasi" (icon chat bubble) → tap → screen "Isu & Aspirasi Warga" terbuka (native header + back chevron, tab bar bawah tersembunyi)
  2. Hero card gelap: "1.284" aspirasi terkumpul, isu dominan "Jalan Rusak" (biru)
  3. Tab "Ringkasan" (default aktif): 6 baris "Isu Terbanyak Disebut Warga" (Jalan Rusak paling atas, bar penuh, merah) + 3 card "Usulan Materi Kampanye" dengan pill prioritas
  4. Tab "Peta Isu": 4 row kelurahan (Cileunyi Kulon/Cinunuk/Cileunyi Wetan/Cimekar) dengan dot warna + badge isu dominan
  5. Tab "Aspirasi": chip filter "Semua/Jalan/Air/Pupuk/Kesehatan" di atas — tap "Jalan" → list ke-filter cuma 2 kartu (Warga RT 03, Pak Dadan); tap "Semua" → balik ke 5 kartu
  6. Tap salah satu kartu aspirasi → bottom sheet terbuka dari bawah (keluhan lengkap, placeholder "Lokasi isu"/"Foto kondisi", info Dicatat oleh/Warga sekitar mengeluh sama/Status)
  7. Tap "Tandai Ditindak" atau "Jadikan Materi" di sheet → sheet tertutup + muncul `Alert` info (tidak mengubah data manapun)
  8. Tap "+ Catat Aspirasi Warga" (footer bawah) → muncul `Alert` "Segera hadir"
  9. Pull-to-refresh di screen utama → tidak error
- 2026-08-22 — **09 Hasil Rekap dimulai atas permintaan langsung user** ("Lanjut ke halaman hasil rekap, saya ingin pada halaman ini mengikuti tahapan yang ada di web"). Karena modul ini belum punya repo backend (CLAUDE.md Aturan #6), "web" di sini dibaca sebagai **alur navigasi/hierarki UI**, bukan kontrak API — dibaca langsung dari `/Users/asdarsaid/JSI/client/src/pages/hasilrekap2024/` (route tree persis di `HasilRekap2024Routes.jsx`), BUKAN dari asumsi/mockup semata (mockup artboard "4 · HASIL REKAP" cuma tunjukkan versi sederhana 2 level). Ditemukan: web punya 2 pohon flow terpisah total berbeda (2019 pakai flat route + localStorage, 2024 pakai nested route params) dan 3 tipe pemilu (DPR RI/DPRD Provinsi/DPRD Kabupaten) dengan **posisi "Dapil" yang beda-beda per tipe** — DPR RI: Dapil di paling atas; DPRD Provinsi: sama (Dapil di atas) tapi ada 1 level TPS ekstra di akhir (TPS-list→TPS-detail); DPRD Kabupaten: Dapil disisipkan SETELAH Kabupaten (Provinsi→Kabupaten→Dapil→Kecamatan→Kelurahan→TPS). User memilih **"tiru yang 2024 saja"**, lalu ditanya eksplisit (scope 3 tipe sekaligus vs 1 tipe dulu, via `AskUserQuestion`) — jawab **"1 tipe dulu — DPR RI"**.
  - **Ditemukan juga (tidak perlu ditanya, observasi langsung kode):** halaman web di level Kabupaten/Kecamatan/Kelurahan (`DprRiProvinsi.jsx` dst.) ternyata bukan sekadar list wilayah — itu **pivot table caleg × sub-wilayah yang sangat padat** (checkbox pembanding tiap caleg, doughnut chart suara partai/caleg/total, kolom dinamis per sub-wilayah), murni pola desktop yang tidak mungkin muat di layar 390px. Keputusan mandiri (bukan ditanya ulang, kategori sama dengan preseden simplifikasi desktop-only lain seperti `AntiFraudEvidenceSheet`/`KekuatanWilayahScreen`): **diadaptasi jadi `HasilRekapPartaiSummaryCard`** (bar per partai, 1 warna netral, reuse bahasa visual "Suara Sah" yang SUDAH ada di artboard mockup) **+ list wilayah untuk drill** (reuse `DptRegionCard`/`DptRegionCardSkeleton`, tidak bikin card baru), dan di level terminal (Kelurahan) ditambah **"Top Caleg"** (ranking flat top-5, bukan pivot penuh) + **"Suara per TPS"** (TPS ditampilkan inline persis web — DPR RI tidak punya route TPS terpisah).
  - **Tidak ada scoping wilayah role-based** — dikonfirmasi dari observasi kode web (`roles` di-destructure dari context tapi tidak pernah dipakai lagi, dead variable di beberapa file Hasil Rekap) — `RekapStack` dipakai identik persis di `AdminTabs`/`TimsesTabs`, semua role bisa browsing bebas dari Dapil manapun.
  - Data: **100% mock deterministik** baru (`services/hasilrekap.ts`) — 8 partai nasional umum (PDIP/Golkar/Gerindra/PKB/NasDem/PKS/Demokrat/PAN) dengan bobot suara tetap + PRNG seeded per scope id (mulberry32 + hash string) supaya angka STABIL saat navigasi bolak-balik (bukan random ulang tiap render/refetch, beda dari fitur mock lain yang datanya statis flat). Pohon wilayah kecil hand-authored: 3 Dapil (Jabar I/II/III), tiap dapil 1-2 kabupaten, tiap kabupaten 1-2 kecamatan — cabang "Bandung Barat → Cileunyi" reuse 4 kelurahan yang sama dengan QuickCount/Tokoh Masyarakat/Isu Aspirasi (Cileunyi Kulon/Cinunuk/Cileunyi Wetan/Cimekar) untuk kontinuitas dataset demo lintas fitur; cabang lain (Cianjur/Garut/Bandung) pakai nama kelurahan generik tapi tetap dapat data lengkap (beda dari DPT yang cuma 1 kabupaten "lengkap", semua cabang Hasil Rekap di sini SAMA lengkapnya karena murni sintetis, bukan porting dari contoh data user).
  - `RekapStack.tsx` baru (mengikuti `AdminTabs`/`TimsesTabs`, dipakai bersama, pola sama `ProgramStack`/`DptStack`) — menggantikan `RekapPlaceholderScreen` inert yang sebelumnya ada di kedua file tab. Root `HasilRekapScreen` (tab, `headerShown:false`) → 3 chip tipe pemilu (DPR RI fungsional, DPRD Provinsi/Kabupaten `Alert` "Segera hadir") → `HasilRekapDapilScreen` → `HasilRekapKabupatenScreen` → `HasilRekapKecamatanScreen` → `HasilRekapKelurahanScreen` → `HasilRekapDetailScreen` (terminal). Title native header tiap level level DINAMIS (nama wilayah yang sedang ditampilkan, bukan judul statis) — pola baru, beda dari `TargetSuara*` yang title-nya selalu statis "Target Suara" di semua level.
  - `npx tsc --noEmit` bersih. **Belum diverifikasi visual/fungsional di device** — tidak ada simulator/device fisik di environment build (Expo dev server jalan di session lain, port 8081, Fast Refresh otomatis pick up). User perlu buka Expo Go dan tes:
    1. Tab "Rekap" → screen "Hasil Rekap" terbuka (heading in-content, TANPA toggle tahun — langsung 3 chip tipe: DPR RI, DPRD Provinsi, DPRD Kabupaten)
    2. Tap "DPRD Provinsi" atau "DPRD Kabupaten" → muncul `Alert` "Segera hadir" (belum diimplementasi sesi ini)
    3. Tap "DPR RI" → screen "Pilih Dapil" terbuka (native header + back chevron) → 3 kartu Dapil (Jabar I/II/III), tiap kartu ada "N kursi · N suara · Unggul {partai}"
    4. Tap "Dapil Jabar I" → screen judul "Dapil Jabar I" terbuka → kartu "Rekap Suara Partai" di atas (8 baris partai, urut dari suara terbesar, partai teratas ada badge "Unggul") → di bawahnya list 2 Kabupaten ("Bandung Barat", "Bandung")
    5. Tap "Bandung Barat" → screen judul "Bandung Barat" → kartu partai (angka BEDA dari level Dapil, lebih kecil) → list 2 Kecamatan ("Cileunyi", "Cililin")
    6. Tap "Cileunyi" → screen judul "Cileunyi" → kartu partai → list 4 Kelurahan (Cileunyi Kulon/Cinunuk/Cileunyi Wetan/Cimekar)
    7. Tap "Cileunyi Kulon" → screen judul "Cileunyi Kulon" (TERMINAL, tidak ada drill lagi) → kartu partai + section "Top Caleg" (5 nama) + section "Suara per TPS" (4 baris, TPS 01-04)
    8. Navigasi mundur (back chevron) ke "Cileunyi" lalu maju lagi ke "Cileunyi Kulon" → angka partai/caleg/TPS HARUS SAMA PERSIS dengan sebelumnya (mock deterministik, bukan acak ulang)
    9. Pull-to-refresh di screen "Cileunyi Kulon" (terminal) → tidak error
    10. Coba tap Dapil lain ("Jabar II"/"Jabar III") → drill sampai ke Kelurahan (cuma 2 kelurahan tiap kecamatan di cabang ini, nama generik seperti "Pamoyanan"/"Sawah Gede") → pastikan tidak crash meski bukan cabang "Cileunyi" yang lebih lengkap
- 2026-08-22 — **Hasil Rekap — "Daftar Caleg" ditambah ke SEMUA level drill** (permintaan langsung user setelah build awal: "Saya ingin kamu menampilkan juga list caleg beserta nama daerahnya seperti ditable di web tapi jangan dibuat table dimobile, buat serelan mungkin"). Sebelumnya section caleg cuma ada di `HasilRekapDetailScreen` (terminal, judul "Top Caleg") — sekarang seragam "Daftar Caleg" muncul di `HasilRekapKabupatenScreen`/`HasilRekapKecamatanScreen`/`HasilRekapKelurahanScreen` juga (via `ListFooterComponent`, di bawah list wilayah). Type `HasilRekapCaleg` dapat field baru `daerahUnggul: string` — nama SATU sub-wilayah (Kabupaten/Kecamatan/Kelurahan/TPS, sesuai scope layar) tempat caleg itu raih suara terbanyak, dipilih deterministik (`generateCalegList()` di `services/hasilrekap.ts`) — ini adaptasi mobile dari kolom pivot caleg × N-sub-wilayah di tabel web (1 daerah paling relevan per caleg, bukan breakdown penuh semua sub-wilayah — user eksplisit minta JANGAN dibuat tabel). `HasilRekapCalegRow` dapat baris baru `text-accent` "Unggul di {daerahUnggul}". `npx tsc --noEmit` bersih. Belum diverifikasi visual di device — tambahan langkah tes: buka screen Kabupaten/Kecamatan/Kelurahan mana saja di alur DPR RI → section "Daftar Caleg" (5 baris) harus tampil di bawah list wilayah, tiap baris ada teks "Unggul di {nama sub-wilayah}" yang relevan dengan level itu (Kecamatan/Kelurahan/dst., bukan "TPS 0X" kecuali di screen terminal Kelurahan/Detail).
- 2026-08-22 — **Hasil Rekap — "Daftar Caleg" diperluas jadi breakdown suara PENUH per caleg** (lanjutan langsung dari entry di atas). User tanya "apakah saya tetap bisa mengikuti pola sama persis dengan yang di web di aplikasi ini?" — dijawab dengan memisahkan 2 hal: kelengkapan DATA (bisa disamakan persis) vs BENTUK tampilan (tabel tetap tidak dipakai, sesuai instruksi sebelumnya). Ditanya eksplisit via `AskUserQuestion` (breakdown penuh + tap-to-detail vs tetap ringkas 1 daerah unggul) → user pilih **breakdown penuh**. Implementasi: `HasilRekapCaleg` dapat field `breakdown: HasilRekapCalegDaerahSuara[]` (suara caleg itu di SETIAP sub-wilayah scope layar, persis kelengkapan kolom pivot web `DprRiProvinsi.jsx` dst., generator baru `generateCalegBreakdown()` di `services/hasilrekap.ts`, bobot acak seeded per sub-wilayah). `HasilRekapCalegRow` jadi `Pressable` (tambah chevron) — tap buka `HasilRekapCalegDetailSheet` (component baru, Modal bottom sheet, list vertikal breakdown + bar per baris, bukan tabel/kolom). Dipasang di ke-4 screen drill (Kabupaten/Kecamatan/Kelurahan/Detail), masing-masing kasih `levelLabel` beda ("Kabupaten/Kota"/"Kecamatan"/"Kelurahan/Desa"/"TPS") supaya judul section breakdown di sheet sesuai konteks. `npx tsc --noEmit` bersih. Belum diverifikasi visual di device — tambahan langkah tes: tap salah satu baris di "Daftar Caleg" mana saja → bottom sheet terbuka dari bawah, judul nama caleg + partai + total suara, lalu "Sebaran suara per {label level}" berisi list SEMUA sub-wilayah di scope itu (bukan cuma 1) dengan bar proporsional, urut dari suara terbesar → tap di luar sheet atau geser turun untuk tutup.
- 2026-08-23 — **Hasil Rekap — TERNYATA ADA repo backend** (`/Users/asdarsaid/JSI/api/src/dpr` + `dprd-kab`/`dprd-pro`/`dpd`), koreksi dari asumsi sebelumnya "belum ada repo backend" (progress-tracker Decisions 2026-08-21, CLAUDE.md Aturan #6). Permintaan eksplisit user: "buat catatan baru untuk response dan type dari endpoint yang digunakan di web itu... bisa kamu pelajari juga di folder /Users/asdarsaid/JSI/api". Kontrak lengkap (route, response shape field-by-field, auth, DTO, base URL) sudah didokumentasikan di **`api-standards.md` § Hasil Rekap** — TIDAK diulang di sini, baca di sana. Ringkasan temuan paling penting: (1) modul ini **open/unauthenticated**, (2) **TIDAK ADA prefix `/api`** (beda dari auth), (3) web frontend **2024** hit host TERPISAH `rekap2024.demokrasikreatif.org` (bukan `VITE_API_URL` yang dipakai modul lain) — **belum dikonfirmasi** apakah host itu memang deployment dari repo `/Users/asdarsaid/JSI/api` yang sama, (4) ada **2 keluarga endpoint berbeda makna** (`hasil-rekap` dapil-first vs `hasil-hitung` wilayah-administratif-murni gaya quick count) — **belum ditentukan mana yang dimaksud fitur mobile ini**, (5) implementasi mobile saat ini (DPR RI, 100% mock) **tidak 1:1** dengan struktur response asli — migrasi nanti butuh reshape data, bukan cuma ganti base URL. **Tidak ada perubahan kode mobile di entry ini** — murni dokumentasi riset, `src/services/hasilrekap.ts` TETAP mock sampai user konfirmasi host + jenis endpoint yang dipakai.
- 2026-08-23 — **Verifikasi ulang branch repo `/Users/asdarsaid/JSI/api`** (user khawatir riset di atas "tadi masih di branch yang lama"): dicek `git reflog` — checkout `persiapan-2029` tidak pernah berubah sepanjang riset. `git fetch --all --prune` lalu bandingkan tanggal commit semua branch: `persiapan-2029` (=`new-update`, 2025-08-19) TERBUKTI branch TERBARU, lebih baru dari `master` (2025-05-30). Diff `master` vs `persiapan-2029` di `src/dpr*` menunjukkan beda nyata (family `hasil-hitung` DPR RI tidak ada di `master`, fallback axios di `hasilRekap().getJsons()` juga baru) — SEMUA sudah masuk catatan `api-standards.md` karena memang dibaca dari `persiapan-2029`. Baca ulang langsung `dpr.controller.ts`/`dpr.service.ts`/`dprd-kab.controller.ts`/`dprd-pro.controller.ts` dari disk (bukan cuma percaya laporan agent sebelumnya) — cocok persis, **tidak ada koreksi**. Catatan verifikasi ditambahkan di `api-standards.md` § Hasil Rekap.
- 2026-08-23 — **09 Hasil Rekap (DPR RI) — WIRED ke backend real, mock dimatikan.** Permintaan eksplisit user: "kita lanjut integrasi ke endpoint realnya tanpa mock_api lagi dengan real menggunakan url https://api.demokrasikreatif.org". Dikerjakan via `/plan` (Explore → Plan agent → `AskUserQuestion` → plan file → approve → implement):
  1. **Base URL dikonfirmasi user** = `https://api.demokrasikreatif.org` (host utama, BUKAN `rekap2024.demokrasikreatif.org` yang dipakai web 2024 — pertanyaan terbuka dari riset sebelumnya jadi terjawab). **Diverifikasi LIVE via curl** (bukan cuma baca kode) ke seluruh 5 level drill DPR RI sebelum kode ditulis: `/dpr/hasil-rekap` (200, 80 dapil real termasuk "ACEH I") → `/dpr/hasil-rekap/1101` (200) → `/1101/2` (200, "ACEH SELATAN") → `/1101/2/3` (**201**, "BAKONGAN" — bug HTTP status di backend terkonfirmasi live, tidak butuh penanganan khusus karena axios resolve semua 2xx sama) → `/1101/2/3/4` (200, "KEUDE BAKONGAN", TPS 1-N inline). Cloudflare **tidak** memblokir endpoint ini (beda dari auth yang pernah diblokir).
  2. **Keluarga endpoint yang dipakai: `hasil-rekap`** (dapil-first) — cocok 1:1 dengan hierarki yang sudah dibangun. `hasil-hitung` (wilayah-administratif-murni, gaya quick count) TIDAK dipakai.
  3. **Ditemukan: endpoint ini TIDAK pakai prefix `/api`** (`.../api/dpr/hasil-rekap` → 404 "Cannot GET"; `.../dpr/hasil-rekap` tanpa `/api` → 200) — beda dari `apiClient` yang sudah ada (`EXPO_PUBLIC_API_URL` = `.../api`). Solusi: axios client BARU `rekapApiClient` (`src/lib/api/client.ts`), base URL dari env var baru `EXPO_PUBLIC_REKAP_API_URL=https://api.demokrasikreatif.org`, tanpa interceptor (endpoint tidak ada auth guard, dikonfirmasi sebelumnya).
  4. **Scope flag global `EXPO_PUBLIC_USE_MOCK_API` ditanya eksplisit ke user** (`AskUserQuestion`, karena flag ini dipakai 16 service, cuma 5 — auth/dtdoor/gotv/profile/timses — yang punya cabang real) → user pilih **"Matikan global sekarang"**. `.env` diubah jadi `false` — **konsekuensi diterima user**: 9 fitur mock-only lain (Budgeting, Saksi, Anti-Fraud, Quick Count, Isu Aspirasi, DPT, Rival Caleg, Real Count, Tokoh) sekarang akan tampil "gagal memuat / Coba Lagi" (bukan crash, cuma kehilangan demo-ability) sampai masing-masing di-wire nanti.
  5. **Reshape data**: response asli nested caleg DI DALAM tiap grup partai (`partai_calegs[].calegs[].{kabupatens|keluarahans|tps}[]`, key array berbeda nama per level — ada typo `keluarahans` yang sebenarnya bukan cuma soal ejaan tapi field yang SAMA-SAMA dipakai untuk level kecamatan maupun kelurahan tergantung endpoint), sedangkan mobile mau flat top-5 caleg lintas partai + list wilayah teragregasi. Diselesaikan 1 helper generic `buildFromPartaiCalegs<K>()` di `services/hasilrekap.ts` — akumulasi suara per id wilayah lintas semua partai×caleg (`Map`-based, sekalian hitung `partaiUnggul` per wilayah dari partai dengan kontribusi suara terbesar di situ), plus flatten semua caleg lintas partai lalu ambil top-5 by total suara (breakdown penuhnya = nested array caleg itu sendiri, sudah otomatis lengkap). `partaiSuara` (ringkasan bar partai) diambil LANGSUNG dari `partai_calegs[]` (sudah bawa `suaraPartai`/`suaraCalegs`/`suaraTotal`) — sengaja TIDAK pernah baca field top-level kedua (`partai`/`partais`) yang penamaannya tidak konsisten antar level, jadi inkonsistensi itu otomatis tidak relevan buat mobile.
  6. **Field `kursiTerakhir` dihapus** dari `HasilRekapDapil` (`types/hasilrekap.ts`) — field ini cuma ada di mock lama, tidak pernah ada di response asli (backend cuma punya `jmlKursi`+`hargaKursi`), dan dikonfirmasi tidak pernah dirender di UI manapun sebelum dihapus.
  7. **Mock TETAP dipertahankan** (`DAPIL_TREE`, generator PRNG, dst. di `services/hasilrekap.ts` tidak dihapus) — pola sama semua service real-wired lain (dtdoor/gotv/dst.), supaya `EXPO_PUBLIC_USE_MOCK_API=true` masih bisa dipakai untuk demo offline kapan pun dibutuhkan.
  8. **Zero perubahan** di `hooks/useHasilRekap.ts` maupun ke-6 screen/ke-5 component Hasil Rekap — signature fungsi service tidak berubah, ini murni penggantian data-layer.
  9. `npx tsc --noEmit` bersih. **Belum diverifikasi di device asli** — langkah tes wajib (urutan penting):
     1. **Login dulu** — belum ada verifikasi independen apakah Cloudflare masih blokir `POST /api/auth/login` khususnya (beda dari `/dpr/...` yang sudah terbukti terbuka). Kalau login gagal, app tidak bisa dipakai sama sekali — laporkan sebelum lanjut.
     2. Tab "Rekap" → "DPR RI" → list Dapil REAL (mis. "ACEH I", "ACEH II"... bukan lagi "Dapil Jabar I/II/III")
     3. Drill sampai ke Kelurahan (terminal) → "Daftar Caleg" berisi nama caleg SUNGGUHAN + "Suara per TPS" real
     4. Tap caleg → sheet detail breakdown real per sub-wilayah
     5. Pull-to-refresh → tidak error
     6. Cek salah satu fitur mock-only lain (mis. Budgeting) → harus tampil "gagal memuat/Coba Lagi", BUKAN crash
- 2026-08-23 — **User laporkan error 404 massal setelah mock dimatikan** (`dtdoor`/`gotv`/`profile` di console). Investigasi ketemu 2 masalah berbeda:
  1. **Base URL SALAH untuk SELURUH app, bukan cuma modul `dpr`.** Asumsi "auth butuh prefix `/api`" (didokumentasikan Feature 02, 2026-08-21) ternyata **sudah tidak berlaku** — dikonfirmasi live: `POST https://api.demokrasikreatif.org/auth/login` (tanpa `/api`) balas 422 validation (route ADA), `.../api/auth/login` balas 404 "Cannot POST" (route TIDAK ADA). Sama untuk `dtdoor`/`gotv` (`/dtdoor/count`, `/gotv/count` tanpa `/api` → 200; dengan `/api` → 404). **Fix:** `.env` → `EXPO_PUBLIC_API_URL=https://api.demokrasikreatif.org` (tanpa `/api`). Konsekuensi desain: `rekapApiClient` yang baru dibuat sesi sebelumnya jadi REDUNDAN (host-nya sama persis dengan `apiClient` sekarang) — **dihapus**, `services/hasilrekap.ts` balik pakai `apiClient` biasa, `EXPO_PUBLIC_REKAP_API_URL` dihapus dari `.env`/`.env.example`. Kemungkinan penyebab: checkout lokal `/Users/asdarsaid/JSI/api` sudah pindah branch (`persiapan-2029`) sejak dokumentasi Feature 02 ditulis, backend-nya sudah berubah tanpa disadari sebelumnya.
  2. **Modul `timses` TIDAK ADA LAGI di backend `persiapan-2029`** — `find`/`grep` ke seluruh `src/` untuk controller timses nihil. Diganti modul `user` (`@Controller('user')`, `@UseGuards(AuthGuard)`, self-service via JWT): `GET /user/profile` & `POST /user/profile` (BUKAN lagi `GET/PATCH /timses/:id`) — internal-nya tetap pakai model yang sama (`TimsesModel`, Sequelize), cuma controller/route-nya beda total (tidak ada `:id` lagi, di-scope dari token JWT). **BELUM ditemukan endpoint pengganti untuk "list SEMUA anggota timses"** (dipakai `TimsesScreen`, `LacakRelawanScreen`, dan `useLocationBeacon`) — pencarian menyeluruh ke seluruh modul backend nihil hasil. **BELUM DIPERBAIKI** (`src/services/profile.ts` & `src/services/timses.ts` masih panggil `/timses/:id` lama, akan terus 404 selama `EXPO_PUBLIC_USE_MOCK_API=false`) — ini beda kategori masalah dari #1 (bukan cuma salah base URL, tapi kontrak endpoint-nya sendiri sudah berubah/hilang), **sengaja tidak langsung diperbaiki tanpa arahan user** karena scope-nya besar (affects profile fetch, location beacon, seluruh fitur Timses/Lacak Relawan) dan endpoint list-nya belum ketemu penggantinya sama sekali.
  3. **React Navigation warning "Found screens with the same name nested inside one another"** — juga dilaporkan user, root cause terpisah (bukan terkait network): tab bernama `"Home"` (`AdminTabs.tsx`/`TimsesTabs.tsx`) membungkus `HomeStack` yang punya `Stack.Screen` root JUGA bernama `"Home"`. **Fix:** nama tab di-rename jadi `"HomeTab"` (`AdminTabParamList`/`TimsesTabParamList` & `Tab.Screen name`) — `Stack.Screen` di dalam `HomeStack.tsx` tetap `"Home"` (tidak diubah, tidak ada referensi eksternal ke nama tab lama selain di file masing-masing, dicek via grep sebelum rename).
  4. Dokumentasi `api-standards.md` § Auth Flow poin 1 & 6 dikoreksi dengan strikethrough + catatan (bukan dihapus, supaya jejak sejarah "kenapa dulu ditulis begitu" tetap ada).
  5. `npx tsc --noEmit` bersih setelah semua fix di atas. **Belum diverifikasi ulang di device** (perlu restart Expo dev server dulu, env var tidak auto-reload) — urutan tes: (a) login harus sukses sekarang, (b) tab "Rekap" DPR RI harus tetap jalan (base URL berubah tapi path `/dpr/hasil-rekap` tidak), (c) dtdoor/gotv (Program Pemenangan) harus tidak lagi 404, (d) Profil/Timses/Lacak Relawan **masih akan error** sampai #2 di atas diputuskan & dikerjakan — user perlu kasih arahan: apakah ada endpoint list-timses lain yang belum ditemukan, atau fitur ini sementara di-mock-kan lagi secara terpisah (butuh flag per-modul, bukan global) sambil modul lain pakai data real.
- 2026-08-23 — **Profil & Timses — di-wire sebagian ke backend baru, scoping wilayah dimatikan sementara** (lanjutan langsung entry di atas). Ditanya eksplisit via `AskUserQuestion` (cek endpoint lain / wire yang ada datanya saja+scoping dimatikan / skip dulu) → user pilih **"Wire yang ADA datanya saja"**. Ditemukan lebih dalam saat investigasi: `TimsesModel` (Sequelize, backend baru) cuma punya kolom `id/nik/namaLengkap/jenisKelamin/roles/statusOnline` — **`dusun`/`desa`/`kecamatan`/`no_telpon`/`lat`/`long` sudah tidak ada di database sama sekali**, bukan cuma belum di-map. Implementasi:
  1. **`src/types/profile.ts`** (`TimsesProfile`) — field yang tidak ada lagi DIHAPUS dari type (bukan `| undefined`), supaya tiap pemakaian lama ketahuan via error TypeScript, bukan diam-diam `undefined` di runtime. `UpdateProfileInput` juga kehilangan `noTelpon`.
  2. **`src/services/profile.ts`** — WIRED ke `GET/POST /user/profile` (self-service dari JWT, bukan `:id` lagi). `POST` kirim `jenisKelamin` = nilai aktif (DTO backend `@IsEnum` wajib field itu).
  3. **`src/components/profile/AccessScopeNotice.tsx`** — prop `wilayahLabel`/`isAdmin` dihapus, teks diubah jujur ("belum dibatasi per wilayah untuk sementara") — BUKAN mengklaim non-admin dapat akses admin.
  4. **`ProfileScreen.tsx`** — field "No. HP" & "Wilayah Tugas" dihapus dari form (tidak ada data backing), badge role sekarang tanpa wilayah.
  5. **Scoping wilayah dimatikan di 9 screen** (semua `profileQuery.data?.kecamatan`/`isAdmin`-gated restriction dihapus, ditemukan exhaustif via `tsc` error setelah type diubah — bukan grep manual, supaya tidak ada yang kelewat): `HomeScreen`, `TimsesScreen`, `KekuatanWilayahScreen`, `KekuatanPemilihScreen`, `SwingVoterFollowUpScreen`, `QuickCountScreen`, `DtdoorAnalyticsScreen`, `TokohMasyarakatScreen`. Pola konsisten: filter/pembatasan by kecamatan dihapus (semua role lihat semua data), picker/breadcrumb yang dulu `disabled` untuk non-admin sekarang selalu aktif untuk semua role. `isAdmin`/`useProfile`/`ADMIN_ROLES` yang jadi tidak terpakai lagi ikut dihapus dari tiap file (bukan dibiarkan jadi dead code).
  6. **`src/services/timses.ts`** — `fetchTimsesList()` (list semua anggota, dipakai `TimsesScreen`/`LacakRelawanScreen`) SENGAJA tetap `throw` pesan jelas ("endpoint sudah tidak ada di backend") — tidak ada endpoint pengganti ditemukan sama sekali untuk kapabilitas ini, bukan cuma salah path (Aturan #6: jangan mengarang endpoint). `updateOwnLocation()` (beacon lokasi) diubah jadi **no-op langsung** (bukan coba PATCH lalu gagal tiap 30 detik) — model backend tidak punya kolom lat/long lagi. `TimsesApiRecord`/`mapTimsesMember`/`parseCoordinate` (kontrak lama) dihapus total dari file, bukan dibiarkan jadi dead code.
  7. Dokumentasi `api-standards.md` § "Door To Door / GOTV / Ruang Publik / Timses" dikoreksi total dengan strikethrough (jejak sejarah kenapa dulu ditulis begitu tetap ada).
  - `npx tsc --noEmit` bersih. **Belum diverifikasi di device.** Fitur yang MASIH akan error 404/gagal-memuat setelah semua fix di atas (diketahui & diterima, bukan bug baru): "Daftar Anggota" `TimsesScreen`, peta `LacakRelawanScreen` (keduanya butuh `fetchTimsesList`). Fitur yang SEHARUSNYA sudah jalan normal: login, Profil (baca+edit nama), Program Pemenangan (dtdoor/gotv), Hasil Rekap DPR RI, dan semua screen yang tadinya scoped-by-kecamatan (sekarang tampil semua wilayah tanpa dibatasi, bukan error).
- 2026-08-23 — **Alur Verifikasi Endpoint dijadikan aturan tetap** (permintaan eksplisit user, dibahas SEBELUM lanjut ke kode: "kita atur langkah kerjanya dulu"). Ditanya eksplisit via `AskUserQuestion` (simpan permanen vs cuma sesi ini) → user pilih **simpan permanen**. Ditulis sebagai section baru `api-standards.md` § "Alur Verifikasi Endpoint" (5 langkah: cek web → cocokkan ke repo API → tes live curl kalau ragu → lapor user sebelum implementasi → update dokumentasi) dan dirujuk dari `CLAUDE.md` Aturan #6 (yang juga dikoreksi — sebelumnya masih bilang "auth/dtdoor/gotv/ruangpublik/timses" semua "sudah ada backend"-nya tanpa syarat, padahal `timses` modulnya sudah tidak ada lagi seperti ditemukan sesi ini). Alur ini persis pola yang dipakai (agak tidak terstruktur) untuk riset Hasil Rekap & Profil/Timses sebelumnya di sesi yang sama — sekarang jadi langkah baku di depan untuk SEMUA fitur berikutnya yang mau di-real-kan, bukan ditemukan sambil jalan lagi.
- 2026-08-23 — **Feature 02 (Login) — kontrak `POST /auth/login` diperbaiki total, menjalankan item terbuka "Login dulu, belum ada verifikasi independen" dari entry sebelumnya.** Dijalankan via `/new-feature` + § Alur Verifikasi Endpoint penuh (cek web → cocokkan repo API → tes live curl → lapor user via plan mode sebelum edit), ketiga sumber cocok satu sama lain:
  1. **Cloudflare TIDAK memblokir `/auth/login`** (beda dari kekhawatiran sebelumnya) — `curl POST` body `{}` balas `422` JSON asli dari NestJS (`{"message":"Validation failed","errors":{"password":"password must be a string"}}`), bukan halaman challenge HTML.
  2. **Dokumentasi lama (2026-08-21) tentang kontrak ini ternyata salah dari awal, bukan drift** — `main.ts` sudah lama pasang `CustomValidationPipe` + `TransformInterceptor` global. Field body yang benar **`username`** (bukan `nik`, walau nilainya tetap dicocokkan ke kolom `nik` di service), response sukses dibungkus **`{ message, data: {...,acces_token} }`** (bukan flat `{payload,acces_token}`), response gagal **HTTP 422 terstruktur** `{message,errors}` per jenis kegagalan (bukan selalu 500 generik). Dikonfirmasi silang dengan `client/src/pages/Login.jsx` (kirim `{username,password,location}`, baca `data.data.acces_token` dst.) — persis sama.
  3. **`src/services/auth.ts` diperbaiki**: `LoginApiResponse` type disesuaikan, body request kirim `username: input.nik` (nama variabel lokal `nik` TIDAK diganti, cuma key JSON-nya), parsing sukses baca `data.data.*`. Blok `catch` error TIDAK perlu diubah (sudah generic baca `error.response.data?.message`, kompatibel dengan status 422 baru). `loginRequestMock`/`MOCK_ACCOUNTS`/`LoginScreen.tsx` TIDAK disentuh (mock mode off, UI label "Username" sudah kebetulan cocok dengan field backend yang benar).
  4. Dikonfirmasi via grep: tidak ada file lain di `src/**` yang baca `LoginApiResponse`/`payload`/`acces_token` mentah — perubahan terisolasi penuh di `services/auth.ts`, tidak ada dampak ke `useAuth.tsx`/`RootNavigator.tsx`/`token.ts`.
  5. Dokumentasi `api-standards.md` § Auth Flow poin 3/4/5 dikoreksi dengan strikethrough + catatan (pola sama koreksi lain di file ini) — termasuk pesan error spesifik yang sekarang bisa dibedakan (`"NIK/EMAIL/USERNAME SALAH"` vs `"Password SALAH"` vs `"Validation failed"`), field `errors` per-input tersedia tapi **belum dipakai UI** (di luar scope perbaikan kontrak ini, `LoginScreen` masih 1 baris error generik).
  - `npx tsc --noEmit` bersih. **Belum diverifikasi fungsional dengan kredensial asli** — tidak boleh kirim kredensial asli lewat curl (Aturan #6), dan tidak ada device/simulator di environment ini. User perlu tes langsung: login dengan akun asli di device → cek redirect ke Admin/Timses tabs sesuai role → cek sesi persist setelah restart app. Kalau NIK/password salah, pesan error sekarang harus lebih spesifik ("NIK/EMAIL/USERNAME SALAH" atau "Password SALAH"), bukan lagi generik "Periksa kembali data yang di masukan".
  - **✅ Dikonfirmasi user login berhasil di device asli (2026-08-23).**
- 2026-08-23 — **Feature 03 (Profile) — bug envelope response ditemukan & diperbaiki, kelas bug SAMA PERSIS dengan yang baru diperbaiki di Login.** User minta lanjut ke "perubahan profile, gunakan yang ada di API saja dulu" setelah login dikonfirmasi jalan. Cek ulang `src/services/profile.ts` (yang sudah di-wire ke `GET/POST /user/profile` di entry sebelumnya) ternyata **belum ikut kena fix envelope** yang baru ditemukan di Auth Flow: `TransformInterceptor` global (`main.ts`) berlaku app-wide untuk SEMUA controller, bukan cuma `/auth/login` — jadi `GET /user/profile` juga balas `{ message, data: {...profil} }`, tapi `fetchProfile()` sempat baca `data` (hasil destructure axios) langsung sebagai record profil tanpa unwrap `.data` sekali lagi. Dikonfirmasi 2 sumber: baca `user.controller.ts` (`apiResponse('User Profile', profile)`) + `client/src/pages/dpt/layout-hook.jsx` yang eksplisit `resp.data["data"]`. **Fix:** `fetchProfile()` sekarang baca `data.data` sebelum `mapProfile()`. `updateProfile()` TIDAK kena bug ini (tidak pernah parse body response POST, langsung re-fetch via `fetchProfile()` setelah sukses — pola yang sudah benar dari awal).
  - Dicek juga: field `jenisKelamin` (satu-satunya field API yang tersedia tapi TIDAK ada di UI `ProfileScreen.tsx`) — dikonfirmasi via `context/designs/profile.png` bahwa desain memang tidak punya field ini sama sekali, jadi TIDAK ditambahkan ke UI (Aturan #1, tidak ada referensi visual) — tetap dikirim silent apa adanya (nilai profil aktif) saat update nama, sesuai keputusan sebelumnya, supaya validasi `@IsEnum` backend tidak gagal.
  - `npx tsc --noEmit` bersih. **✅ Dikonfirmasi user profil kebaca dengan benar di device asli (2026-08-23)** — bug envelope di atas terbukti jadi root cause-nya.
- 2026-08-23 — **Feature 04 (Door To Door) — TEMUAN BESAR: modul `dtdoor` backend ternyata di-desain ulang total (bukan drift kecil kelas envelope seperti Login/Profile), Feature 05 (GOTV) drift lebih kecil. Dikerjakan setelah dikonfirmasi eksplisit user via `AskUserQuestion` ("lanjut sekarang" untuk gotv, dan klarifikasi tambahan user untuk scope dtdoor).**
  1. **gotv — perbaikan kecil (path + envelope), field/DTO TETAP akurat.** `GET /gotv/data` → `GET /gotv` (root, dikonfirmasi live: path lama 404). Semua response (list/count/create) dibungkus `{message,data}` oleh `TransformInterceptor` app-wide — list **tidak punya field pagination sama sekali** (`GoTvService.get()` pakai `findAll()` biasa), `totalPage` di-infer dari jumlah baris balik. `src/services/gotv.ts` diperbaiki (`fetchGotvList`/`fetchGotvCount`/`createGotv`), field mapping (`mapGotv`) TIDAK berubah (masih snake_case, cocok persis dikonfirmasi live curl).
  2. **dtdoor — modul di-desain ulang total** (Sequelize baru `database/models/timses/Dtdoor.ts`, bukan lagi TypeORM lama): field response CAMELCASE, kategori/program-bantuan pindah dari top-level record ke array nested WAJIB `kunjungans[]` (tiap elemen: `tipePemilihId`/`pilihanPilegId`/`programBantuanId`/`merchendise`/`namaRelawan` wajib, `kontakRelawan` opsional), 3 endpoint lookup BARU ditemukan (`GET /dtdoor/tipe-pemilih` — persis sama id/label dengan `KATEGORI_DTDOOR_OPTIONS` hardcode lama, jadi asumsi id 1-7 TERNYATA BENAR meski sumbernya beda dari dugaan; `/pilihan-pileg` — konsep BARU "A"-"G" tidak pernah ada di UI mobile; `/program-bantuans` — persis `PROGRAM_BANTUAN_OPTIONS` lama), `GET /dtdoor/count` **hilang total** (diganti derive dari `meta.total`). Semua diverifikasi **live via curl** (bukan cuma baca kode): `GET /dtdoor?limit=2` balas data produksi asli, `POST /dtdoor` body kosong/sebagian balas 422 dengan daftar field wajib lengkap.
  3. **User klarifikasi scope eksplisit** (setelah ditanya "rebuild sekarang vs pisah sesi"): *"Pada halaman door to door atau program/progreskunjungan di web itu tidak ada form untuk menambah data dtd, semua form dtd ditambah lewat halaman dpt list, tapi untuk sementara kamu tambahkan saja form add dtd di halaman dtd itu sendiri dengan mengikuti form yang ada di dpt list web modal dtd"* — dicek `client/src/pages/dtdoor/dtdoor-modal2024.jsx` → `FormDataKunjungan2024.jsx` sebagai referensi field yang benar (bukan `InputKunjungan.jsx`/`FormDataKunjungan.jsx` lama).
  4. **Implementasi:**
     - `src/types/dtdoor.ts` — `CreateDtdoorInput` ditulis ulang total (field wajib baru + `kunjungans: [KunjunganInput,...]`), tipe `Dtdoor` (list/display) **TIDAK diubah** (scope dibatasi ke create form, bukan migrasi penuh fitur turunan).
     - `src/lib/api/syntheticId.ts` — tambah `generateSyntheticIdDptNumber()` (dtdoor sekarang butuh `idDpt` NUMBER, beda dari gotv yang tetap string).
     - `src/services/dtdoor.ts` — `mapDtdoor()` ditulis ulang (camelCase + fallback kategori/program-bantuan/relawan dari `kunjungans[0]` kalau kolom top-level lama kosong, supaya Kekuatan Wilayah/Kekuatan Pemilih/Ringkasan Data tetap dapat kategori dari data baru TANPA perlu diubah sendiri — **belum full-migrasi ke `kunjungans[]` sebagai sumber utama**, kalau 1 record punya beberapa kunjungan beda tipePemilih cuma yang pertama kepakai). `fetchDtdoorList`/`fetchDtdoorCount` diperbaiki (path+envelope+meta). `createDtdoor` payload baru total, default `kabId=7303`/`kelId=7303011002` (Bantaeng, kabupaten dengan data DPT lengkap yang dikonfirmasi user) untuk entri standalone — **WAJIB kabId nyata**, ditemukan bug backend baru: `DtdoorService.create()` akses `WilKab2024.findOne(...).proKode` TANPA null-check, kabId sembarang bikin crash 500 (bukan 422 rapi). Fungsi baru `fetchDtdoorLookups()` (3 GET paralel).
     - `src/hooks/useDtdoorLookups.ts` (baru) — fetch 3 lookup, `staleTime` 5 menit.
     - `src/screens/program/DtdoorFormScreen.tsx` — ditulis ulang total mengikuti field `FormDataKunjungan2024.jsx` (Nama Lengkap/NIK/Jenis Kelamin/No Telp/Jumlah Wajib Pilih/TPS/RT/RW/Desa/Kecamatan/Kabupaten + 1 section "Kunjungan": Tipe Pemilih/Pilihan Pileg/Program Bantuan/Merchandise/Nama Relawan/Kontak Relawan) — **cuma 1 kunjungan per submit** (web support multi, disederhanakan sesuai instruksi "form dulu di halaman dtd sendiri", bukan replikasi 1:1 fitur multi-kunjungan). Screen ini dipakai 2 entry point (`ProgramStack` standalone & `DptStack` linkage DPT, lihat `DtdoorFormParams`) — prefill dari `dptRecord` tetap jalan (idDpt/kabId/kelId asli dipakai kalau ada).
  5. **Yang SENGAJA belum dikerjakan (di luar scope sesi ini, dicatat bukan didiamkan):** migrasi penuh `Dtdoor`/`KATEGORI_DTDOOR_OPTIONS`-consumer (`KekuatanWilayahScreen`, `KekuatanPemilihScreen`, `SwingVoterFollowUpScreen`, `DtdoorAnalyticsScreen`, `DtdoorCard`) ke `kunjungans[]` sebagai sumber utama — saat ini cuma dapat manfaat via fallback di `mapDtdoor()`, entri lama (kategoriId terisi, tanpa kunjungan) & entri baru (kunjungan terisi) sama-sama tertangani tapi multi-kunjungan-per-record belum direpresentasikan penuh. Form multi-kunjungan (seperti web) belum dibangun, cuma 1 kunjungan per submit.
  - `npx tsc --noEmit` bersih. **Belum diverifikasi fungsional di device** — langkah tes: buka Program Pemenangan → Door To Door → buka form tambah kunjungan (standalone, dari `ProgramStack`) → isi semua field wajib termasuk section Kunjungan (dropdown Tipe Pemilih/Pilihan Pileg/Program Bantuan harus terisi dari API, bukan kosong) → Simpan → harus berhasil & kembali ke list, list & count harus ikut ter-update (bukan lagi "gagal memuat").
  6. **Bug KEDUA ditemukan dari tes device user:** submit form gagal dengan error backend `WHERE parameter "kepalaKeluargaId" has invalid "undefined" value` — `DtdoorService.create()` (backend) build `Dtdoor.findOne({where:{...,kepalaKeluargaId: createDtdoorDto.kepalaKeluargaId}})` tanpa guard `undefined`, dan mobile tidak pernah mengirim field opsional itu sama sekali. **Fix:** `createDtdoor()` (`src/services/dtdoor.ts`) sekarang SELALU sertakan `kepalaKeluarga: null, kepalaKeluargaId: null` eksplisit di body (bukan diomit) — `null` valid buat Sequelize `where` (`IS NULL`), `undefined` tidak. Didokumentasikan sebagai pola wajib untuk field opsional serupa di endpoint Sequelize manapun (`api-standards.md` § dtdoor).
  - **✅ Dikonfirmasi user submit form Door To Door berhasil di device asli (2026-08-23), setelah fix `kepalaKeluargaId` di atas.**
  7. **Laporan user: "validation failed" saat submit form GOTV/Social Event.** Didiagnosis via curl POST validation-only ke `/gotv` (body sengaja dibuat gagal validasi, TIDAK ada insert sungguhan ke tabel produksi) — ketemu 3 bug sekaligus:
     - `tps` dengan angka nol di depan ("01") ditolak — pipe validasi global mempertahankan string zero-padded, tapi DTO minta number murni. **Fix:** strip leading zero sebelum kirim (`services/gotv.ts`).
     - `kabId` — bug SAMA PERSIS `kepalaKeluargaId` di dtdoor (`GoTvController.insert()` juga `findOne({idDpt,kabId})` tanpa guard `undefined`). **Fix:** kirim `kabId: null` eksplisit.
     - `nik` ternyata WAJIB di level database (`NOT NULL` tanpa default) meski DTO bilang optional — form lama membiarkan NIK kosong, request lolos ke SQL lalu gagal mentah (`"Field 'nik' doesn't have a default value"`). **Fix:** `CreateGotvInput.nik` & `GotvFormScreen.tsx` diubah jadi wajib diisi. **Risiko belum terselesaikan (backend, bukan mobile):** DTO `nik: @IsNumber()` bikin NIK 16-digit berpotensi kehilangan presisi lewat konversi number di pipe global — didokumentasikan di `api-standards.md` § gotv sebagai known-risk, tidak bisa diperbaiki dari mobile.
     - `npx tsc --noEmit` bersih. Tidak dites live sampai sukses (POST yang berhasil akan menulis data ke tabel produksi) — cuma diverifikasi sampai titik validasi lolos. **User perlu tes ulang submit GOTV di device.**
- 2026-08-23 — **08 DPT — TEMUAN BESAR: modul DPT ternyata SUDAH ADA lengkap di backend** (`/Users/asdarsaid/JSI/api/src/dpt` + controller `provinsi`/`kabupaten`/`kecamatan`/`kelurahan`), koreksi total dari asumsi lama "belum ada repo" (`CLAUDE.md` Aturan #6, `api-standards.md`) — sama pola dengan Hasil Rekap sebelumnya. User minta lanjut ke DPT "dengan step-step yang sama dengan web". Alur: fork riset pertama gagal (selesai instan tanpa kerja, di-retry dengan instruksi lebih tegas) → laporan riset kedua dicek ulang manual (baca langsung `dpt.controller.ts`/`dpt.service.ts`/DTO/model + live curl) sebelum dipercaya, ketemu 1 koreksi terhadap laporan fork (field `tahun` di `DptEditDto` TERNYATA ada, cuma tidak dikirim web — bukan "tidak ada sama sekali" seperti klaim awal). User pilih **"Semua sekaligus"** (bukan List dulu baru CRUD menyusul) via `AskUserQuestion`.
  1. **Endpoint GET (Provinsi/Kabupaten/Kecamatan/Kelurahan/List DPT/TPS lookup/Total/Detail) cocok byte-for-byte dengan `contohdpt.ts`** (dikonfirmasi live curl, termasuk data produksi asli — id 191483 "HASMAWATI M"). Kontrak lengkap didokumentasikan di `api-standards.md` § DPT — base URL sama dengan modul lain (`EXPO_PUBLIC_API_URL`, tanpa `/api`), semua response dibungkus `{message,data,...extra}` (pola `TransformInterceptor` yang sama).
  2. **3 landmine backend ditemukan & diakali dari mobile (bukan bug mobile, dikonfirmasi baca kode langsung):**
     - Kolom `idDpt` **TIDAK PERNAH diisi** oleh `POST /dpt/2024-created` — record baru selalu `idDpt: null`. Konsekuensi: tidak bisa di-link ke Dtdoor/Gotv asli, tidak bisa di-detail-fetch, **tidak bisa dihapus** (`deleteDptRecord()` sengaja `throw` pesan jelas untuk record dengan `idDpt: null`, bukan kirim request yang pasti gagal — Aturan #6).
     - `PUT` edit match kolom **`id`** (primary key besar), padahal segment URL-nya bernama `:idDpt` dan `GET details`/`DELETE` yang sama-sama pakai path `:idDpt` justru match kolom **`idDpt`** ASLI — inkonsistensi asli backend. `updateDptRecord()` kirim `record.id` di URL, `deleteDptRecord()`/`fetchDptDetail()` kirim `record.idDpt`.
     - Edit & Delete default ke jalur data **2019** kalau `tahun`/`?tahun` tidak dikirim eksplisit — web sendiri punya bug ini (`Dpt2024EditModal` tidak pernah kirim `tahun`), **tidak diikuti** di mobile: `updateDptRecord()` selalu kirim `tahun:2024` di body, `deleteDptRecord()` selalu kirim `?tahun=2024`.
  3. **`usia` dihapus dari form Create/Edit** (`DptRecordFormScreen.tsx`) — backend tidak punya field ini di DTO sama sekali, selalu `null` untuk record baru. **`alamat` tetap di form tapi dikirim ke backend lewat key `tempatLahir`** (penamaan asli backend, dikonfirmasi baca `dpt.service.ts`: `alamat: dptDto.tempatLahir`).
  4. **List & filter DIUBAH dari client-side (fetch-penuh-sekali) jadi server-side** — `useDptList` (`src/hooks/useDptList.ts`) sekarang `useInfiniteQuery` dengan filter (`nama`/`kecId`/`kelId`/`tps`) jadi bagian queryKey, `DptListScreen.tsx` ditulis ulang total (search di-debounce 400ms, TPS options dari endpoint lookup asli — bukan lagi di-derive dari record yang sudah termuat, summary card pakai `meta.total` dari query aktif). `fetchKelurahanList()`/`useDptKelurahanList()` butuh `kabWilId` TAMBAHAN (bukan cuma `kecWilId`) — endpoint asli wajib kedua param bareng.
  5. **Fitur lain yang butuh SELURUH record kabupaten/kelurahan untuk agregasi client-side** (Target Suara laporan, Real Count TPS grouping, demo widget HomeScreen) **dipindah ke helper baru `fetchDptListAll()`/`useDptListAll()`** (`src/services/dpt.ts`/`src/hooks/useDptListAll.ts`) — loop semua halaman paginated, **hardcap 5000 record** biar tidak hang di kabupaten besar tanpa filter. Konsumen yang diperbaiki: `useTargetSuaraReport.ts`, `HomeScreen.tsx` (demo widget), `RealCountKelurahanScreen.tsx`, `TargetSuaraKelurahanScreen.tsx` (2 terakhir sekarang scope ke 1 kelurahan via filter `kelId`, bukan fetch seluruh kabupaten lagi — perbaikan efisiensi sekalian). **Ini BUKAN solusi skalabilitas sesungguhnya** untuk laporan level-kabupaten tanpa filter (Target Suara report, demo widget) — idealnya backend punya endpoint agregat sendiri, dicatat sebagai utang teknis.
  6. **Badge "sudah Door To Door/Social Event" sekarang derive dari field `dtdoor`/`gotv` di response asli** (linkage nyata via idDpt+kabId, bukan cuma flip lokal seperti sebelumnya) — TAPI cuma akurat untuk record dengan `idDpt` non-null (data impor asli), record baru (idDpt null) tidak akan pernah ke-link otomatis meski dtdoor/gotv-nya dibuat (fallback ke idDpt sintetis standalone).
  7. **`sudahTokoh` (fitur "Identifikasi Tokoh Baru dari DPT") TIDAK punya padanan di backend real sama sekali** — flag ini sekarang reset ke `false` tiap list di-refetch dari server (beda dari mock yang persist in-memory), karena tidak ada kolom untuk ini di database. Didokumentasikan sebagai keterbatasan yang diterima, bukan bug baru.
  8. `CLAUDE.md` Aturan #6 & `api-standards.md` § DPT dikoreksi total dengan strikethrough + catatan (pola sama koreksi lain di project ini).
  - `npx tsc --noEmit` bersih.
  9. **Bug KEDUA ditemukan dari tes device user (crash langsung setelah pilih kabupaten):** `Render Error: Cannot read property 'toLocaleString' of undefined` di `DptSummaryCards`/`formatCount`. Root cause: entry Decisions di atas (poin 1, dari laporan fork riset) **SALAH** soal shape `meta` — diklaim `{page,limit,offset,totalPages,total}` sama seperti dtdoor, padahal setelah dicek LANGSUNG baca `DptService.getDpt()` (bukan cuma percaya laporan fork lagi), backend cuma balas `meta:{page}` — TIDAK ADA total count dari endpoint list sama sekali. **Fix:**
     - `DptListResult` (`types/dpt.ts`) diubah dari `meta:{...5 field}` jadi `page:number, hasMore:boolean` — `hasMore` di-infer dari `data.length === limit yang diminta` (pola sama gotv), bukan dari total count yang memang tidak tersedia.
     - **Endpoint terpisah `GET /dpt/2024/total/:wilId` (sudah didokumentasikan sebelumnya tapi belum di-wire) ditambahkan** — `fetchDptTotal()`/`useDptTotal()` baru, dipakai `DptListScreen.tsx` untuk summary card (total count AKURAT yang ikut filter kecId/kelId/tps/nama aktif). **Diverifikasi live via curl** sebelum dianggap fix: `GET /dpt/2024/total/7303` → 151952 (total kabupaten Bantaeng, cocok persis field `totalDpt` dari `/kabupaten` lookup), `+kecId=730301` → 26255 (cocok persis `totalDpt` kecamatan Bissappu).
     - `fetchDptListAll()` (helper "fetch semua halaman") diubah loop-terminationnya dari cek `page >= meta.totalPages` jadi cek `!hasMore`.
  - **Pelajaran proses:** laporan riset fork (bahkan yang "berhasil", bukan yang gagal instan) tetap perlu di-spot-check ke source langsung untuk detail yang akan langsung dipakai membangun asumsi struktur data — sudah dilakukan untuk landmine #1/#2/#3 di atas (makanya ketemu duluan), TAPI terlewat untuk shape `meta` karena dianggap "sudah jelas ikut pola dtdoor". `npx tsc --noEmit` bersih setelah fix. **User perlu tes ulang dari awal** — langkah tes (urutan disarankan): (a) buka DPT → Provinsi → Kabupaten → pilih kabupaten mana saja → List DPT harus tampil TANPA crash; (b) coba search nama & filter Kecamatan/Kelurahan/TPS → summary card di atas list harus tampil angka total yang masuk akal; (c) scroll ke bawah → pagination harus lanjut fetch halaman berikutnya otomatis; (d) tambah data DPT baru → cek muncul di list; (e) edit data (baik yang baru dibuat maupun data lama) → cek perubahan tersimpan & tampil benar; (f) coba hapus data LAMA (bukan yang baru dibuat sesi ini) → harus berhasil; (g) coba hapus data yang BARU DIBUAT sesi ini → harus muncul pesan error jelas "belum bisa dihapus", BUKAN crash; (h) buka Target Suara & Real Count untuk kabupaten yang sama → pastikan tidak "gagal memuat" dan tidak hang lama.
